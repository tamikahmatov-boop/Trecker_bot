"""
Crypto Alert Bot — v25
Полный переход на Bybit (бессрочные USDT-фьючерсы, category=linear) как
ЕДИНСТВЕННЫЙ источник данных. MEXC и OKX полностью удалены из кода.

═══════════════════════════════════════════════════════
 НОВОЕ v25.10 — ТОЧНЫЕ УРОВНИ ПОДДЕРЖКИ/СОПРОТИВЛЕНИЯ НА ГРАФИКЕ
═══════════════════════════════════════════════════════
• На PNG-график (generate_chart_png) теперь наносятся уровни поддержки
  (голубые линии, подпись "S ...") и сопротивления (янтарные линии,
  подпись "R ..."), рассчитанные функцией calculate_support_resistance
  по ПЯТИ независимым факторам одновременно:
    1) фрактальные свинг-хай/лоу в нескольких окнах (2/3/5 баров) —
       структура цены;
    2) профиль объёма (Volume Profile) — зоны, где реально прошло
       больше всего сделок (Point of Control + вторичные HVN);
    3) классические Floor Trader Pivot Points (PP/R1-R3/S1-S3) по
       предыдущим суткам (Day1) — не зависят от ТФ самого графика;
    4) крайние хай/лоу видимого окна графика;
    5) "круглые" психологические уровни, шаг которых подбирается под
       масштаб цены — от BTC (~65000) до монет по ~0.00002.
• Точность обеспечивается кластеризацией (confluence): близкие цены из
  разных факторов схлопываются в один уровень с суммарным весом — линия
  на графике показывает только те уровни, где реально совпало несколько
  независимых признаков, а не любую случайную свечу. Показываются до
  SR_MAX_LEVELS (по умолчанию 3) сильнейших уровней в каждую сторону.
• Работает на тех же данных, что уже загружены для самого графика (без
  доп. запросов) + один дополнительный дешёвый запрос Day1-свечей для
  pivot points (тот же кэш get_bybit_klines, TTL=60с). Любая ошибка
  расчёта — просто пустой список уровней, график всё равно строится.

═══════════════════════════════════════════════════════
 НОВОЕ v25.8 — ИСПРАВЛЕНЫ ЛОЖНЫЕ ОШИБКИ "editMessageText: HTTP 400" В ЛОГЕ
═══════════════════════════════════════════════════════
• ПРИЧИНА: _last_pushed_settings_json (кэш "что уже отправлено в Telegram")
  — переменная в памяти процесса, она сбрасывается в None при КАЖДОМ
  перезапуске бота. После рестарта, если настройки не менялись, содержимое
  закреплённого сообщения в Telegram уже правильное — но бот об этом "не
  помнит" и всё равно пытается его обновить при первом же save_state()
  (каждые 30с или сразу после любой кнопки). Telegram в ответ на попытку
  заменить текст сообщения ТАКИМ ЖЕ текстом отвечает HTTP 400 "message is
  not modified" — это не сбой, а нормальный ответ API, но раньше он
  трактовался как ошибка и логировался как WARNING на каждой попытке.
• ИСПРАВЛЕНО:
  1) _tg_api_sync теперь отдельно ловит urllib.error.HTTPError и читает
     тело ответа (раньше при HTTP-ошибке тело терялось, и не было видно
     настоящую причину — только код HTTP);
  2) push_state_to_telegram_sync распознаёт "message is not modified" в
     description ответа и засчитывает это как успех, без warning в лог;
  3) load_state() теперь сразу после восстановления из Telegram-бэкапа
     инициализирует _last_pushed_settings_json текущим состоянием (если
     набор SETTINGS_KEYS не менялся с прошлого пуша) — так что после
     обычного рестарта без изменения настроек лишний editMessageText вообще
     не отправляется, а не просто "тихо" получает 400.
• Функционально ничего не изменилось — резервная копия настроек в Telegram
  как сохранялась и восстанавливалась при каждом рестарте/редеплое (v25.6),
  так и продолжает; просто убран ложный шум в логе и лишний HTTP-запрос.

═══════════════════════════════════════════════════════
 НОВОЕ v25.7 — УСТРАНЕНА ПРИЧИНА "ACCESS TOO FREQUENT" ОТ BYBIT, УСКОРЕНИЕ
═══════════════════════════════════════════════════════
• ГЛАВНАЯ ПРИЧИНА блокировки: klines_multi (3 HTTP-запроса — Min1+Min5+
  Min15) запрашивался для КАЖДОЙ отслеживаемой монеты на КАЖДОМ тике —
  то есть для всех нескольких сотен бессрочных фьючерсов Bybit каждые
  config.INTERVAL секунд, даже если монета вообще не приближается ни к
  одному порогу и заведомо не даст ни одного уведомления в этом тике.
  Это были сотни×3 запросов за тик, независимо от того, что реально
  происходит на рынке.
• ИСПРАВЛЕНО: перед получением klines теперь считаются (без единого
  запроса — по уже имеющимся в памяти данным) три условия, при которых
  монета вообще МОЖЕТ дать хоть одно уведомление в этом тике: порог
  разворота, порог 24ч-уведомления, порог обычного роста/падения — каждое
  с учётом своего кулдауна. Если ни одно не выполняется — klines для этой
  монеты в этом тике не запрашиваются вообще. Набор реально отправляемых
  уведомлений НЕ изменился ни на одно сообщение — изменилось только то,
  что для заведомо неинтересных монет (обычно подавляющее большинство)
  сеть и CPU больше не тратятся. Ожидаемое сокращение запросов к Bybit —
  на порядок и более, в зависимости от волатильности рынка.
• ДОПОЛНИТЕЛЬНО: добавлено распознавание самого ответа-блокировки Bybit
  (HTTP 403 с фразой "Access too frequent") — это не обычный rate-limit
  с Retry-After, а полная блокировка IP на несколько минут. Раньше бот на
  такой ответ реагировал короткими ретраями (1с/2с/3с), что бесполезно и
  может продлевать саму блокировку. Теперь при обнаружении такого ответа
  ставится общий cool-down на BYBIT_CONCURRENCY_LIMIT (по умолчанию 300с
  = 5 минут, как и просит сам Bybit в теле ответа) — ВСЕ запросы к Bybit
  (тикеры, список монет, klines, RSI, long/short ratio) в этот период
  вообще не отправляются, вместо того чтобы бесполезно долбить
  заблокировавший сервер. Статус блокировки виден в GET /health
  (bybit_blocked, bybit_block_remaining_sec).
• Мелкая чистка мёртвого кода при сквозной ревизии: удалены неиспользуемые
  get_peak_growth_24h, get_bybit_closes, pct24h_cache (нигде не читался).

═══════════════════════════════════════════════════════
 НОВОЕ v25.6 — НАСТРОЙКИ ПЕРЕЖИВАЮТ ЛЮБОЙ РЕДЕПЛОЙ RAILWAY (НЕ ТОЛЬКО РЕСТАРТ)
═══════════════════════════════════════════════════════
• ПРОБЛЕМА, которую этот патч решает: state.json (появился в v25.3) пишется
  на локальный диск контейнера. На Railway этот диск ЭФЕМЕРНЫЙ — при каждом
  редеплое (пуш кода, апдейт переменных окружения, ручной Redeploy) контейнер
  пересоздаётся из образа заново, и state.json вместе с ним пропадает, даже
  если сам код сохранения работал абсолютно правильно. Раньше это выглядело
  так: настройки переживают обычный краш/рестарт процесса, но обнуляются
  именно после редеплоя — то есть в тот момент, когда это больнее всего.
• РЕШЕНИЕ: теперь каждое изменение настроек кнопками ДОПОЛНИТЕЛЬНО
  сохраняется в закреплённое сообщение чата бота в Telegram
  (push_state_to_telegram_sync) — компактный JSON только по SETTINGS_KEYS.
  Хранилище — серверы Telegram, а не диск контейнера, поэтому это переживает
  АБСОЛЮТНО ЛЮБОЙ редеплой Railway без необходимости покупать/настраивать
  Volume. При старте бот сначала пытается восстановить настройки именно
  оттуда (pull_state_from_telegram_sync) и только если не получилось —
  откатывается на локальный state.json, и только если нет и его — на
  значения по умолчанию из config.py. Стартовое сообщение бота теперь честно
  показывает, ИЗ КАКОГО именно источника восстановлены настройки.
• last_alert_growth (внутренний кэш антидублирования сигналов, может быть
  большим) в Telegram-бэкап намеренно НЕ попадает — только в state.json:
  это не настройка, и его частичная потеря при полном редеплое не критична.
• Реализация полностью синхронная (urllib, без aiohttp) — как и уже
  существовавший notify_sync — специально для мест, где ещё нет запущенного
  event loop/сессии (старт модуля) или где async недоступен (обработчик
  сигнала SIGTERM/SIGINT).

═══════════════════════════════════════════════════════
 НОВОЕ v25.5 — ДАННЫЕ, НАДЁЖНОСТЬ, HEALTHCHECK, РЕФАКТОРИНГ
═══════════════════════════════════════════════════════
• 💧 Спред bid/ask: строка "Спред: X% (bid/ask)" — показывается только когда
  спред заметно широкий (>= SPREAD_WIDE_PCT, по умолчанию 0.05%), из того же
  тикера, что и остальные данные — без доп. запроса.
• 📊 CVD (Cumulative Volume Delta): приближённая дельта покупок/продаж за
  последние CVD_WINDOW_CANDLES свечей Min1 (по умолчанию 20), посчитанная по
  уже загруженным klines (turnover по знаку свечи) — без доп. запроса.
• ⚖️ Long/Short Ratio: доля аккаунтов в лонгах/шортах — Bybit отдаёт это
  только по одной монете за раз (нет bulk-эндпоинта), поэтому запрашивается
  только при реальном срабатывании сигнала (не на каждый тик по каждой
  монете) и кэшируется на LSR_TTL_SEC.
• Новая команда /funding SYMBOL — фандинг по конкретной монете вручную, не
  дожидаясь сигнала (funding_cache теперь не только пишется, но и читается).
• Магические числа (пороги OI/BTC-корреляции/мульти-ТФ/фандинга/ликвидаций/
  спреда) вынесены в именованные константы с getattr(config, ..., default) —
  поведение по умолчанию не изменилось, но теперь настраиваемо без правки
  кода.
• Надёжность: get_prices()/get_symbols() теперь используют
  _bybit_get_with_retry — до 3 попыток с экспоненциальным backoff вместо
  одной попытки; кратковременный сбой сети больше не пропускает весь тик.
  Per-symbol запросы (klines, RSI, long/short ratio) ограничены общим
  семафором BYBIT_CONCURRENCY_LIMIT, чтобы при массовом одновременном
  срабатывании сигналов не упереться в rate-limit биржи.
• Healthcheck: новый эндпоинт GET /health (порт HEALTH_PORT, по умолчанию
  8081) для внешнего мониторинга (Railway/UptimeRobot/Prometheus blackbox) —
  отдельно от Prometheus-метрик, отдаёт JSON со статусом и счётчиками.
  Полностью изолирован от остальных фоновых тасков (как liquidation_listener)
  — его сбой не приводит к перезапуску бота.
• Рефакторинг: format_growth_alert/format_drop_alert/format_reversal_alert
  больше не дублируют один и тот же код форматирования строки "24ч",
  RSI-значения, MACD и RSI-ТФ — вынесены в общие хелперы
  (format_pct24h_line, format_rsi_tf_line, _fmt_rsi_value, _fmt_macd_value).
  Текст самих уведомлений не изменился — это чисто внутренний рефакторинг.
• Тестируемость: точка входа (acquire_single_instance_lock + основной цикл)
  обёрнута в `if __name__ == "__main__":` — модуль теперь можно безопасно
  импортировать (например, в юнит-тестах форматирующих функций) без побочных
  эффектов вроде захвата файлового лока и запуска бесконечного цикла
  поллинга. Поведение при обычном запуске (`python main_4.py`) не меняется.
• Добавлены юнит-тесты форматирующих функций (см. отдельный файл
  test_format.py) — покрывают format_growth_alert/format_drop_alert/
  format_reversal_alert/build_extra_market_context/format_funding_line/
  format_cvd_line/format_lsr_line/format_spread_line/format_pct24h_line.

═══════════════════════════════════════════════════════
 НОВОЕ v25.4 — ФАНДИНГ (СТАВКА ФИНАНСИРОВАНИЯ) ВО ВСЕХ УВЕДОМЛЕНИЯХ
═══════════════════════════════════════════════════════
• 💰 Фандинг: get_prices() дополнительно разбирает поля fundingRate и
  nextFundingTime из того же самого тикера Bybit, что уже используется для
  цены/24ч-изменения/OI — без единого лишнего HTTP-запроса. fundingRate
  приходит от Bybit долей (например "0.0001"), переводится в проценты
  (×100) точно так же, как уже сделано для price24hPcnt — никаких
  придуманных коэффициентов.
• Строка "💰 Фандинг: +X.XXXX% — <кто платит кому> · след. через Hч ММм"
  добавлена во ВСЕ уведомления, где показываются рыночные данные по
  монете: сигналы РОСТА и ПАДЕНИЯ (БЛОК 2), сигнал РАЗВОРОТА (БЛОК 1) — через
  общий build_extra_market_context(), и отдельное уведомление
  "🌐 ИЗМЕНЕНИЕ ЗА 24Ч" (БЛОК 1.6). Знак ставки — как есть у Bybit:
  положительная → лонги платят шортам, отрицательная → шорты платят лонгам.
  Строка не показывается только если Bybit не прислал fundingRate для
  монеты (например, в момент временных сбоев ответа тикера) — это чисто
  информационное поле, оно ничего не фильтрует и не блокирует сами сигналы,
  как и OI/ликвидации/24ч-изменение.
• Мелкие попутные исправления при ревизии кода: pct24h_cache и
  funding_cache теперь тоже очищаются от "мёртвых" монет вместе с
  price_history/oi_history/_liq_events (раньше туда попадали значения и
  оставались там навсегда даже для монет, переставших торговаться —
  небольшая, но постоянная утечка памяти на долго работающем процессе).

═══════════════════════════════════════════════════════
 НОВОЕ v25.3 — НАСТРОЙКИ ПЕРЕЖИВАЮТ ПЕРЕЗАПУСК, УБРАН СИГНАЛ "КРУПНОЕ ДВИЖЕНИЕ",
 УВЕДОМЛЕНИЯ О НЕРАБОТЕ БОТА
═══════════════════════════════════════════════════════
• Все настройки, изменяемые кнопками/командами в Telegram (пороги %, окна,
  параметры разворота, RSI-фильтры, график, регулировка по 24ч, пауза и
  т.д. — полный список в SETTINGS_KEYS), теперь сохраняются в state.json и
  восстанавливаются при следующем запуске. Раньше в state.json попадал
  только last_alert_growth — после любого перезапуска (сбой, редеплой,
  рестарт контейнера) все настройки молча откатывались на значения по
  умолчанию из config.py. Сохранение происходит: сразу после обработки
  каждого сообщения в Telegram, каждые 30с фоновым save_state_loop, и при
  получении SIGTERM/SIGINT. Запись в state.json атомарная (через .tmp +
  os.replace), чтобы падение прямо во время записи не оставило битый файл.
• Удалён отдельный сигнал «🔔 КРУПНОЕ ДВИЖЕНИЕ — РОСТ/ПАДЕНИЕ» (бывший
  БЛОК 1.5 в monitor(), кнопка/команда /notify_pct, флаг
  NOTIFY_BIG_MOVE_PCT). Обычные сигналы роста/падения (БЛОК 2) и разворота
  (БЛОК 1), а также отдельное уведомление «Изменение за 24ч» (БЛОК 1.6,
  кнопки 📈 Свой % (24ч) / 📈🚦) — не затронуты и продолжают работать как
  раньше.
• Добавлены уведомления в Telegram о том, что бот НЕ работает: при
  критическом сбое во внешнем retry-цикле, при неожиданном падении
  фонового процесса, при получении сигнала остановки (SIGTERM/SIGINT) и
  при штатном завершении процесса. Используется новая функция
  notify_sync() — работает без asyncio/aiohttp (чистый urllib), поэтому
  доставляет сообщение, даже если основной event loop и HTTP-сессия уже
  недоступны. Существующие уведомления (watchdog о зависании monitor(),
  падение фонового таска) сохранены без изменений.

═══════════════════════════════════════════════════════
 НОВОЕ v25.2 — «ИЗМЕНЕНИЕ ЗА 24Ч» СТАЛО ОТДЕЛЬНЫМ УВЕДОМЛЕНИЕМ
═══════════════════════════════════════════════════════
• Регулировка по 24ч (кнопки 📈 Свой % (24ч) и 📈🚦 24ч-регулировка ВЫКЛ/ВКЛ,
  а также команда /set_pct24h) теперь управляет ТОЛЬКО отдельным, новым
  типом уведомления «🌐 ИЗМЕНЕНИЕ ЗА 24Ч» (БЛОК 1.6 в monitor()) — своя
  логика, свой кулдаун (_pct24h_notify_cooldown, 1 час на монету), не
  делит cooldown с обычными сигналами.
• Обычные сигналы РОСТА/ПАДЕНИЯ (БЛОК 2) снова фильтруются ТОЛЬКО
  внутренним порогом current_percent/current_window — точно как было до
  v25. Регулировка по 24ч на них больше не влияет никак.
• Сигнал РАЗВОРОТА (БЛОК 1) как не зависел от порога по 24ч, так и не
  зависит.
• Во всех трёх типах сигналов (рост/падение/разворот) строка
  «🌐 24ч: +X.XX% (Bybit)» остаётся — она информационная и показывается
  всегда, независимо от состояния кнопок 📈🚦, ровно как и просил запрос.
• Само уведомление «🌐 ИЗМЕНЕНИЕ ЗА 24Ч» тоже отправляется через
  broadcast_with_chart — прикладывает скриншот графика (если кнопка
  🖼 График включена), с тем же fallback на текст без графика.

═══════════════════════════════════════════════════════
 НОВОЕ v25.1 — ЛИКВИДАЦИИ / OI / МУЛЬТИ-ТФ / КОРРЕЛЯЦИЯ С BTC
═══════════════════════════════════════════════════════
Всё ниже — ЧИСТО ИНФОРМАЦИОННЫЕ добавки в текст сигналов роста, падения и
разворота. Ничего не фильтрует и не блокирует сами сигналы, новых кнопок
не добавлено — как и было явно попрошено.
• 🧨 Ликвидации: фоновая задача liquidation_listener() держит одно WS-
  соединение к Bybit (topic allLiquidation.{symbol}, официальный публичный
  канал — REST-эндпоинта для истории ликвидаций у Bybit не существует).
  Копит объём ликвидированных лонгов/шортов в USDT по каждой монете за
  скользящее окно (30 мин по умолчанию) и добавляет строку вида
  "🧨 Ликвидации 30м: лонги $12.3K │ шорты $45.6K — шорты доминируют 🟢"
  в сигналы. Полностью изолирована от остального бота: любая ошибка WS
  (обрыв, недоступность) просто означает отсутствие строки в сигнале —
  не влияет ни на мониторинг цен, ни на другие функции, и не считается
  "падением фонового процесса" (не роняет и не перезапускает весь бот).
• 📦 Открытый интерес (OI): openInterestValue разбирается из того же
  тикера Bybit, что и цена/24ч-изменение (без единого лишнего запроса).
  % изменения OI считается за то же окно, что и рост/падение цены, и
  добавляется в сигнал с пометкой "подтверждает движение" (OI растёт в
  ту же сторону) или "шорт-сквиз/закрытие позиций" (OI падает).
• 🕐 Мульти-ТФ подтверждение: по уже загруженным свечам 1м/5м/15м (без
  доп. HTTP) проверяется, совпадает ли направление движения на каждом
  ТФ с направлением сигнала — строка вида "🕐 Подтверждение ТФ: 1м✅ 5м✅
  15м❌ (2/3)".
• ₿ Корреляция с BTC: сравнивается рост/падение сигнальной монеты с
  ростом/падением BTCUSDT за то же окно — помогает отличить движение
  всего рынка от локального движения по конкретной монете.

═══════════════════════════════════════════════════════
 НОВОЕ v25 — СКРИНШОТ ГРАФИКА + РЕГУЛИРОВКА ПО 24Ч
═══════════════════════════════════════════════════════
• 🖼 Скриншот графика в сигналах: к сигналам РОСТА, ПАДЕНИЯ и РАЗВОРОТА теперь
  прикладывается PNG-график (свечи 1H, ~72 последних часа — таймфрейм 1ч,
  как на самом Bybit), генерируется прямо в Python (matplotlib) в реальном
  времени по свежим свечам с GET /v5/market/kline?interval=60. Тёмная тема,
  зелёные/красные свечи — визуально похоже на сам Bybit. Если matplotlib
  недоступен или график не построился — бот НЕ падает и НЕ теряет сигнал:
  автоматически шлётся обычное текстовое сообщение (полный fallback).
  Если текст алерта длиннее лимита Telegram на подпись к фото (1024
  символа, актуально для разворотов с длинным списком факторов) — фото
  уходит без подписи, а следом отдельным сообщением полный текст.
• 🖼 График ВЫКЛ / 🖼 График ВКЛ — новая пара кнопок (флаг CHART_ENABLED,
  по умолчанию включено), полностью отключает/включает прикладывание
  скриншота ко всем сигналам (не трогая остальную логику самих сигналов).
• 🌐 Изменение за 24ч (как на Bybit): get_prices() теперь дополнительно
  разбирает поле price24hPcnt из того же тикера Bybit (без единого лишнего
  HTTP-запроса — оно приходит в том же ответе, что и цена) и добавляет
  строку "🌐 24ч: +X.XX% (Bybit)" в сигналы РОСТА, ПАДЕНИЯ и РАЗВОРОТА —
  показывается ВСЕГДА, независимо от состояния регулировки ниже.
• 📈 Свой % (24ч) — новая кнопка: нажатие переводит бота в режим ожидания
  числа, следующим сообщением просто вводится процент от 1 до 500 (без
  команды) — устанавливается порог CUSTOM_PCT_24H, по которому (при
  включённой регулировке) фильтруются обычные сигналы роста/падения —
  теперь не по внутреннему окну (current_percent/current_window), а по
  готовому 24ч-изменению цены с Bybit (price24hPcnt), точно как на самой
  бирже. Доступна и команда /set_pct24h 12.5.
• 📈🚦 24ч-регулировка ВЫКЛ / ВКЛ — новая пара кнопок (флаг
  PCT24H_GATE_ENABLED, по умолчанию включено). ВКЛ — сигналы роста/падения
  фильтруются порогом CUSTOM_PCT_24H по 24ч-изменению (как на Bybit).
  ВЫКЛ — фильтрация возвращается к прежней схеме (порог current_percent по
  внутреннему окну current_window), но строка "🌐 24ч: ..." в сообщениях
  как показывалась, так и продолжает показываться — это чисто
  информационное поле, отключение регулировки его не прячет.

═══════════════════════════════════════════════════════
 НОВОЕ v24 — ПОЛНЫЙ ПЕРЕХОД НА BYBIT
═══════════════════════════════════════════════════════
• Список монет (get_symbols): раньше собирался парсингом HTML-листинга
  директорий public.bybit.com/trading/ и /spot/ (страницы со списком файлов
  исторических дампов) — хрупкий способ, зависящий от структуры страницы.
  Теперь используется официальный REST-эндпоинт Bybit V5
  GET /v5/market/instruments-info?category=linear — прямой список всех
  активных бессрочных USDT-фьючерсов (LinearPerpetual, status=Trading),
  с пагинацией через cursor. Надёжнее и быстрее.
• Цены (get_prices): раньше цена бралась по очереди с MEXC
  (contract.mexc.com) и OKX (www.okx.com), с "мержем" первого не-None
  результата. Оба источника полностью удалены. Теперь один запрос к
  GET /v5/market/tickers?category=linear — цены сразу по ВСЕМ бессрочным
  фьючерсам Bybit за один HTTP-вызов (быстрее, без гонки между биржами,
  без риска расхождения цены между источниками для одного и того же сигнала).
  Поле "источник" в алертах теперь всегда "Bybit".
• Свечи/индикаторы (RSI, StochRSI, MACD, EMA, ATR, Bollinger, паттерны,
  OBV, Wick Rejection, Lower Highs и т.д.): раньше грузились с
  contract.mexc.com/api/v1/contract/kline. Теперь — с официального
  GET /v5/market/kline?category=linear биржи Bybit, для тех же
  таймфреймов (Min1×120, Min5×100, Min15×60) и с той же TTL-кэш логикой
  (60с). Bybit отдаёт свечи от новых к старым — добавлен разворот списка
  в хронологический порядок (как и ожидают pandas/ta индикаторы).
• Конвертация символа под формат другой биржи (_to_mexc_symbol,
  добавлявшая "_" перед USDT) удалена за ненадобностью — Bybit API
  использует те же тикеры без разделителя ("BTCUSDT"), что и внутренний
  список монет, конвертация больше не нужна нигде в пайплайне.
• Функция get_rsi_trend_from_mexc (не вызывалась нигде, дублировала уже
  используемый calculate_rsi_trend(closes) на данных, которые и так
  загружены) — удалена как мёртвый код.
• normalize_symbol/​_norm_cache — использовались только для сведения
  разных нотаций тикеров MEXC/OKX к единому виду; с одним источником
  данных (Bybit, единая нотация) больше не нужны — удалены.
• Все текстовые упоминания MEXC/OKX в сообщениях бота (RSI-фильтр роста
  и т.п.) заменены на Bybit.

═══════════════════════════════════════════════════════
 НОВОЕ v23
═══════════════════════════════════════════════════════
• 🔄🚦 RSI разворота ВЫКЛ / 🔄🚦 RSI разворота ВКЛ — новая пара кнопок (флаг
  RSI_REVERSAL_FILTER_ENABLED). До этого REVERSAL_RSI_OB/RSI_REVERSAL_TF были
  ЖЁСТКИМ гейтом: разворотный сигнал не проверялся вообще (даже 20 факторов
  не считались), пока RSI на выбранном ТФ не достигал порога — независимо от
  score. Кнопка ВЫКЛ полностью снимает это условие: решение принимает только
  REVERSAL_MIN_SCORE из факторов.
• При выключенном гейте пропускается доп. HTTP-запрос get_rsi_for_tf() на
  RSI_REVERSAL_TF (экономия сети на каждой проверке потенциального
  разворота). Фактор 15 (один из 20) при этом не исчезает — падает на
  встроенный fallback (RSI по Min15 из уже загруженных klines_15m).
• _rsi_reversal_status_line(): аналог _rsi_signal_status_line, но для
  гейта разворота — используется в /menu, 🆘 Помощь, /status и
  ⚙️ Настройки разворота вместо дублирования условной логики.
• БАГ: format_drop_alert() показывал для алертов ПАДЕНИЯ строку
  "(порог ≤100-уровень)" — формула, не отражающая никакую реальную логику
  фильтрации (RSI-порог в принципе не применяется к падению, только к
  росту). На практике не проявлялся, т.к. вызывающий код всегда передавал
  rsi_tf_level=None, но формула оставалась мёртвым и вводящим в заблуждение
  кодом. Убрана — теперь для падения показывается только значение RSI, без
  придуманного порога.
• Полный построчный аудит файла (все ~50 функций) на предмет багов —
  новых проблем, помимо вышеуказанной, не найдено; логика инициализации
  БД/кэша, cooldown-фильтров, очистки памяти и обработки сигналов
  подтверждена корректной.

═══════════════════════════════════════════════════════
 НОВОЕ v22
═══════════════════════════════════════════════════════
• db_connect(): раньше открывал НОВОЕ sqlite3-соединение (+ заново выставлял
  PRAGMA journal_mode/synchronous) на КАЖДЫЙ вызов — db_save_alert,
  db_get_alert_level, db_set_alert_level и т.д. Теперь используется одно
  переиспользуемое соединение (_get_shared_db_conn), которое открывается
  один раз и живёт всё время работы процесса — меньше файлового I/O на
  каждую операцию с БД, особенно заметно при частых алертах/разворотах.
• db_vacuum() сознательно НЕ переиспользует общее соединение: она
  выполняется в отдельном потоке через run_in_executor, и делить один
  sqlite3.Connection между этим потоком и основным event-loop потоком
  небезопасно — у неё теперь своё независимое короткоживущее соединение.
• db_close(): новая функция, закрывает общее соединение (сбрасывает WAL на
  диск) при штатной остановке процесса — вызывается в finally-блоке рядом
  с release_single_instance_lock().
• Логика проверена изолированными функциональными тестами (переиспользование
  соединения, commit/rollback, параллельный VACUUM, close/reopen) — все
  сценарии отработали корректно.

═══════════════════════════════════════════════════════
 НОВОЕ v21
═══════════════════════════════════════════════════════
• 🚦 RSI-фильтр ВЫКЛ / 🚦 RSI-фильтр ВКЛ — новая пара кнопок (и флаг
  RSI_SIGNAL_FILTER_ENABLED) для сигналов роста/падения. ВЫКЛ полностью
  отключает проверку RSI: алерты идут только по порогу % (current_percent),
  ТФ/уровень RSI (кнопки 📊 RSI ТФ / RSI ≥) при этом сохраняются в памяти,
  но не применяются — повторное нажатие ВКЛ возвращает их в работу без
  необходимости настраивать заново. Не путать с 🔄📊 RSI-гейтом разворота
  (REVERSAL_RSI_OB / RSI_REVERSAL_TF) — это отдельный, самостоятельный фильтр.
• Оптимизация: при выключенном RSI-фильтре пропускается сам HTTP-запрос
  get_rsi_for_tf() за RSI на отдельном таймфрейме (когда RSI_SIGNAL_TF
  отличается от Min1) — раньше запрос уходил всегда, даже если порог
  фактически не использовался для фильтрации падения.
• format_growth_alert/format_drop_alert: строка "📊 RSI {ТФ}: ..." в самих
  алертах теперь не показывается, пока фильтр выключен — меньше шума в
  сообщении, раз показатель в этот момент ни на что не влияет.
• _rsi_signal_status_line(): вынесена общая функция формирования текста
  "ТФ X, порог ≥Y / ВЫКЛЮЧЕН" — раньше одна и та же условная строка была
  продублирована в /menu, 🆘 Помощь и 📊 Статус по отдельности (DRY).
• Хардкоженные версии бота в текстах ("v19" в /start, /menu, 🆘 Помощь и
  "v17" в стартовом broadcast monitor()) не были обновлены при переходе на
  v20 — приведены к единому виду.

═══════════════════════════════════════════════════════
 НОВОЕ v20 — ПОЛНЫЙ АУДИТ
═══════════════════════════════════════════════════════
• telegram_loop(): ГЛАВНЫЙ ФИКС. При ошибке getUpdates (в т.ч. 409 Conflict)
  бот раньше повторял запрос немедленно — пауза была всего 0.2с независимо
  от результата. При конфликте двух инстансов (например, во время
  Railway-редеплоя, когда старый и новый контейнер на короткое время
  работают одновременно) это означало ~5 запросов/сек к getUpdates,
  что НЕ давало конфликту разрешиться и забивало логи бесконечными 409.
  Теперь при ошибке включается экспоненциальный backoff: 1с → 2с → 4с →
  ... → потолок 30с. Как только Telegram снова отвечает ok=true — backoff
  сразу сбрасывается на 1с. Это не убирает 409 полностью (если второй
  процесс реально жив), но даёт системе шанс на саморазрешение и не
  усугубляет конфликт активным спамом запросов.
• telegram_loop(): после 10 ошибок подряд бот один раз (не на каждую
  ошибку) шлёт владельцу предупреждение через sendMessage — этот метод
  НЕ конфликтует с getUpdates, поэтому сообщение дойдёт даже во время
  активного 409-конфликта на long-polling. Текст прямо объясняет, что
  нужно искать второй процесс с тем же BOT_TOKEN (другой сервер/
  Railway-сервис/локальный запуск), а не молча копить ошибки в логах.
• ВАЖНО про single-instance lock (bot.lock, добавлен в v19): этот лок
  на уровне ОС защищает только от двух процессов В ОДНОМ контейнере/
  файловой системе. На Railway каждый деплой — это отдельный контейнер
  с собственным диском, поэтому bot.lock одного контейнера НЕ видит
  bot.lock другого. Если 409 идёт постоянно (не только в момент
  редеплоя) — следует искать дублирующий Railway-сервис/проект или
  локально запущенный процесс с тем же токеном, lock здесь не поможет
  и не должен восприниматься как полная защита от cross-container
  конфликтов.
• format_reversal_alert и _cmd_reversals: уровни уверенности (🔴🟠🟡)
  были откалиброваны под старый максимум в 16 факторов (10/16≈62%,
  6/16≈37%) и не пересчитаны при добавлении факторов 17-20 в v18.
  Из-за этого 10/20 (всего 50% сработавших факторов) ошибочно
  показывалось как "ВЫСОКАЯ" уверенность. Пересчитано пропорционально
  под 20: высокая ≥12 (60%), средняя ≥7 (35%), слабая <7.
• handle_message: версия бота в приветственных сообщениях (/menu,
  /help, автоподписка для не-владельца) была захардкожена как "v16"
  ещё с ранних версий и не обновлялась — исправлено на v19/v20 по месту.
• detect_short_reversal: убрана мёртвая инициализация stoch_rsi_val=None
  в начале фактора 1, которая немедленно перезаписывалась в факторе 2 —
  не баг, но лишний код, могущий ввести в заблуждение при чтении.

═══════════════════════════════════════════════════════
 v19 — ИСТОРИЯ ИЗМЕНЕНИЙ
═══════════════════════════════════════════════════════
• Single-instance lock через fcntl.flock на файл bot.lock (путь
  настраивается через config.LOCK_FILE). Раньше при случайном запуске
  второй копии бота оба процесса начинали бесконечно конфликтовать за
  getUpdates, Telegram отвечал 409 Conflict, сообщения терялись/дублировались,
  а причина была не очевидна из логов.
  Теперь второй процесс при старте сразу видит занятый лок, печатает
  понятную ошибку с инструкцией (ps aux | grep python3 + что убить) и
  завершается с exit code 1 — вместо тихого зависания в цикле 409-ошибок.
• Лок — на уровне ОС (POSIX advisory lock), не PID-файл: если процесс
  убит любым способом (kill -9, OOM killer, краш сервера), ядро снимает
  лок автоматически. Никаких "залипших" lock-файлов со старым PID,
  которые пришлось бы вручную чистить.
• Лок захватывается ОДИН раз на весь жизненный цикл процесса, до входа
  во внутренний retry-цикл (while True: asyncio.run(main())), а не
  внутри main() — иначе при каждом внутреннем перезапуске после
  необработанного исключения возникало бы окно release→acquire.
• Если fcntl недоступен (не-POSIX окружение) — бот не падает, просто
  логирует предупреждение, что защита от двойного запуска отключена.

═══════════════════════════════════════════════════════
 v18 — ИСТОРИЯ ИЗМЕНЕНИЙ
═══════════════════════════════════════════════════════
• 17. MACD гистограмма падает (Min1) — быстрое подтверждение разворота,
      не дожидаясь Min5-сигнала (фактор 4).
• 18. Цена растянута >1.5% над EMA21 (Min5) — перегрев тренда,
      дополняет крест/gap EMA (фактор 9) количественной мерой растяжения.
• 19. Серия из 3+ красных свечей подряд (Min1) — прямой признак давления
      продавцов, не пересекается с Wick Rejection (фактор 14).
• 20. StochRSI разворачивается вниз из экстремума >0.85 (Min1) — отличается
      от фактора 2 (статичная перекупленность): здесь ловится именно момент
      разворота индикатора, а не просто нахождение в зоне.
  Все 4 фактора используют уже загруженные Min1/Min5 свечи —
  никаких дополнительных HTTP-запросов, скорость не падает.
• max_score теперь 20 везде: кнопки, /rev_score (1–20), /rev_score_delta
  (0–20), _cmd_reversal_settings, format_reversal_alert, лог разворотов.
• Кнопки порога разворота: 3/20, 5/20, 7/20, 9/20, 12/20, 15/20.

═══════════════════════════════════════════════════════
 v17 — ИСТОРИЯ ИЗМЕНЕНИЙ
═══════════════════════════════════════════════════════
• handle_message: первый блок `if text == "/start"` перехватывал /start
  ДО проверки chat_id, из-за чего владелец бота (CHAT_ID) никогда не видел
  полное меню с клавиатурой — получал только короткое сообщение и return.
  Удалён мёртвый/конфликтующий блок, /start теперь всегда даёт владельцу
  полное меню, а подписчикам — приветствие + автоподписку.
• monitor(): стартовое сообщение говорило "v12 — 12-факторный разворот"
  хотя бот уже v16 с 16 факторами — исправлено на v16/16.
• checks_count инкрементировался дважды за итерацию (в начале цикла и в
  блоке периодической очистки) — счётчик циклов был врут в 2 раза чаще
  триггерил периодическую очистку памяти. Дубликат убран.
• log.info при REVERSAL использовал переменную price_change, которая не
  определена если last_info is None (первый сигнал по монете) → NameError,
  тихо проглатываемый внешним try/except, но ломавший лог. Исправлено.
• Умный фильтр повторных разворотов: при пропуске дубликата `continue`
  пропускал ВЕСЬ остаток итерации по монете — включая обычные алерты
  роста/падения. Теперь пропускается только разворотный сигнал.
• Все кнопки и тексты "X/12 факторов" заменены на корректные "X/16"
  (клавиатура порога разворота, /menu, /reversal_settings, /reversals).
• Кнопки порога разворота расширены и переименованы:
  3/16, 5/16, 9/16, 12/16 (агрессивный → стандарт → строгий → очень строгий).
• _cmd_reversals: пороги confidence (🔴🟠🟡) синхронизированы с
  format_reversal_alert (10/6 вместо устаревших 8/5).
• /rev_score_delta: диапазон валидации расширен с 0–12 до 0–16
  (соответствует максимальному скору в 16-факторном детекторе).
• _rev_cmds: сообщение об ошибке для /rev_cooldown показывало текущее
  значение в СЕКУНДАХ при том, что диапазон ввода — в МИНУТАХ.
  Теперь оба значения в одних единицах (минуты).
• Стандартный пресет приведён к единому виду: REVERSAL_MIN_SCORE по
  умолчанию = 5 (как заявлено в пресетах), было 4.

═══════════════════════════════════════════════════════
 НОВЫЕ ВОЗМОЖНОСТИ v17
═══════════════════════════════════════════════════════
• 🔄 Полный перезапуск — новая кнопка и команда /restart. Безопасно
  пересоздаёт asyncio.Task для monitor() БЕЗ потери price_history,
  БД, подписчиков и текущих настроек. Кулдаун 60с защищает от спама.
  Использует общую функцию restart_monitor(), которую теперь вызывает
  и watchdog (устранено дублирование кода).
• 🆘 Помощь — новая кнопка с кратким описанием текущих настроек и кнопок,
  отдельно от полного /menu.
• restart_monitor(reason) — переиспользуемая безопасная функция
  перезапуска цикла мониторинга (раньше код перезапуска дублировался
  только внутри watchdog).

═══════════════════════════════════════════════════════
 v16 — ИСТОРИЯ ИЗМЕНЕНИЙ (см. предыдущие правки)
═══════════════════════════════════════════════════════
• db_connect: не делал commit() → INSERT мог не сохраняться. Добавлен commit/rollback.
• _fetch_bybit_klines (ранее _fetch_mexc_klines): не запрашивал поле "open" → detect_candle_pattern использовал
  closes[-2] как open текущей свечи → Shooting Star никогда не срабатывал.
  Исправлено: opens теперь запрашиваются и передаются в detect_candle_pattern.
• detect_candle_pattern: полностью переписан — реальные opens, добавлен паттерн
  "Вечерняя звезда" (3 бара), исправлена логика тела/тени.
• monitor: get_rsi_trend_from_bybit() делал отдельный HTTP запрос хотя closes уже есть.
  Заменено на calculate_rsi_trend(closes).
• price_change использовалась в log.info но могла быть не определена → NameError.
• import os был внутри db_size_mb() → перемещён на уровень модуля.
• 🧹 БД Очистка: выводила Python dict → заменена на читаемый текст.
• _esc() определялась внутри format_reversal_alert при каждом вызове → перенесена
  на уровень модуля (DRY).
• MACD_SLOPE дефолт -0.000005 слишком мал → никогда не срабатывал slope-ветка.
• get_bybit_klines_multi: fallback dict не содержал "opens" → KeyError в паттернах.
• _kline_cache / _alert_cooldown / price_history: не чистились → утечка памяти.
  Добавлена периодическая очистка в monitor loop.

═══════════════════════════════════════════════════════
 ФАКТОРЫ РАЗВОРОТА (16/16)
═══════════════════════════════════════════════════════
• 13. OBV-дивергенция (Min15): цена растёт, On-Balance Volume падает.
      Сигнал ослабления покупательского давления.
• 14. Wick Rejection Ratio > 0.55 (Min1): среднее отношение верхней тени
      к диапазону за 3 свечи. Указывает на систематическое отталкивание цены от верха.
• 15. RSI старшего TF > 75 (Min15): перекупленность на 15-минутном таймфрейме
      в сочетании с 1m-сигналами даёт подтверждение разворота.
• 16. Lower Highs паттерн (Min5): 3+ последовательных снижения максимумов —
      структурный медвежий разворот.

═══════════════════════════════════════════════════════
 ДАННЫЕ (3 таймфрейма параллельно)
═══════════════════════════════════════════════════════
• Min1  x120: RSI, StochRSI, BB, паттерны (с реальными opens), wick rejection
• Min5  x100: MACD, EMA, ATR, моментум, объём, lower highs
• Min15 x60:  OBV-дивергенция, RSI старшего TF, RSI-дивергенция
  Все три таймфрейма загружаются параллельно через asyncio.gather.

═══════════════════════════════════════════════════════
 ДРУГИЕ ОСОБЕННОСТИ
═══════════════════════════════════════════════════════
• format_reversal_alert: RSI 1m + RSI 15m, Wick ratio, 3 цели Фибо (38.2/50/61.8%)
• Уровни уверенности: слабый <6, средний 6-9, высокий 10+
• _cmd_reversal_settings: описание всех 16 факторов
• Пресеты: агрессивный /rev_score 3, стандарт /rev_score 5, строгий /rev_score 9
• /rev_score принимает значения 1–16

═══════════════════════════════════════════════════════
 О CoinGlass
═══════════════════════════════════════════════════════
Ликвидации, открытый интерес и (с v25.4) funding rate уже реализованы БЕЗ
CoinGlass — напрямую с публичного API Bybit (WS allLiquidation.{symbol} и
поля openInterestValue/fundingRate из тикера), без платного ключа.
Интеграция именно с CoinGlass (например, ради long/short ratio по всему
рынку или сводных данных сразу по нескольким биржам) НЕ включена в эту
версию — требует платного API-ключа CoinGlass. Архитектура
detect_short_reversal() уже модульная: для добавления CoinGlass как
17-го+ фактора достаточно написать async-функцию получения данных и
добавить её в get_bybit_klines_multi()-подобный параллельный gather, затем
добавить блок в detect_short_reversal(). При наличии ключа — обращайтесь,
добавим этот функционал отдельным патчем.
"""

from __future__ import annotations

import asyncio
import csv
import fcntl
import io
import json
import logging
import math
import os
import random
import signal
import sqlite3
import sys
import time
from contextlib import contextmanager
from typing import Optional

import aiohttp
from aiohttp import web
import pandas as pd
from ta.momentum import RSIIndicator
from ta.trend import MACD, EMAIndicator
from ta.volatility import BollingerBands, AverageTrueRange

import config

# ── Prometheus (опционально) ──────────────────────────────────────────────────
try:
    from prometheus_client import Counter, Gauge, start_http_server
    PROM_SIGNALS   = Counter("bot_signals_total",   "Всего сигналов")
    PROM_CHECKS    = Counter("bot_checks_total",    "Всего циклов")
    PROM_COINS     = Gauge("bot_tracked_coins",     "Монет в истории")
    PROM_REVERSALS = Counter("bot_reversals_total", "Разворотных сигналов")
    PROM_AVAILABLE = True
except ImportError:
    PROM_AVAILABLE = False

# ================================================================
#  LOGGING
# ================================================================

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s %(levelname)s [%(funcName)s] %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S",
)
log = logging.getLogger(__name__)

# ================================================================
#  SINGLE INSTANCE LOCK
# ================================================================
#  Защита от двойного запуска бота с одним BOT_TOKEN.
#  Без этого второй запущенный процесс получает от Telegram:
#    409 Conflict: terminated by other getUpdates request
#  и оба процесса начинают бесконечно конфликтовать, теряя/дублируя
#  сообщения. Используем POSIX file lock (fcntl.flock) — он атомарный
#  на уровне ОС и автоматически снимается ядром, если процесс убит
#  любым способом (kill -9, OOM killer, падение питания), поэтому
#  никаких "зависших" lock-файлов с устаревшим PID не остаётся.

LOCK_FILE = getattr(config, "LOCK_FILE", "bot.lock")
_lock_fh = None  # держим файловый дескриптор открытым на всё время жизни процесса


def acquire_single_instance_lock() -> None:
    """
    Гарантирует, что одновременно запущен только один процесс бота.
    Если лок уже занят другим процессом — печатает понятную ошибку
    в лог и завершает процесс с кодом 1 (вместо тихого 409-конфликта
    с Telegram, который раньше было сложно диагностировать).
    """
    global _lock_fh
    try:
        _lock_fh = open(LOCK_FILE, "w")
        fcntl.flock(_lock_fh, fcntl.LOCK_EX | fcntl.LOCK_NB)
        _lock_fh.write(str(os.getpid()))
        _lock_fh.flush()
        log.info("Single-instance lock получен (PID=%d, файл=%s)", os.getpid(), LOCK_FILE)
    except BlockingIOError:
        log.error(
            "❌ Бот уже запущен другим процессом! Lock-файл '%s' занят. "
            "Завершаю работу, чтобы не получать 409 Conflict от Telegram. "
            "Проверьте: ps aux | grep python3 — и убейте лишний процесс перед перезапуском.",
            LOCK_FILE,
        )
        sys.exit(1)
    except OSError as e:
        # fcntl недоступен (например Windows) — не блокируем запуск,
        # просто предупреждаем, что защита от двойного запуска отключена.
        log.warning("Single-instance lock недоступен (%s) — защита от двойного запуска отключена", e)


def release_single_instance_lock() -> None:
    global _lock_fh
    if _lock_fh is not None:
        try:
            fcntl.flock(_lock_fh, fcntl.LOCK_UN)
            _lock_fh.close()
        except Exception:
            pass
        try:
            os.remove(LOCK_FILE)
        except OSError:
            pass
        _lock_fh = None

# ================================================================
#  DATABASE
# ================================================================

DB_FILE = "alerts.db"

# Единое переиспользуемое соединение для всех обращений из основного
# event-loop потока. Раньше db_connect() открывал НОВОЕ соединение (+ заново
# выставлял PRAGMA) на каждый вызов db_save_alert/db_get_alert_level/...,
# хотя вызовы идут последовательно из одного и того же потока — соединение
# можно спокойно переиспользовать. db_vacuum() сюда сознательно НЕ входит:
# она выполняется в отдельном потоке через run_in_executor и открывает
# отдельное соединение, чтобы не делить один sqlite3.Connection между двумя
# потоками одновременно.
_db_conn: sqlite3.Connection | None = None


def _get_shared_db_conn() -> sqlite3.Connection:
    global _db_conn
    if _db_conn is None:
        _db_conn = sqlite3.connect(DB_FILE, check_same_thread=False, timeout=10)
        _db_conn.row_factory = sqlite3.Row
        _db_conn.execute("PRAGMA journal_mode=WAL")
        _db_conn.execute("PRAGMA synchronous=NORMAL")
    return _db_conn


@contextmanager
def db_connect():
    conn = _get_shared_db_conn()
    try:
        yield conn
        conn.commit()
    except Exception:
        conn.rollback()
        raise


def db_close():
    """Закрывает общее соединение при штатной остановке процесса (сбрасывает WAL)."""
    global _db_conn
    if _db_conn is not None:
        try:
            _db_conn.close()
        except Exception as e:
            log.warning("db_close: %s", e)
        _db_conn = None


def db_init():
    with db_connect() as conn:
        conn.executescript("""
            CREATE TABLE IF NOT EXISTS alerts (
                id        INTEGER PRIMARY KEY AUTOINCREMENT,
                symbol    TEXT    NOT NULL,
                price     REAL    NOT NULL,
                growth    REAL    NOT NULL,
                rsi       REAL,
                macd      REAL,
                source    TEXT,
                ts        REAL    NOT NULL
            );
            CREATE INDEX IF NOT EXISTS idx_alerts_symbol ON alerts(symbol);
            CREATE INDEX IF NOT EXISTS idx_alerts_ts     ON alerts(ts);

            CREATE TABLE IF NOT EXISTS alert_levels (
                symbol       TEXT    PRIMARY KEY,
                alert_price  REAL    NOT NULL,
                direction    INTEGER NOT NULL,
                ts           REAL    NOT NULL
            );

            CREATE TABLE IF NOT EXISTS price_stats (
                symbol   TEXT PRIMARY KEY,
                high24h  REAL,
                low24h   REAL,
                vol24h   REAL,
                updated  REAL
            );

            CREATE TABLE IF NOT EXISTS subscribers (
                chat_id  INTEGER PRIMARY KEY,
                added_ts REAL NOT NULL
            );

            CREATE TABLE IF NOT EXISTS reversal_signals (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                symbol      TEXT    NOT NULL,
                price       REAL    NOT NULL,
                score       INTEGER NOT NULL,
                factors     TEXT    NOT NULL,
                rsi         REAL,
                macd        REAL,
                stoch_rsi   REAL,
                bb_pct      REAL,
                atr         REAL,
                target1     REAL,
                target2     REAL,
                source      TEXT,
                ts          REAL    NOT NULL
            );
            CREATE INDEX IF NOT EXISTS idx_rev_symbol ON reversal_signals(symbol);
            CREATE INDEX IF NOT EXISTS idx_rev_ts     ON reversal_signals(ts);
        """)


db_init()

DB_KEEP_ALERTS_DAYS  = getattr(config, "DB_KEEP_ALERTS_DAYS",  30)
DB_KEEP_LEVELS_DAYS  = getattr(config, "DB_KEEP_LEVELS_DAYS",   7)
DB_VACUUM_INTERVAL_H = getattr(config, "DB_VACUUM_INTERVAL_H", 24)


def db_save_alert(symbol, price, growth, rsi, macd, source):
    with db_connect() as conn:
        conn.execute(
            "INSERT INTO alerts (symbol, price, growth, rsi, macd, source, ts) VALUES (?,?,?,?,?,?,?)",
            (symbol, price, growth, rsi, macd, source, time.time()),
        )


def db_save_reversal(symbol, price, score, factors, rsi, macd,
                     stoch_rsi, bb_pct, atr, target1, target2, source):
    with db_connect() as conn:
        conn.execute(
            "INSERT INTO reversal_signals "
            "(symbol, price, score, factors, rsi, macd, stoch_rsi, bb_pct, atr, target1, target2, source, ts) "
            "VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)",
            (symbol, price, score, json.dumps(factors, ensure_ascii=False),
             rsi, macd, stoch_rsi, bb_pct, atr, target1, target2, source, time.time()),
        )


def db_recent_reversals(limit: int = 10) -> list[dict]:
    with db_connect() as conn:
        rows = conn.execute(
            "SELECT symbol, price, score, factors, rsi, macd, stoch_rsi, bb_pct, "
            "atr, target1, target2, source, ts "
            "FROM reversal_signals ORDER BY ts DESC LIMIT ?", (limit,),
        ).fetchall()
    result = []
    for r in rows:
        d = dict(r)
        try:
            d["factors"] = json.loads(d["factors"])
        except Exception:
            d["factors"] = []
        result.append(d)
    return result


def db_get_alert_level(symbol):
    with db_connect() as conn:
        row = conn.execute(
            "SELECT alert_price, direction, ts FROM alert_levels WHERE symbol=?", (symbol,)
        ).fetchone()
    return dict(row) if row else None


def db_set_alert_level(symbol, alert_price, direction):
    with db_connect() as conn:
        conn.execute(
            "INSERT OR REPLACE INTO alert_levels (symbol, alert_price, direction, ts) VALUES (?,?,?,?)",
            (symbol, alert_price, direction, time.time()),
        )


def db_clear_alert_levels():
    with db_connect() as conn:
        conn.execute("DELETE FROM alert_levels")


def db_clear_alert_level(symbol):
    with db_connect() as conn:
        conn.execute("DELETE FROM alert_levels WHERE symbol=?", (symbol,))


def db_cleanup() -> dict:
    now = time.time()
    deleted = {}
    with db_connect() as conn:
        cur = conn.execute("DELETE FROM alerts WHERE ts < ?", (now - DB_KEEP_ALERTS_DAYS * 86400,))
        deleted["alerts"] = cur.rowcount
        cur = conn.execute("DELETE FROM alert_levels WHERE ts < ?", (now - DB_KEEP_LEVELS_DAYS * 86400,))
        deleted["alert_levels"] = cur.rowcount
        cur = conn.execute("DELETE FROM price_stats WHERE updated < ?", (now - 86400,))
        deleted["price_stats"] = cur.rowcount
        cur = conn.execute("DELETE FROM reversal_signals WHERE ts < ?", (now - 14 * 86400,))
        deleted["reversal_signals"] = cur.rowcount
    return deleted


def db_vacuum():
    # Намеренно НЕ использует общее соединение (_get_shared_db_conn): эта
    # функция вызывается через run_in_executor в отдельном потоке, и делить
    # один sqlite3.Connection между этим потоком и основным event-loop
    # потоком, которые могут работать параллельно, небезопасно.
    conn = sqlite3.connect(DB_FILE, timeout=30)
    try:
        conn.execute("VACUUM")
        conn.commit()
    finally:
        conn.close()


def db_size_mb() -> float:
    try:
        return os.path.getsize(DB_FILE) / 1_048_576
    except OSError:
        return 0.0


def db_stats() -> dict:
    with db_connect() as conn:
        alerts_count = conn.execute("SELECT COUNT(*) FROM alerts").fetchone()[0]
        oldest       = conn.execute("SELECT MIN(ts) FROM alerts").fetchone()[0]
        levels_count = conn.execute("SELECT COUNT(*) FROM alert_levels").fetchone()[0]
        rev_count    = conn.execute("SELECT COUNT(*) FROM reversal_signals").fetchone()[0]
    return {
        "alerts":       alerts_count,
        "oldest_alert": time.strftime("%d.%m.%Y", time.localtime(oldest)) if oldest else "—",
        "levels":       levels_count,
        "reversals":    rev_count,
        "size_mb":      db_size_mb(),
    }


def db_recent_alerts(limit: int = 10) -> list[dict]:
    with db_connect() as conn:
        rows = conn.execute(
            "SELECT symbol, price, growth, rsi, macd, source, ts FROM alerts ORDER BY ts DESC LIMIT ?",
            (limit,),
        ).fetchall()
    return [dict(r) for r in rows]


def db_top_signals(limit: int = 5) -> list[dict]:
    cutoff = time.time() - 86400
    with db_connect() as conn:
        rows = conn.execute(
            "SELECT symbol, growth, price, source, ts FROM alerts "
            "WHERE ts >= ? ORDER BY ABS(growth) DESC LIMIT ?",
            (cutoff, limit),
        ).fetchall()
    return [dict(r) for r in rows]


def db_export_csv(limit: int = 500) -> str:
    rows = db_recent_alerts(limit)
    buf = io.StringIO()
    writer = csv.DictWriter(buf, fieldnames=["ts", "symbol", "price", "growth", "rsi", "macd", "source"])
    writer.writeheader()
    for r in rows:
        r["ts"] = time.strftime("%Y-%m-%d %H:%M:%S", time.localtime(r["ts"]))
        writer.writerow(r)
    return buf.getvalue()


# ── Подписчики ────────────────────────────────────────────────────────────────

def db_add_subscriber(chat_id: int):
    with db_connect() as conn:
        conn.execute(
            "INSERT OR IGNORE INTO subscribers (chat_id, added_ts) VALUES (?,?)",
            (chat_id, time.time()),
        )


def db_remove_subscriber(chat_id: int):
    with db_connect() as conn:
        conn.execute("DELETE FROM subscribers WHERE chat_id=?", (chat_id,))


def db_get_subscribers() -> list[int]:
    with db_connect() as conn:
        rows = conn.execute("SELECT chat_id FROM subscribers").fetchall()
    ids = {r["chat_id"] for r in rows}
    ids.add(int(config.CHAT_ID))
    return list(ids)


# ================================================================
#  STATE
# ================================================================

STATE_FILE = "state.json"
last_alert_growth: dict = {}

# Список всех настроек, изменяемых кнопками/командами в Telegram. При
# перезапуске (редеплой, сбой, рестарт контейнера) все они восстанавливаются
# из state.json / Telegram, а НЕ сбрасываются на значения по умолчанию из
# config.py.
SETTINGS_KEYS = [
    "current_percent", "current_window", "monitor_paused",
    "REVERSAL_MIN_SCORE", "REVERSAL_RSI_OB", "REVERSAL_STOCH_OB", "REVERSAL_STOCH_EXT",
    "REVERSAL_BB_OB", "REVERSAL_MACD_SLOPE", "REVERSAL_ACCEL", "REVERSAL_HIGH_MARGIN",
    "REVERSAL_MOMENTUM", "REVERSAL_COOLDOWN_SEC", "REVERSAL_ATR_MULT", "REVERSAL_VOL_RATIO",
    "REVERSAL_GROWTH_MIN_PCT", "REVERSAL_WINDOW_SEC",
    "REVERSAL_REPEAT_PRICE_PCT", "REVERSAL_REPEAT_SCORE_DELTA",
    "RSI_SIGNAL_TF", "RSI_SIGNAL_LEVEL", "RSI_REVERSAL_TF", "RSI_SIGNAL_FILTER_ENABLED",
    "RSI_REVERSAL_FILTER_ENABLED",
    "CHART_ENABLED", "CHART_TF", "CUSTOM_PCT_24H", "PCT24H_GATE_ENABLED",
]

# ── v25.6: РЕЗЕРВНАЯ КОПИЯ НАСТРОЕК В ЗАКРЕПЛЁННОМ СООБЩЕНИИ TELEGRAM ────────
# ПРОБЛЕМА: state.json пишется на локальный диск контейнера. На Railway (и
# большинстве похожих PaaS) диск контейнера ЭФЕМЕРНЫЙ — он полностью
# пересоздаётся из образа при каждом редеплое (пуш кода, обновление
# переменных окружения, ручной "Redeploy" и т.п.), если явно не подключён
# постоянный Volume. Из-за этого раньше настройки, изменённые кнопками,
# переживали обычный краш/перезапуск процесса ВНУТРИ одного контейнера, но
# терялись именно при редеплое — то есть именно тогда, когда это больнее
# всего заметно.
# РЕШЕНИЕ: помимо state.json, компактный JSON с настройками (только
# SETTINGS_KEYS — единицы килобайт, без last_alert_growth, который может
# быть большим) дополнительно сохраняется в ЗАКРЕПЛЁННОЕ сообщение в чате
# бота. Telegram-сервера не имеют отношения к диску контейнера Railway,
# поэтому это переживает АБСОЛЮТНО ЛЮБОЙ редеплой/пересоздание контейнера,
# без необходимости настраивать Volume на Railway. При старте бот сначала
# пытается прочитать настройки из этого закреплённого сообщения (наиболее
# надёжный источник) и только если это не получилось (нет сети на старте,
# сообщение открепили руками и т.п.) — откатывается на локальный state.json,
# и только если нет и его — на значения по умолчанию из config.py.
_STATE_BACKUP_MARKER = "⚙️ CRYPTO_BOT_SETTINGS_BACKUP_V1 — НЕ УДАЛЯТЬ И НЕ ОТКРЕПЛЯТЬ"
_last_pushed_settings_json: Optional[str] = None
_state_restore_source: Optional[str] = None   # для стартового сообщения боту — откуда восстановлены настройки


def _tg_api_sync(method: str, params: dict, timeout: int = 10) -> Optional[dict]:
    """
    Синхронный (urllib, БЕЗ aiohttp) вызов Telegram Bot API — специально для
    мест, где ещё/уже нет запущенного event loop и aiohttp-сессии: при
    старте модуля (load_state вызывается до создания _session) и в
    sync-обработчике сигнала (_handle_signal). Тот же подход уже используется
    в notify_sync() для тех же целей.

    v25.8: раньше HTTP-ошибки (4xx/5xx) ловились общим except Exception, из-за
    чего терялось тело ответа — а у Telegram оно почти всегда содержит
    осмысленное поле "description" (например, "Bad Request: message is not
    modified"), которое вызывающему коду важно уметь отличать от настоящего
    сбоя. Теперь urllib.error.HTTPError обрабатывается отдельно и тело
    ответа читается и парсится как обычно.

    Возвращает распарсенный JSON-ответ (в т.ч. при HTTP-ошибке, если Telegram
    прислал JSON-тело с описанием) или None при полном сбое сети/таймауте/
    невалидном JSON — вызывающий код должен считать отсутствие результата
    некритичным и просто использовать резервный источник данных.
    """
    import urllib.error
    import urllib.request
    try:
        data = json.dumps(params).encode("utf-8")
        req = urllib.request.Request(
            f"{TG}/{method}",
            data=data,
            headers={"Content-Type": "application/json"},
        )
        with urllib.request.urlopen(req, timeout=timeout) as resp:
            return json.loads(resp.read().decode("utf-8"))
    except urllib.error.HTTPError as e:
        try:
            body = e.read().decode("utf-8")
            return json.loads(body)
        except Exception:
            log.warning("_tg_api_sync(%s): HTTP %s (тело не распарсить)", method, e.code)
            return None
    except Exception as e:
        log.warning("_tg_api_sync(%s): %s", method, e)
        return None


def push_state_to_telegram_sync(force: bool = False) -> None:
    """
    Сохраняет текущие SETTINGS_KEYS в закреплённое сообщение чата
    config.CHAT_ID. Полностью синхронно (см. _tg_api_sync) — можно звать из
    любого контекста, включая обработчик сигнала завершения.

    force=False (по умолчанию): если настройки с прошлого успешного пуша не
    изменились — ничего не отправляет (иначе save_state_loop дёргал бы
    Telegram API каждые 30с даже без единого изменения настроек, что и
    бесполезно, и рискует упереться в rate-limit editMessageText).
    force=True: всегда отправляет, даже если содержимое не изменилось —
    используется один раз при старте, чтобы "самовылечить" резервную копию,
    если она восстанавливалась из локального state.json (т.е. в Telegram
    её либо не было, либо она устарела).
    """
    global _last_pushed_settings_json
    try:
        settings = {k: globals()[k] for k in SETTINGS_KEYS if k in globals()}
        settings_json = json.dumps(settings, sort_keys=True, ensure_ascii=False)
    except Exception:
        log.exception("push_state_to_telegram_sync: не удалось собрать настройки")
        return

    if not force and settings_json == _last_pushed_settings_json:
        return

    text = f"{_STATE_BACKUP_MARKER}\n{settings_json}"
    chat_id = int(config.CHAT_ID)

    chat_info = _tg_api_sync("getChat", {"chat_id": chat_id})
    pinned = None
    if chat_info and chat_info.get("ok"):
        pinned = (chat_info.get("result") or {}).get("pinned_message")

    if pinned and isinstance(pinned.get("text"), str) and pinned["text"].startswith(_STATE_BACKUP_MARKER):
        # Уже есть наше закреплённое сообщение — просто обновляем текст,
        # не плодим новые сообщения и не перепиниваем на каждое изменение.
        result = _tg_api_sync("editMessageText", {
            "chat_id": chat_id, "message_id": pinned["message_id"], "text": text,
        })
        ok = bool(result and result.get("ok"))
        if not ok and result and "not modified" in str(result.get("description", "")).lower():
            # v25.8: Telegram отвечает 400 "message is not modified", если текст
            # уже точно такой же, как сейчас закреплён — например, сразу после
            # рестарта, когда _last_pushed_settings_json ещё пуст (сбросился при
            # перезапуске процесса), а в Telegram уже лежит актуальный бэкап с
            # прошлого запуска. Это НЕ ошибка — контент и так уже правильный,
            # просто считаем пуш успешным и не спамим лог warning'ом.
            ok = True
    else:
        # Либо ещё никогда не сохраняли, либо закреплено что-то другое
        # (например, владелец сам закрепил постороннее сообщение) — отправляем
        # новое сообщение с бэкапом и закрепляем именно его, беззвучно.
        sent = _tg_api_sync("sendMessage", {"chat_id": chat_id, "text": text})
        ok = False
        if sent and sent.get("ok"):
            msg_id = sent["result"]["message_id"]
            pin_result = _tg_api_sync("pinChatMessage", {
                "chat_id": chat_id, "message_id": msg_id, "disable_notification": True,
            })
            ok = bool(pin_result and pin_result.get("ok"))
            if not ok:
                log.warning(
                    "push_state_to_telegram_sync: сообщение с бэкапом настроек отправлено, "
                    "но закрепить не удалось (возможно, боту не хватает прав) — "
                    "резервная копия всё равно останется читаемой по getChat/пересылке вручную."
                )

    if ok:
        _last_pushed_settings_json = settings_json
    else:
        log.warning("push_state_to_telegram_sync: не удалось сохранить бэкап настроек в Telegram")


def pull_state_from_telegram_sync() -> Optional[dict]:
    """
    Читает настройки из закреплённого сообщения чата config.CHAT_ID (см.
    push_state_to_telegram_sync). Возвращает dict настроек или None, если
    закреплённого бэкапа нет / его не удалось прочитать / нет сети — в этом
    случае вызывающий код (load_state) откатывается на локальный state.json.
    """
    try:
        chat_id = int(config.CHAT_ID)
        chat_info = _tg_api_sync("getChat", {"chat_id": chat_id})
        if not chat_info or not chat_info.get("ok"):
            return None
        pinned = (chat_info.get("result") or {}).get("pinned_message")
        if not pinned or not isinstance(pinned.get("text"), str):
            return None
        text = pinned["text"]
        if not text.startswith(_STATE_BACKUP_MARKER):
            return None
        payload = text[len(_STATE_BACKUP_MARKER):].lstrip("\n")
        settings = json.loads(payload)
        if not isinstance(settings, dict):
            return None
        return settings
    except Exception as e:
        log.warning("pull_state_from_telegram_sync: %s", e)
        return None


def save_state():
    """
    Сохраняет last_alert_growth И все настройки из SETTINGS_KEYS в state.json
    (быстрый локальный кэш — переживает обычный краш/рестарт процесса внутри
    одного контейнера), а также в закреплённое сообщение Telegram через
    push_state_to_telegram_sync (v25.6 — переживает ЛЮБОЙ редеплой Railway,
    даже с полной пересборкой контейнера).
    Вызывается: периодически (save_state_loop, каждые 30с), сразу после
    обработки каждого сообщения в Telegram (см. handle_message-обёртку ниже)
    и при получении SIGTERM/SIGINT (штатная остановка) — поэтому настройки,
    изменённые кнопками, переживают перезапуск/сбой/редеплой.
    """
    try:
        settings = {k: globals()[k] for k in SETTINGS_KEYS if k in globals()}
        tmp = STATE_FILE + ".tmp"
        with open(tmp, "w") as f:
            json.dump({"last_alert_growth": last_alert_growth, "settings": settings}, f)
        os.replace(tmp, STATE_FILE)  # атомарная замена — исключает битый файл при краше на середине записи
    except Exception:
        log.exception("save_state: не удалось сохранить состояние локально")
    # Пуш в Telegram — намеренно ПОСЛЕ локальной записи и в отдельном try
    # внутри push_state_to_telegram_sync: сбой сети не должен мешать хотя бы
    # локальному сохранению, и наоборот.
    push_state_to_telegram_sync()


def load_state():
    """
    Восстанавливает last_alert_growth и настройки. Порядок источников (от
    самого надёжного к запасному):
      1) закреплённое сообщение Telegram (push_state_to_telegram_sync) —
         переживает ЛЮБОЙ редеплой Railway, т.к. хранится не на диске
         контейнера, а на серверах Telegram;
      2) локальный state.json — переживает обычный рестарт процесса внутри
         одного контейнера, но НЕ переживает редеплой на эфемерном диске;
      3) значения по умолчанию из config.py, если нет ни того, ни другого.
    last_alert_growth восстанавливается ТОЛЬКО из state.json (в Telegram-
    бэкап он намеренно не попадает — это внутренний кэш для антидублирования
    сигналов, может быть большим, и потеря части этих данных при полном
    редеплое не критична, в отличие от настроек, выставленных кнопками).
    ВАЖНО: вызывается из кода НИЖЕ, ПОСЛЕ того как все настройки уже получили
    значения по умолчанию из config.py (конец блока GLOBALS) — иначе
    присваивания по умолчанию, идущие после этого места по ходу выполнения
    модуля, затёрли бы восстановленные значения.
    """
    global last_alert_growth
    global _state_restore_source
    global _last_pushed_settings_json

    file_data: Optional[dict] = None
    try:
        with open(STATE_FILE) as f:
            file_data = json.load(f)
    except FileNotFoundError:
        pass
    except Exception:
        log.exception("load_state: не удалось прочитать state.json")

    last_alert_growth = (file_data or {}).get("last_alert_growth", {})

    tg_settings = pull_state_from_telegram_sync()
    if tg_settings:
        settings, source = tg_settings, "закреплённого сообщения Telegram (переживает редеплой Railway)"
    elif file_data:
        settings, source = file_data.get("settings", {}), "локального state.json (только в пределах одного контейнера)"
    else:
        settings, source = {}, None

    restored = []
    for k, v in settings.items():
        if k not in SETTINGS_KEYS:
            continue
        try:
            globals()[k] = v
            restored.append(k)
        except Exception:
            log.warning("load_state: не удалось восстановить настройку %s", k)

    if restored:
        log.info("Настройки восстановлены из %s (%d шт.): %s", source, len(restored), ", ".join(sorted(restored)))
        _state_restore_source = source
    else:
        log.info("Ни закреплённого сообщения Telegram, ни state.json не найдено — используются настройки по умолчанию из config.py")
        _state_restore_source = None

    # v25.8: если восстановили из Telegram и бэкап уже содержит ВСЕ текущие
    # SETTINGS_KEYS (т.е. с прошлого пуша список настраиваемых ключей не
    # менялся) — сразу считаем это "уже запушенным" состоянием. Без этого
    # push_state_to_telegram_sync() при первом же save_state() после рестарта
    # видел бы _last_pushed_settings_json пустым (сбросился при перезапуске
    # процесса) и пытался бы отправить editMessageText с ТЕМ ЖЕ САМЫМ текстом,
    # который Telegram и так уже хранит — а Telegram на это отвечает HTTP 400
    # "message is not modified". Такой ответ теперь и сам по себе трактуется
    # как успех (см. push_state_to_telegram_sync), но лучше вообще не делать
    # заведомо бесполезный запрос.
    if tg_settings and set(SETTINGS_KEYS) <= set(tg_settings.keys()):
        try:
            current_settings = {k: globals()[k] for k in SETTINGS_KEYS if k in globals()}
            _last_pushed_settings_json = json.dumps(current_settings, sort_keys=True, ensure_ascii=False)
        except Exception:
            pass

    # Если восстановили из локального файла (или вообще не восстановили, но
    # тогда push всё равно отправит текущие дефолты) — сразу же "самолечим"
    # резервную копию в Telegram, чтобы следующий редеплой уже мог
    # восстановиться из неё, а не из диска, который будет пересоздан.
    if not tg_settings:
        push_state_to_telegram_sync(force=True)

# ================================================================
#  GLOBALS

# ================================================================

TOKEN = config.BOT_TOKEN
TG    = f"https://api.telegram.org/bot{TOKEN}"

offset           = 0
price_history:   dict  = {}
signals_count    = 0
reversal_count   = 0
checks_count     = 0
start_time       = time.time()
last_check_time: float = 0.0

current_percent: float = config.PERCENT
current_window:  int   = config.WINDOW

monitor_paused = False
monitor_task: asyncio.Task | None = None

_session: aiohttp.ClientSession | None = None

_alert_cooldown:    dict[str, float] = {}
_reversal_cooldown: dict[str, float] = {}
_restart_cooldown:  dict[int, float] = {}   # {chat_id: last_restart_ts} — защита от спама кнопкой
RESTART_COOLDOWN_SEC: int = 60
ALERT_COOLDOWN_SEC:    int = getattr(config, "ALERT_COOLDOWN_SEC",    60)
REVERSAL_COOLDOWN_SEC: int = getattr(config, "REVERSAL_COOLDOWN_SEC", 1800)  # 30 мин по умолчанию

# ── v25.5: пороги, которые раньше были "магическими числами" прямо в коде —
# вынесены в настраиваемые константы (переопределяются через config.py, без
# правки кода бота). Поведение по умолчанию не меняется — значения те же,
# что были захардкожены раньше.
OI_CHANGE_MIN_PCT:       float = getattr(config, "OI_CHANGE_MIN_PCT", 0.5)      # мин. |Δ OI| для показа хинта
BTC_CORR_SYNC_PCT:       float = getattr(config, "BTC_CORR_SYNC_PCT", 1.0)      # |BTC change| для "синхронно"
BTC_CORR_FLAT_PCT:       float = getattr(config, "BTC_CORR_FLAT_PCT", 0.3)      # |BTC change| для "почти не двигался"
TF_CONFIRM_MIN_ABS_PCT:  float = getattr(config, "TF_CONFIRM_MIN_ABS_PCT", 0.01)  # мин. |Δ| ТФ, чтобы считать подтверждением
FUNDING_NEUTRAL_EPS_PCT: float = getattr(config, "FUNDING_NEUTRAL_EPS_PCT", 0.0001)  # порог "нейтрального" фандинга
LIQ_DOMINANCE_RATIO:     float = getattr(config, "LIQ_DOMINANCE_RATIO", 1.2)    # во сколько раз должна доминировать сторона ликвидаций
SPREAD_WIDE_PCT:         float = getattr(config, "SPREAD_WIDE_PCT", 0.05)      # с какого % спред считается "широким"
CVD_WINDOW_CANDLES:      int   = getattr(config, "CVD_WINDOW_CANDLES", 20)     # свечей Min1 для CVD (по умолчанию ~20 мин)
LSR_TTL_SEC:             int   = getattr(config, "LSR_TTL_SEC", 60)            # TTL кэша long/short ratio

# ── v25.5: ограничение конкурентности запросов к Bybit по конкретным монетам
# (klines, RSI на отдельном ТФ, long/short ratio) — без лимита при большом
# количестве одновременно сработавших сигналов бот мог упереться в rate-limit
# биржи. Общий тикер get_prices()/get_symbols() под лимит не подпадает — это
# один запрос сразу по всем монетам, а не по одной.
BYBIT_CONCURRENCY_LIMIT: int = getattr(config, "BYBIT_CONCURRENCY_LIMIT", 8)
_bybit_sem: asyncio.Semaphore | None = None


def _get_bybit_sem() -> asyncio.Semaphore:
    """Semaphore создаётся лениво внутри работающего event loop (не при
    импорте модуля), иначе он привязался бы не к тому loop'у в тестах."""
    global _bybit_sem
    if _bybit_sem is None:
        _bybit_sem = asyncio.Semaphore(BYBIT_CONCURRENCY_LIMIT)
    return _bybit_sem


# ── v25.7: жёсткая блокировка Bybit ("Access too frequent") ─────────────────
# Это НЕ обычный 429 rate-limit с Retry-After — Bybit/Cloudflare полностью
# блокирует IP на несколько минут при слишком частых запросах. Ретраить его
# короткими паузами (было: 1с/2с/3с) не только бесполезно, но и продлевает
# саму блокировку, а также забивает лог сотнями одинаковых ошибок. Вместо
# этого при обнаружении такого ответа ставим глобальный "стоп-кран" на все
# запросы к Bybit (включая тикеры/список монет) на BYBIT_BLOCK_COOLDOWN_SEC,
# и все функции ниже эту паузу уважают, даже не пытаясь достучаться до сети.
BYBIT_BLOCK_COOLDOWN_SEC: int = getattr(config, "BYBIT_BLOCK_COOLDOWN_SEC", 300)
_bybit_blocked_until: float = 0.0


def _bybit_block_active() -> bool:
    return time.time() < _bybit_blocked_until


def _bybit_block_remaining() -> float:
    return max(0.0, _bybit_blocked_until - time.time())


def _note_bybit_block(raw_text: str = "") -> None:
    """Вызывается при обнаружении ответа-блокировки (HTTP 403 с фразой
    'too frequent' или явным Cloudflare-заголовком). Продлевает общий
    cool-down (если уже активен и новый дедлайн раньше текущего — не
    сокращаем его, берём максимум)."""
    global _bybit_blocked_until
    until = time.time() + BYBIT_BLOCK_COOLDOWN_SEC
    was_active = _bybit_block_active()
    _bybit_blocked_until = max(_bybit_blocked_until, until)
    if not was_active:
        log.error(
            "🚫 Bybit заблокировал запросы с этого IP ('Access too frequent'). "
            "Приостанавливаем ВСЕ запросы к Bybit на %d с, чтобы не усугублять "
            "блокировку (это НЕ ошибка кода — биржа временно не пускает запросы "
            "с текущего IP, типично для общих IP хостинг-провайдеров). "
            "Тело ответа: %s",
            BYBIT_BLOCK_COOLDOWN_SEC, raw_text[:200].replace("\n", " "),
        )


def _looks_like_bybit_block(status: Optional[int], raw_text: str) -> bool:
    if status != 403:
        return False
    low = raw_text.lower()
    return "too frequent" in low or "access too frequent" in low


# ── v25.5: healthcheck HTTP-эндпоинт (для Railway/внешнего мониторинга) ──────
HEALTH_PORT: int = getattr(config, "HEALTH_PORT", 8081)

# Умный фильтр повторных разворотных сигналов по одной монете:
#   - цена должна измениться минимум на REVERSAL_REPEAT_PRICE_PCT% от прошлого сигнала
#   - или скор должен вырасти минимум на REVERSAL_REPEAT_SCORE_DELTA
_reversal_last: dict[str, dict] = {}   # {sym: {price, score, ts}}
REVERSAL_REPEAT_PRICE_PCT:  float = getattr(config, "REVERSAL_REPEAT_PRICE_PCT",  3.0)   # 3% смены цены
REVERSAL_REPEAT_SCORE_DELTA: int  = getattr(config, "REVERSAL_REPEAT_SCORE_DELTA", 2)    # +2 фактора

_levels_cache: dict[str, dict] = {}

# ── Настройки детектора разворота (все изменяемы через Telegram) ──────────────
REVERSAL_MIN_SCORE:   int   = getattr(config, "REVERSAL_MIN_SCORE",   5)      # из 20
REVERSAL_RSI_OB:      float = getattr(config, "REVERSAL_RSI_OB",      70.0)
REVERSAL_STOCH_OB:    float = getattr(config, "REVERSAL_STOCH_OB",    0.80)
REVERSAL_STOCH_EXT:   float = getattr(config, "REVERSAL_STOCH_EXT",   0.85)
REVERSAL_BB_OB:       float = getattr(config, "REVERSAL_BB_OB",       1.0)
REVERSAL_MACD_SLOPE:  float = getattr(config, "REVERSAL_MACD_SLOPE",  -0.000005)
REVERSAL_ACCEL:       float = getattr(config, "REVERSAL_ACCEL",       0.5)
REVERSAL_HIGH_MARGIN: float = getattr(config, "REVERSAL_HIGH_MARGIN", 0.998)
REVERSAL_MOMENTUM:    float = getattr(config, "REVERSAL_MOMENTUM",    -0.5)
REVERSAL_ATR_MULT:    float = getattr(config, "REVERSAL_ATR_MULT",    3.0)    # ATR перегрев
REVERSAL_VOL_RATIO:   float = getattr(config, "REVERSAL_VOL_RATIO",   0.7)    # объём < 70% средн.

# ── Настраиваемый RSI-фильтр (через кнопки) ────────────────────────────────
# Для обычных сигналов роста/падения:
RSI_SIGNAL_TF:    str   = getattr(config, "RSI_SIGNAL_TF",    "Min1")   # ТФ: Min1/Min5/Min15/Min60/Hour4/Hour12/Day1
RSI_SIGNAL_LEVEL: float = getattr(config, "RSI_SIGNAL_LEVEL", 50.0)     # "не меньше": 50/60/70/80/90/95
# Вкл/выкл RSI-фильтра для сигналов роста/падения (кнопка 🚦). Когда выключен —
# алерты роста/падения работают только по % (current_percent), без проверки RSI
# и без доп. HTTP-запроса за RSI на отдельном ТФ (если RSI_SIGNAL_TF != Min1).
RSI_SIGNAL_FILTER_ENABLED: bool = getattr(config, "RSI_SIGNAL_FILTER_ENABLED", True)
# Для разворотных сигналов (фактор 15 + переопределяет REVERSAL_RSI_OB по выбранному ТФ):
RSI_REVERSAL_TF:  str   = getattr(config, "RSI_REVERSAL_TF",  "Min15")
# Вкл/выкл жёсткого RSI-гейта разворота (кнопка 🔄🚦). Когда выключен —
# реверсивный сигнал больше не блокируется требованием "RSI на выбранном ТФ
# >= REVERSAL_RSI_OB перед тем, как вообще считать 20 факторов": решение
# принимается только по REVERSAL_MIN_SCORE из самих факторов. Также пропускает
# доп. HTTP-запрос get_rsi_for_tf() на RSI_REVERSAL_TF — фактор 15 при этом не
# исчезает, а падает на свой встроенный fallback (RSI по Min15 из уже
# загруженных klines_15m, без сети).
RSI_REVERSAL_FILTER_ENABLED: bool = getattr(config, "RSI_REVERSAL_FILTER_ENABLED", True)

# Минимальный % роста за период для проверки разворота на шорт (кнопки 1/5/10/15/25%)
REVERSAL_GROWTH_MIN_PCT: float = getattr(config, "REVERSAL_GROWTH_MIN_PCT", 5.0)

# Период для расчёта пикового роста разворота (кнопки 5м/30м/1ч/4ч/1д)
REVERSAL_WINDOW_SEC: int = getattr(config, "REVERSAL_WINDOW_SEC", 3600)  # по умолчанию 1 час

# v25.2: отдельный кулдаун для уведомлений "Изменение за 24ч" (БЛОК 1.6) —
# независим от _alert_cooldown обычных сигналов роста/падения.
_pct24h_notify_cooldown: dict[str, float] = {}
PCT24H_NOTIFY_COOLDOWN_SEC: int = 3600

# ── v25: скриншот графика в сигналах (кнопки 🖼 График ВЫКЛ/ВКЛ) ──────────────
CHART_ENABLED: bool = getattr(config, "CHART_ENABLED", True)
CHART_TF:      str  = getattr(config, "CHART_TF", "Min60")   # ТФ графика, переключается кнопками 🖼📊
# Таймфреймы, доступные для переключения кнопками 🖼📊 ТФ (1м/15м/1ч/2ч/4ч/1д)
CHART_TF_INTERVALS = {
    "1м":  "Min1",
    "15м": "Min15",
    "1ч":  "Min60",
    "2ч":  "Hour2",
    "4ч":  "Hour4",
    "1д":  "Day1",
}
CHART_TF_LABELS = {v: k for k, v in CHART_TF_INTERVALS.items()}
# Кол-во свечей на графике — подобрано под каждый ТФ, чтобы график выглядел
# как читаемый график на Bybit (не 5 свечей и не 500 нечитаемых точек).
CHART_TF_CANDLES = {
    "Min1":  180,   # 3 часа
    "Min15": 96,    # 24 часа
    "Min60": 72,    # 3 суток
    "Hour2": 90,    # ~7.5 суток
    "Hour4": 90,    # ~15 суток
    "Day1":  90,    # ~3 месяца
}

# ── v25.4: фандинг (ставка финансирования) из того же тикера, без доп. запроса ──
funding_cache: dict[str, tuple[float, Optional[float]]] = {}   # {symbol: (ставка_%, nextFundingTime_сек)}
# v25.2: это ОТДЕЛЬНЫЙ тип уведомления "Изменение за 24ч" (БЛОК 1.6 в monitor()).
# Кнопки 📈 Свой % (24ч) / 📈🚦 24ч-регулировка / /set_pct24h управляют ТОЛЬКО им
# и НЕ влияют на обычные сигналы роста/падения (БЛОК 2) и разворот (БЛОК 1) —
# там строка "🌐 24ч: ..." остаётся чисто информационной независимо от этих кнопок.
CUSTOM_PCT_24H: float = getattr(config, "CUSTOM_PCT_24H", 5.0)   # порог уведомления, 1–500%
PCT24H_GATE_ENABLED: bool = getattr(config, "PCT24H_GATE_ENABLED", True)  # вкл/выкл уведомления
# ── v25.1: доп. рыночные данные в сигналах (только информационно, без
# фильтрации/блокировки самих сигналов) — открытый интерес, ликвидации,
# мульти-ТФ подтверждение, корреляция с BTC ────────────────────────────────
oi_history: dict[str, list[tuple[float, float]]] = {}   # {symbol: [(ts, openInterestValue), ...]}

LIQ_WS_URL         = "wss://stream.bybit.com/v5/public/linear"
LIQ_WINDOW_SEC     = 1800   # окно суммирования ликвидаций для строки в сигнале (30 мин)
LIQ_MAX_STORE_SEC  = 3600   # события храним чуть дольше окна, про запас
LIQ_SUB_CHUNK      = 10     # топиков за один subscribe-запрос (щадяще к лимитам Bybit)

_liq_events: dict[str, list[tuple[float, str, float]]] = {}  # {symbol: [(ts, side, usd_value), ...]}
_liq_task: asyncio.Task | None = None
_health_task: asyncio.Task | None = None   # v25.5: изолированный healthcheck HTTP-сервер
liq_symbols: set[str] = set()   # актуальный набор символов для подписки на ликвидации

# Режим ожидания ручного ввода числа с клавиатуры (кнопка 📈 Свой % (24ч))
_awaiting_input: Optional[str] = None

# Восстанавливаем last_alert_growth и все настройки (SETTINGS_KEYS) из
# state.json ЗДЕСЬ — после того, как все настройки выше уже получили значения
# по умолчанию из config.py. Если state.json есть (бот уже когда-то работал
# и был остановлен/упал/перезапущен) — восстановленные значения перекрывают
# значения по умолчанию, и все настройки, изменённые кнопками, переживают
# перезапуск/сбой/редеплой.
load_state()


def _cache_load_levels():
    global _levels_cache
    with db_connect() as conn:
        rows = conn.execute("SELECT symbol, alert_price, direction FROM alert_levels").fetchall()
    _levels_cache = {r["symbol"]: {"alert_price": r["alert_price"], "direction": r["direction"]} for r in rows}
    log.info("Загружено уровней в кэш: %d", len(_levels_cache))


def _cache_get_level(symbol):
    return _levels_cache.get(symbol)


def _cache_set_level(symbol, alert_price, direction):
    _levels_cache[symbol] = {"alert_price": alert_price, "direction": direction}
    db_set_alert_level(symbol, alert_price, direction)


def _cache_clear_level(symbol):
    _levels_cache.pop(symbol, None)
    db_clear_alert_level(symbol)


def _cache_clear_all():
    _levels_cache.clear()
    db_clear_alert_levels()


# ================================================================
#  BYBIT KLINE CACHE  (цена + объём)
# ================================================================

_kline_cache: dict[str, dict] = {}
KLINE_CACHE_TTL  = 60
KLINE_LIMIT_1M   = 250   # 250 свечей по 1 мин (RSI, BB, StochRSI, паттерны).
# v25.9: было 120 — этого хватало для большинства индикаторов, но RSI считается
# рекурсивным экспоненциальным сглаживанием Уайлдера (RMA), которое зависит от
# ВСЕЙ предшествующей истории, а не только от последних window свечей. Чем
# короче поданная история, тем сильнее финальное значение RSI отличается от
# "истинного" (того, что показывает график Bybit/TradingView, посчитанный по
# практически неограниченной глубине истории). При window=12 (alpha=1/12)
# влияние точки начала расчёта затухает как (11/12)^n — на n≈250 остаточная
# погрешность порядка 1e-8, то есть RSI(12) на 250 свечах фактически совпадает
# со значением на графике Bybit с точностью до округления. На 120 свечах
# остаточная погрешность была на несколько порядков больше и в моменте могла
# давать заметное расхождение с биржевым RSI.
KLINE_LIMIT_5M   = 100   # 100 свечей по 5 мин = 8+ часов (ATR, EMA, MACD, объём)
# Для обратной совместимости
KLINE_LIMIT = KLINE_LIMIT_1M

# Внутренние обозначения таймфреймов ("Min1", "Hour4" и т.п.) → интервалы
# в формате, который принимает Bybit V5 kline API.
BYBIT_INTERVAL_MAP = {
    "Min1":   "1",
    "Min5":   "5",
    "Min15":  "15",
    "Min60":  "60",
    "Hour2":  "120",
    "Hour4":  "240",
    "Hour12": "720",
    "Day1":   "D",
}


async def _fetch_bybit_klines(symbol: str, interval: str = "Min1", limit: int = KLINE_LIMIT_1M) -> dict:
    """Возвращает dict с ключами: opens, closes, highs, lows, volumes, turnovers
    (все list[float]), в хронологическом порядке (от старых к новым).
    turnovers — оборот в USDT за свечу (поле turnover из Bybit kline), нужен
    для расчёта CVD (build_extra_market_context) без отдельного запроса.
    v25.5: запрос ограничен общим семафором _get_bybit_sem(), чтобы при
    большом числе одновременно сработавших сигналов не упереться в
    rate-limit Bybit по количеству параллельных соединений.
    v25.7: если активна общая блокировка Bybit (_bybit_block_active) — не
    делает запрос вообще, сразу отдаёт пустые данные; если сам ответ
    оказался блокировкой ("Access too frequent") — выставляет cool-down."""
    empty = {"opens": [], "closes": [], "highs": [], "lows": [], "volumes": [], "turnovers": []}
    if _bybit_block_active():
        return empty
    bybit_interval = BYBIT_INTERVAL_MAP.get(interval, "1")
    try:
        async with _get_bybit_sem():
            async with _session.get(
                "https://api.bybit.com/v5/market/kline",
                params={
                    "category": "linear",
                    "symbol":   symbol,
                    "interval": bybit_interval,
                    "limit":    str(limit),
                },
                timeout=aiohttp.ClientTimeout(total=10),
            ) as r:
                status = r.status
                raw    = await r.text()
        if _looks_like_bybit_block(status, raw):
            _note_bybit_block(raw)
            return empty
        data = json.loads(raw)
        if data.get("retCode") == 0:
            # Bybit отдаёт свечи от новых к старым — разворачиваем в
            # хронологический порядок (от старых к новым), как ожидают
            # pandas/ta индикаторы (iloc[-1] должен быть последней свечой).
            rows = list(reversed(data.get("result", {}).get("list", [])))
            if rows:
                return {
                    "opens":     [float(c[1]) for c in rows],
                    "highs":     [float(c[2]) for c in rows],
                    "lows":      [float(c[3]) for c in rows],
                    "closes":    [float(c[4]) for c in rows],
                    "volumes":   [float(c[5]) for c in rows],
                    "turnovers": [float(c[6]) if len(c) > 6 else 0.0 for c in rows],
                }
    except Exception as e:
        log.debug("Bybit kline %s %s: %s", symbol, interval, e)
    return empty


async def get_bybit_klines(symbol: str, interval: str = "Min1", limit: int = KLINE_LIMIT_1M) -> dict:
    cache_key = f"{symbol}:{interval}:{limit}"
    now       = time.time()
    cached    = _kline_cache.get(cache_key)
    if cached and now - cached["ts"] < KLINE_CACHE_TTL:
        return cached["data"]
    data = await _fetch_bybit_klines(symbol, interval, limit)
    if data["closes"]:
        _kline_cache[cache_key] = {"ts": now, "data": data}
    # Очищаем устаревшие записи при переполнении (предотвращаем утечку памяти)
    if len(_kline_cache) > 3000:
        stale = [k for k, v in _kline_cache.items() if now - v["ts"] > KLINE_CACHE_TTL * 5]
        for k in stale:
            _kline_cache.pop(k, None)
    return data


KLINE_LIMIT_15M  = 250   # 250 свечей по 15 мин (старший таймфрейм RSI, OBV).
# v25.9: было 60 — увеличено по той же причине, что и KLINE_LIMIT_1M выше:
# RSI(12) на Min15 сходится к биржевому значению только при достаточно длинной
# истории для рекурсивного сглаживания Уайлдера.


async def get_bybit_klines_multi(symbol: str) -> dict:
    """
    Получает три таймфрейма параллельно:
      - Min1  x120 — RSI, StochRSI, BB, паттерны свечей, wick rejection
      - Min5  x100 — ATR, EMA, MACD, объём, моментум, lower highs
      - Min15 x60  — OBV-дивергенция, RSI старшего TF, структура тренда
    """
    _empty = {"opens": [], "closes": [], "highs": [], "lows": [], "volumes": [], "turnovers": []}
    results = await asyncio.gather(
        get_bybit_klines(symbol, "Min1",  KLINE_LIMIT_1M),
        get_bybit_klines(symbol, "Min5",  KLINE_LIMIT_5M),
        get_bybit_klines(symbol, "Min15", KLINE_LIMIT_15M),
        return_exceptions=True,
    )
    return {
        "klines_1m":  results[0] if not isinstance(results[0], Exception) else _empty,
        "klines_5m":  results[1] if not isinstance(results[1], Exception) else _empty,
        "klines_15m": results[2] if not isinstance(results[2], Exception) else _empty,
    }


# ── Настраиваемые таймфреймы RSI (кнопки 5м/15м/1ч/4ч/12ч/1д) ─────────────────
RSI_TF_INTERVALS = {
    "5 мин":  "Min5",
    "15 мин": "Min15",
    "1ч":     "Min60",
    "4ч":     "Hour4",
    "12ч":    "Hour12",
    "1 день": "Day1",
}
RSI_TF_LABELS = {v: k for k, v in RSI_TF_INTERVALS.items()}
RSI_TF_LABELS["Min1"] = "1м"
RSI_TF_LIMIT = {
    # v25.9: лимиты подняты до 250 свечей на каждом ТФ (было 60-120). RSI —
    # рекурсивное экспоненциальное сглаживание Уайлдера, зависящее от ВСЕЙ
    # истории; на коротких сериях (60-120 свечей) значение заметно отличалось
    # от того, что показывает график Bybit. На 250 свечах остаточное влияние
    # точки старта расчёта пренебрежимо мало, и RSI(12) практически точно
    # совпадает с биржевым.
    "Min1": 250, "Min5": 250, "Min15": 250, "Min60": 250,
    "Hour4": 250, "Hour12": 250, "Day1": 250,
}


def _resample_closes(closes: list[float], group: int) -> list[float]:
    """Грубая агрегация серии закрытий в более крупный ТФ
    (берём цену закрытия последней свечи каждой группы — точное значение,
    просто реже семплированное, без интерполяции/округлений)."""
    if group <= 1 or len(closes) < group:
        return closes
    n = (len(closes) // group) * group
    trimmed = closes[-n:]
    return [trimmed[i + group - 1] for i in range(0, n, group)]


async def get_rsi_for_tf(symbol: str, interval: str, window: int = 12) -> Optional[float]:
    """
    Точный RSI с биржи Bybit (linear-фьючерсы) для выбранного через кнопки
    таймфрейма. Использует тот же TTL-кэш свечей (60с), поэтому повторное
    обращение в течение минуты не делает лишних HTTP-запросов.
    Hour12 не поддерживается биржей Bybit напрямую — агрегируется из Hour4×3
    (берётся реальная цена закрытия каждой 3-й 4-часовой свечи).
    """
    if interval == "Hour12":
        # v25.9: 750 свечей Hour4 / 3 = 250 свечей Hour12 (было 180/3=60) —
        # столько же глубины истории, сколько у остальных ТФ в RSI_TF_LIMIT,
        # для той же сходимости RSI(12) к биржевому значению.
        data = await get_bybit_klines(symbol, "Hour4", 750)
        closes = _resample_closes(data["closes"], 3)
        if len(closes) < window + 1:
            return None
        return calculate_rsi(closes, window=window)
    limit  = RSI_TF_LIMIT.get(interval, 60)
    data   = await get_bybit_klines(symbol, interval, limit)
    closes = data["closes"]
    if len(closes) < window + 1:
        return None
    return calculate_rsi(closes, window=window)


# ================================================================
#  INDICATORS
# ================================================================

MIN_SAMPLES = 10


def _wilder_rma(series: pd.Series, window: int) -> pd.Series:
    """
    Классическое сглаживание Уайлдера (RMA) — ТОЧНО как ta.rma() в Pine Script
    TradingView, на чьей чарт-библиотеке построены графики Bybit (включая
    встроенный индикатор RSI):
        rma[0]   = SMA(src, window)              — затравка: простое среднее
                                                     первых `window` значений
        rma[i]   = alpha*src[i] + (1-alpha)*rma[i-1],  alpha = 1/window
    v25.10: раньше использовалась RSIIndicator из пакета `ta`, где pandas
    ewm(adjust=False) стартует с затравки rma[0] = src[0] (фактически 0,
    т.к. это первый gain/loss после diff()). Это НЕ то же самое, что делает
    TradingView/Bybit — отсюда систематическое расхождение значений RSI
    с биржей, даже на длинной истории (влияние неверной затравки затухает
    экспоненциально, но полностью не исчезает и заметно на коротких сериях).
    С SMA-затравкой результат совпадает с биржевым/TradingView RSI.
    """
    s = series.reset_index(drop=True).astype(float)
    n = len(s)
    result = pd.Series([float("nan")] * n)
    if n < window:
        return result
    alpha = 1.0 / window
    prev = float(s.iloc[:window].mean())
    result.iloc[window - 1] = prev
    for i in range(window, n):
        prev = alpha * float(s.iloc[i]) + (1 - alpha) * prev
        result.iloc[i] = prev
    return result


def _rsi_series(prices: list[float], window: int) -> pd.Series:
    """Полная серия RSI, посчитанная классическим методом Уайлдера (см.
    _wilder_rma) — совпадает с тем, что показывает график Bybit/TradingView
    при том же периоде. Индекс серии сброшен (0..n-2), т.к. diff() съедает
    первую свечу."""
    s    = pd.Series(prices, dtype=float)
    diff = s.diff().dropna().reset_index(drop=True)
    gain = diff.clip(lower=0)
    loss = -diff.clip(upper=0)
    avg_gain = _wilder_rma(gain, window)
    avg_loss = _wilder_rma(loss, window)
    rs  = avg_gain / avg_loss
    rsi = 100 - (100 / (1 + rs))
    rsi = rsi.where(avg_loss != 0, 100.0)          # down==0 → RSI=100 (как в TradingView)
    rsi = rsi.mask((avg_loss != 0) & (avg_gain == 0), 0.0)  # up==0 & down!=0 → RSI=0
    return rsi.dropna()


def calculate_rsi(prices: list[float], window: int = 12) -> Optional[float]:
    try:
        if len(prices) < window + 1:
            return None
        rsi = _rsi_series(prices, window)
        if rsi.empty:
            return None
        val = rsi.iloc[-1]
        return None if pd.isna(val) else round(float(val), 2)
    except Exception as e:
        log.error("RSI error: %s", e)
        return None


def calculate_stoch_rsi(prices: list[float], window: int = 12) -> Optional[float]:
    """StochRSI: позиция RSI в его собственном диапазоне. >0.8 = перекупленность."""
    try:
        if len(prices) < window * 2 + 1:
            return None
        rsi_series = _rsi_series(prices, window)
        if len(rsi_series) < window:
            return None
        rsi_min = rsi_series.rolling(window).min().iloc[-1]
        rsi_max = rsi_series.rolling(window).max().iloc[-1]
        if pd.isna(rsi_min) or pd.isna(rsi_max) or (rsi_max - rsi_min) == 0:
            return None
        return round(float((rsi_series.iloc[-1] - rsi_min) / (rsi_max - rsi_min)), 4)
    except Exception as e:
        log.error("StochRSI error: %s", e)
        return None


def calculate_bollinger_pct(prices: list[float], window: int = 20) -> Optional[float]:
    """%B Боллинджера. >1.0 = выше верхней полосы."""
    try:
        if len(prices) < window:
            return None
        s     = pd.Series(prices)
        bb    = BollingerBands(close=s, window=window, window_dev=2)
        upper = bb.bollinger_hband().iloc[-1]
        lower = bb.bollinger_lband().iloc[-1]
        if pd.isna(upper) or pd.isna(lower) or (upper - lower) == 0:
            return None
        return round(float((prices[-1] - lower) / (upper - lower)), 4)
    except Exception as e:
        log.error("BB error: %s", e)
        return None


def calculate_macd_full(prices: list[float]) -> dict:
    result = {"histogram": None, "histogram_prev": None, "slope": None, "cross_down": False}
    try:
        if len(prices) < 30:
            return result
        s    = pd.Series(prices)
        hist = MACD(close=s).macd_diff().dropna()
        if len(hist) < 4:
            return result
        result["histogram"]      = round(float(hist.iloc[-1]), 6)
        result["histogram_prev"] = round(float(hist.iloc[-2]), 6)
        result["slope"]          = round(float(hist.iloc[-1] - hist.iloc[-3]), 6)
        result["cross_down"]     = bool(hist.iloc[-2] > 0 and hist.iloc[-1] < 0)
    except Exception as e:
        log.error("MACD full error: %s", e)
    return result


def calculate_ema_cross(prices: list[float], fast: int = 9, slow: int = 21) -> dict:
    """
    EMA крест: если EMA9 только что пересекла EMA21 вниз — медвежий сигнал.
    Возвращает: cross_down (bool), ema_fast, ema_slow, gap_pct (разрыв в %).
    """
    result = {"cross_down": False, "ema_fast": None, "ema_slow": None, "gap_pct": None}
    try:
        if len(prices) < slow + 2:
            return result
        s        = pd.Series(prices)
        ema_fast = EMAIndicator(close=s, window=fast).ema_indicator()
        ema_slow = EMAIndicator(close=s, window=slow).ema_indicator()
        ef_now   = float(ema_fast.iloc[-1])
        ef_prev  = float(ema_fast.iloc[-2])
        es_now   = float(ema_slow.iloc[-1])
        es_prev  = float(ema_slow.iloc[-2])
        # Крест вниз: до — fast > slow, после — fast < slow
        result["cross_down"] = bool(ef_prev >= es_prev and ef_now < es_now)
        result["ema_fast"]   = round(ef_now, 6)
        result["ema_slow"]   = round(es_now, 6)
        result["gap_pct"]    = round((ef_now - es_now) / es_now * 100, 3)
    except Exception as e:
        log.error("EMA cross error: %s", e)
    return result


def calculate_atr(highs: list[float], lows: list[float],
                  closes: list[float], window: int = 14) -> Optional[float]:
    """ATR — средний истинный диапазон. Мера волатильности."""
    try:
        if len(closes) < window + 1:
            return None
        s_h = pd.Series(highs)
        s_l = pd.Series(lows)
        s_c = pd.Series(closes)
        atr = AverageTrueRange(high=s_h, low=s_l, close=s_c, window=window).average_true_range()
        val = atr.iloc[-1]
        return None if pd.isna(val) else round(float(val), 8)
    except Exception as e:
        log.error("ATR error: %s", e)
        return None


def detect_candle_pattern(closes: list[float], highs: list[float],
                           lows: list[float],
                           opens: list[float] | None = None) -> Optional[str]:
    """
    Медвежий свечной паттерн на последних 2 барах.
    opens — реальные цены открытия из Bybit kline (поле 'open').
    Если не переданы, аппроксимируем prev_close.
      - Shooting Star: длинная верхняя тень >2× тела, тело внизу, закрытие < открытие
      - Доджи: тело < 10% диапазона — нерешительность
      - Медвежье поглощение: красная свеча полностью поглощает предыдущую зелёную
      - Вечерняя звезда (3 бара): бычья → доджи/малое тело → красная
    """
    try:
        if len(closes) < 3 or len(highs) < 3 or len(lows) < 3:
            return None

        # Реальные opens из Bybit или аппроксимация
        if opens and len(opens) >= 3:
            o_prev2 = opens[-3]
            o_prev  = opens[-2]
            o_cur   = opens[-1]
        else:
            o_prev2 = closes[-4] if len(closes) >= 4 else closes[-3]
            o_prev  = closes[-3]
            o_cur   = closes[-2]

        c_prev2 = closes[-3]
        c_prev  = closes[-2]
        c_cur   = closes[-1]
        h_cur   = highs[-1]
        l_cur   = lows[-1]
        h_prev  = highs[-2]

        body_cur  = abs(c_cur  - o_cur)
        body_prev = abs(c_prev - o_prev)
        rng_cur   = h_cur - l_cur
        if rng_cur == 0:
            return None

        upper_wick = h_cur - max(c_cur, o_cur)
        lower_wick = min(c_cur, o_cur) - l_cur

        # Shooting Star: длинная верхняя тень >2× тела, нижняя тень < тела, медвежья
        if (body_cur > 0
                and upper_wick > 2 * body_cur
                and lower_wick < body_cur * 0.5
                and c_cur < o_cur):
            return "Shooting Star 🌠"

        # Доджи: тело < 10% диапазона (нерешительность на пике)
        if body_cur < rng_cur * 0.1:
            return "Доджи ✝️"

        # Медвежье поглощение: пред. зелёная, текущая красная и больше
        if (c_prev > o_prev         # предыдущая — зелёная
                and c_cur < o_cur   # текущая — красная
                and o_cur >= c_prev # открытие ≥ закрытие пред.
                and c_cur <= o_prev # закрытие ≤ открытие пред.
                and body_cur > body_prev):
            return "Медвежье поглощение 🐻"

        # Вечерняя звезда (3 бара): бычья → малое тело/доджи → красная
        body_prev2 = abs(c_prev2 - o_prev2)
        if (c_prev2 > o_prev2              # 1й бар — зелёный бычий
                and body_prev < body_prev2 * 0.3  # 2й бар — малое тело
                and c_cur < o_cur          # 3й бар — красный медвежий
                and c_cur < (o_prev2 + c_prev2) / 2):  # закрытие ниже середины 1го бара
            return "Вечерняя звезда 🌆"

    except Exception as e:
        log.error("Candle pattern error: %s", e)
    return None


def calculate_fibonacci_levels(high: float, low: float) -> dict:
    """
    Уровни Фибоначчи от локального хая к лою (зоны коррекции/цели).
    При развороте вниз цели: 0.236, 0.382, 0.5, 0.618 от текущего хая.
    """
    diff = high - low
    return {
        "fib_236": round(high - diff * 0.236, 8),
        "fib_382": round(high - diff * 0.382, 8),
        "fib_500": round(high - diff * 0.500, 8),
        "fib_618": round(high - diff * 0.618, 8),
        "fib_786": round(high - diff * 0.786, 8),
    }


def calculate_rsi_divergence(prices: list[float], window: int = 12,
                              lookback: int = 10) -> bool:
    """Медвежья дивергенция: цена = новый хай, RSI = ниже предыдущего пика."""
    try:
        if len(prices) < window + lookback + 1:
            return False
        rsi_series = _rsi_series(prices, window)
        if len(rsi_series) < lookback:
            return False
        price_now  = prices[-1]
        price_prev = max(prices[-(lookback + 1):-1])
        rsi_now    = float(rsi_series.iloc[-1])
        rsi_prev   = float(rsi_series.iloc[-(lookback + 1):].max())
        return (price_now > price_prev) and (rsi_now < rsi_prev - 2)
    except Exception:
        return False


def calculate_price_momentum(prices: list[float],
                              fast: int = 5, slow: int = 20) -> Optional[float]:
    """Разница быстрого и медленного моментума. < 0 = иссякание."""
    try:
        if len(prices) < slow + 1:
            return None
        mom_fast = (prices[-1] - prices[-fast]) / prices[-fast] * 100
        mom_slow = (prices[-1] - prices[-slow]) / prices[-slow] * 100
        return round(mom_fast - mom_slow, 4)
    except Exception:
        return None


def calculate_volume_weakness(volumes: list[float]) -> Optional[str]:
    """
    Реальный объём из свечей Bybit.
    Если последний бар < REVERSAL_VOL_RATIO от среднего — сигнал слабости роста.
    """
    try:
        if len(volumes) < 20:
            return None
        avg_vol  = sum(volumes[-20:-1]) / 19
        last_vol = volumes[-1]
        if avg_vol == 0:
            return None
        ratio = last_vol / avg_vol
        if ratio < REVERSAL_VOL_RATIO:
            return f"📊 Объём слабый ({ratio:.1%} от среднего)"
        return None
    except Exception:
        return None


def calculate_volume_signal(sym: str) -> Optional[str]:
    """Fallback: анализ иссякания по тикам цены (если нет данных свечей)."""
    hist = price_history.get(sym, [])
    now  = time.time()
    if len(hist) < 20:
        return None
    hour_prices = [p for t, p in hist if now - t <= 3600]
    if len(hour_prices) < 10:
        return None
    mid   = len(hour_prices) // 2
    move1 = abs(hour_prices[mid - 1] - hour_prices[0])   / hour_prices[0]   * 100
    move2 = abs(hour_prices[-1]      - hour_prices[mid])  / hour_prices[mid] * 100
    if move1 > 0.5 and move2 < move1 * 0.3:
        return "📊 Движение без импульса (иссякание)"
    return None


def calculate_rsi_trend(prices: list[float], window: int = 12) -> Optional[str]:
    try:
        if len(prices) < window + 3:
            return None
        rsi = _rsi_series(prices, window)
        if len(rsi) < 3:
            return None
        delta = rsi.iloc[-1] - rsi.iloc[-3]
        if delta > 1:
            return "▲"
        if delta < -1:
            return "▼"
        return "—"
    except Exception:
        return None


def calculate_acceleration(recent: list[tuple[float, float]]) -> Optional[float]:
    try:
        if len(recent) < 6:
            return None
        mid    = len(recent) // 2
        half1  = recent[:mid]
        half2  = recent[mid:]
        speed1 = (half1[-1][1] - half1[0][1]) / half1[0][1] * 100 / max(half1[-1][0] - half1[0][0], 1)
        speed2 = (half2[-1][1] - half2[0][1]) / half2[0][1] * 100 / max(half2[-1][0] - half2[0][0], 1)
        if speed1 == 0:
            return None
        return round(speed2 / abs(speed1), 2)
    except Exception:
        return None


def calculate_growth_duration(recent: list[tuple[float, float]]) -> int:
    if len(recent) < 2:
        return 0
    return int(recent[-1][0] - recent[0][0])


def check_24h_breakout(sym: str, price: float) -> Optional[str]:
    hist = price_history.get(sym, [])
    now  = time.time()
    day_prices = [p for t, p in hist if now - t <= 86400]
    if len(day_prices) < 20:
        return None
    high24 = max(day_prices[:-1])
    low24  = min(day_prices[:-1])
    if price > high24:
        return f"🔺 пробой хая 24h ({high24:.4g})"
    if price < low24:
        return f"🔻 пробой лоя 24h ({low24:.4g})"
    return None


def get_24h_context(sym: str, price: float) -> Optional[str]:
    hist = price_history.get(sym, [])
    now  = time.time()
    day_prices = [p for t, p in hist if now - t <= 86400]
    if len(day_prices) < 20:
        return None
    high24    = max(day_prices)
    low24     = min(day_prices)
    dist_high = (price - high24) / high24 * 100
    dist_low  = (price - low24)  / low24  * 100
    return f"📉 от хая: {dist_high:+.1f}%  📈 от лоя: {dist_low:+.1f}%"


def get_peak_growth(sym: str, price: float, window_sec: int = 3600) -> float:
    """
    Максимальный рост за window_sec от минимума к текущей цене.
    Используется для разворотного детектора: монета могла уже откатить,
    но пиковый рост сохраняется в истории.
    """
    hist = price_history.get(sym, [])
    now  = time.time()
    window_prices = [p for t, p in hist if now - t <= window_sec]
    if not window_prices:
        return 0.0
    low = min(window_prices)
    if low <= 0:
        return 0.0
    return (price - low) / low * 100


def calculate_obv_divergence(closes: list[float], volumes: list[float],
                              lookback: int = 15) -> bool:
    """
    OBV-дивергенция: цена растёт, On-Balance Volume падает.
    Надёжный признак ослабления покупательского давления.
    """
    try:
        if len(closes) < lookback + 1 or len(volumes) < lookback + 1:
            return False
        # Считаем OBV
        obv = [0.0]
        for i in range(1, len(closes)):
            if closes[i] > closes[i - 1]:
                obv.append(obv[-1] + volumes[i])
            elif closes[i] < closes[i - 1]:
                obv.append(obv[-1] - volumes[i])
            else:
                obv.append(obv[-1])
        # Цена: новый хай за lookback
        price_rising = closes[-1] > max(closes[-(lookback + 1):-1])
        # OBV: падает (текущий < среднего за lookback)
        obv_now  = obv[-1]
        obv_prev = sum(obv[-(lookback + 1):-1]) / lookback
        obv_falling = obv_now < obv_prev * 0.97  # OBV упал на 3%+
        return price_rising and obv_falling
    except Exception:
        return False


def calculate_wick_rejection(highs: list[float], lows: list[float],
                              opens: list[float], closes: list[float],
                              lookback: int = 3) -> Optional[float]:
    """
    Wick Rejection Ratio: отношение верхней тени к полному диапазону.
    >0.6 = сильное отталкивание от верхней зоны (медвежий признак).
    Усредняется за последние lookback свечей.
    """
    try:
        n = min(lookback, len(closes))
        if n < 1:
            return None
        ratios = []
        for i in range(-n, 0):
            rng = highs[i] - lows[i]
            if rng == 0:
                continue
            upper_wick = highs[i] - max(opens[i], closes[i])
            ratios.append(upper_wick / rng)
        return round(sum(ratios) / len(ratios), 3) if ratios else None
    except Exception:
        return None


def calculate_lower_highs(highs: list[float], window: int = 5) -> bool:
    """
    Паттерн убывающих максимумов (Lower Highs) на последних window свечах.
    3+ последовательных снижения хая = структурный разворот.
    """
    try:
        if len(highs) < window + 1:
            return False
        recent = highs[-window:]
        # Считаем сколько раз хай ниже предыдущего
        lower_count = sum(1 for i in range(1, len(recent)) if recent[i] < recent[i - 1])
        return lower_count >= window - 2  # минимум window-2 убывания из window-1 пар
    except Exception:
        return False


def calculate_rsi_higher_tf(prices_15m: list[float], window: int = 12) -> Optional[float]:
    """
    RSI на 15m свечах — аппроксимация «старшего таймфрейма».
    Перекупленность >75 на 15m при уже высоком 1m RSI — сильный сигнал.
    """
    return calculate_rsi(prices_15m, window=window)


# ================================================================
#  REVERSAL DETECTOR  — 20 факторов
# ================================================================

def _esc(s: str) -> str:
    """Экранирование HTML-символов для Telegram parse_mode=HTML."""
    return s.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")


def detect_short_reversal(
    sym:        str,
    price:      float,
    klines_1m:  dict,   # Min1  x120 — RSI, StochRSI, BB, паттерны, wick
    klines_5m:  dict,   # Min5  x100 — ATR, EMA, MACD, объём, моментум, lower highs
    klines_15m: dict,   # Min15 x60  — OBV-дивергенция, RSI старшего TF
    recent:     list[tuple[float, float]],
    growth:     float,
    rsi:        Optional[float],
    min_score:  int = 4,
    rsi_reversal_tf:       Optional[float] = None,
    rsi_reversal_tf_label: str             = "Min15",
) -> dict:
    """
    20-факторный детектор разворота на шорт.

    Min1  (краткосрочные): RSI, StochRSI, BB, паттерн, отбой от хая, Wick Rejection,
                            MACD Min1, серия красных свечей, разворот StochRSI
    Min5  (среднесрочные): MACD, EMA, ATR, моментум, Lower Highs, объём, растяжение от EMA21
    Min15 (долгосрочные):  OBV-дивергенция, RSI-дивергенция, RSI старшего TF
    Тики (акселерация):    замедление импульса

    Факторы (+1 каждый):
      1.  RSI перекупленность (Min1 > RSI_OB)
      2.  StochRSI зона перекупленности (Min1)
      3.  Цена выше верхней полосы Боллинджера %B>BB_OB (Min1)
      4.  MACD медвежий крест или убывающий slope (Min5)
      5.  RSI-дивергенция: цена↑, RSI↓ (Min15)
      6.  Замедление импульса accel < ACCEL (тики)
      7.  Отбой от 24h High с подтверждением свечи (Min1)
      8.  Моментум иссякает fast-slow < MOMENTUM (Min5)
      9.  EMA9 пробила EMA21 вниз или ниже EMA21 (Min5)
      10. ATR-перегрев: движение > ATR_MULT × ATR (Min5, 1 час)
      11. Медвежий свечной паттерн (Min1, реальные opens)
      12. Слабый объём при росте vol < VOL_RATIO (Min5 → Min1 → тики)
      13. OBV-дивергенция: цена↑, OBV↓ (Min15)
      14. Wick Rejection Ratio > 0.55 (Min1, 3 последних свечи)
      15. RSI старшего TF > 75 (Min15)
      16. Lower Highs паттерн (Min5, 5 свечей)
      17. MACD гистограмма падает (Min1, быстрое подтверждение разворота)
      18. Цена растянута выше EMA21 на >1.5% (Min5, перегрев)
      19. Серия из 3+ красных свечей подряд (Min1)
      20. StochRSI разворачивается вниз из экстремума >0.85 (Min1)
    """
    score   = 0
    factors: list[str] = []

    # Распаковываем данные по таймфреймам
    closes_1m   = klines_1m.get("closes",  [])
    highs_1m    = klines_1m.get("highs",   [])
    lows_1m     = klines_1m.get("lows",    [])
    opens_1m    = klines_1m.get("opens",   [])
    volumes_1m  = klines_1m.get("volumes", [])

    closes_5m   = klines_5m.get("closes",  [])
    highs_5m    = klines_5m.get("highs",   [])
    lows_5m     = klines_5m.get("lows",    [])
    volumes_5m  = klines_5m.get("volumes", [])

    closes_15m  = klines_15m.get("closes",  [])
    volumes_15m = klines_15m.get("volumes", [])

    # Fallback на тики если нет свечей
    prices_1m  = closes_1m  if len(closes_1m)  >= 20 else [p for _, p in price_history.get(sym, [])[-120:]]
    prices_5m  = closes_5m  if len(closes_5m)  >= 20 else prices_1m
    prices_15m = closes_15m if len(closes_15m) >= 20 else prices_5m

    # ── 1. RSI перекупленность (Min1) ──────────────────────────────────────────
    if rsi is not None and rsi > REVERSAL_RSI_OB:
        score += 1
        factors.append(f"RSI перекуплен ({rsi:.1f} &gt; {REVERSAL_RSI_OB}) Min1")

    # ── 2. StochRSI (Min1) ─────────────────────────────────────────────────────
    stoch_rsi_val = calculate_stoch_rsi(prices_1m)
    if stoch_rsi_val is not None:
        thr = REVERSAL_STOCH_OB if (rsi is not None and rsi > REVERSAL_RSI_OB) else REVERSAL_STOCH_EXT
        if stoch_rsi_val > thr:
            score += 1
            factors.append(f"StochRSI перекупленность ({stoch_rsi_val:.2f} &gt; {thr}) Min1")

    # ── 3. Боллинджер %B (Min1) ────────────────────────────────────────────────
    bb_pct = calculate_bollinger_pct(prices_1m)
    if bb_pct is not None and bb_pct > REVERSAL_BB_OB:
        score += 1
        factors.append(f"Цена выше верхней BB %B={bb_pct:.2f} Min1")

    # ── 4. MACD (Min5) ─────────────────────────────────────────────────────────
    macd_data = calculate_macd_full(prices_5m)
    macd_hist = macd_data["histogram"]
    if macd_data["cross_down"]:
        score += 1
        factors.append("MACD медвежий крест (hist 0→-) Min5")
    elif (macd_hist is not None
          and macd_data["slope"] is not None
          and macd_hist > 0
          and macd_data["slope"] < REVERSAL_MACD_SLOPE):
        score += 1
        factors.append(f"MACD убывает Min5 (slope={macd_data['slope']:+.6f})")

    # ── 5. RSI-дивергенция (Min15 — более надёжный сигнал) ────────────────────
    if len(prices_15m) >= 30 and calculate_rsi_divergence(prices_15m):
        score += 1
        factors.append("RSI-дивергенция Min15 (цена↑, RSI↓)")
    elif len(prices_5m) >= 30 and calculate_rsi_divergence(prices_5m):
        score += 1
        factors.append("RSI-дивергенция Min5 (цена↑, RSI↓)")

    # ── 6. Замедление импульса (тики) ─────────────────────────────────────────
    accel = calculate_acceleration(recent)
    if accel is not None and growth >= current_percent and accel < REVERSAL_ACCEL:
        score += 1
        factors.append(f"Замедление импульса (accel={accel:.2f}x)")

    # ── 7. Отбой от 24h High (Min1 с реальными opens) ────────────────────────
    hist_data  = price_history.get(sym, [])
    now_ts     = time.time()
    day_prices = [p for t, p in hist_data if now_ts - t <= 86400]
    high24 = max(day_prices) if day_prices else price
    low24  = min(day_prices) if day_prices else price
    if len(day_prices) >= 20:
        near_high = price >= high24 * REVERSAL_HIGH_MARGIN
        if near_high:
            bearish = False
            if len(closes_1m) >= 2 and len(opens_1m) >= 1:
                # Реальная красная свеча у хая (close < open из Bybit)
                bearish = (opens_1m[-1] > closes_1m[-1]  # медвежья свеча
                           and highs_1m[-1] >= high24 * REVERSAL_HIGH_MARGIN)
            if not bearish and len(recent) >= 4:
                ticks = [p for _, p in recent[-4:]]
                falling = sum(ticks[i] > ticks[i + 1] for i in range(len(ticks) - 1))
                bearish = falling >= 2
            if bearish:
                pct = (price - high24) / high24 * 100
                score += 1
                factors.append(f"Отбой от 24h High ({pct:+.2f}%) Min1")

    # ── 8. Моментум иссякает (Min5) ───────────────────────────────────────────
    momentum = calculate_price_momentum(prices_5m)
    if momentum is not None and momentum < REVERSAL_MOMENTUM:
        score += 1
        factors.append(f"Моментум иссякает Min5 (diff={momentum:+.2f}%)")

    # ── 9. EMA крест / ниже EMA (Min5) ────────────────────────────────────────
    ema_data = calculate_ema_cross(prices_5m)
    if ema_data["cross_down"]:
        score += 1
        factors.append(
            f"EMA9 пробила EMA21 вниз Min5 "
            f"({ema_data['ema_fast']:.4g} &lt; {ema_data['ema_slow']:.4g})"
        )
    elif ema_data.get("gap_pct") is not None and ema_data["gap_pct"] < -0.05:
        score += 1
        factors.append(f"EMA9 ниже EMA21 Min5 (gap={ema_data['gap_pct']:+.2f}%)")

    # ── 10. ATR-перегрев (Min5, последний час) ────────────────────────────────
    atr = None
    if len(highs_5m) >= 15 and len(lows_5m) >= 15 and len(closes_5m) >= 15:
        atr = calculate_atr(highs_5m, lows_5m, closes_5m)
        if atr is not None and atr > 0:
            lookback    = min(12, len(closes_5m) - 1)
            period_move = abs(price - closes_5m[-lookback])
            atr_ratio   = period_move / atr
            if atr_ratio > REVERSAL_ATR_MULT:
                score += 1
                factors.append(f"ATR-перегрев Min5 ({atr_ratio:.1f}× ATR={atr:.4g})")

    # ── 11. Свечной паттерн (Min1, реальные opens из Bybit) ──────────────────
    candle_pattern = None
    if len(closes_1m) >= 4 and len(highs_1m) >= 4 and len(lows_1m) >= 4:
        candle_pattern = detect_candle_pattern(
            closes_1m, highs_1m, lows_1m,
            opens_1m if len(opens_1m) >= 4 else None,
        )
        if candle_pattern:
            score += 1
            factors.append(f"Паттерн Min1: {candle_pattern}")

    # ── 12. Слабый объём (Min5 → Min1 → тики) ────────────────────────────────
    vol_signal: Optional[str] = None
    if len(volumes_5m) >= 20:
        vol_signal = calculate_volume_weakness(volumes_5m)
    elif len(volumes_1m) >= 20:
        vol_signal = calculate_volume_weakness(volumes_1m)
    else:
        vol_signal = calculate_volume_signal(sym)
    if vol_signal:
        score += 1
        factors.append(vol_signal)

    # ── 13. OBV-дивергенция (Min15) ───────────────────────────────────────────
    if calculate_obv_divergence(prices_15m, volumes_15m if len(volumes_15m) >= 16
                                else volumes_5m):
        score += 1
        factors.append("OBV-дивергенция Min15 (цена↑, OBV↓)")

    # ── 14. Wick Rejection Ratio > 0.55 (Min1) ───────────────────────────────
    wick_ratio = None
    if len(closes_1m) >= 3 and len(opens_1m) >= 3:
        wick_ratio = calculate_wick_rejection(highs_1m, lows_1m, opens_1m, closes_1m, lookback=3)
        if wick_ratio is not None and wick_ratio > 0.55:
            score += 1
            factors.append(f"Wick Rejection {wick_ratio:.2f} (верхняя тень доминирует) Min1")

    # ── 15. RSI настраиваемого старшего ТФ > REVERSAL_RSI_OB (кнопки) ─────────
    if rsi_reversal_tf is not None:
        rsi_15m       = rsi_reversal_tf
        rsi_15m_label = rsi_reversal_tf_label
    else:
        rsi_15m       = calculate_rsi_higher_tf(prices_15m) if len(prices_15m) >= 16 else None
        rsi_15m_label = "Min15"
    if rsi_15m is not None and rsi_15m > REVERSAL_RSI_OB:
        score += 1
        factors.append(f"RSI перекуплен на {rsi_15m_label} ({rsi_15m:.1f} &gt; {REVERSAL_RSI_OB})")

    # ── 16. Lower Highs паттерн (Min5) ────────────────────────────────────────
    if len(highs_5m) >= 6 and calculate_lower_highs(highs_5m, window=5):
        score += 1
        factors.append("Lower Highs паттерн Min5 (убывающие максимумы)")

    # ── 17. MACD гистограмма падает (Min1, быстрое подтверждение) ────────────
    macd_data_1m = calculate_macd_full(prices_1m)
    macd_hist_1m = macd_data_1m["histogram"]
    if (macd_hist_1m is not None
            and macd_data_1m["histogram_prev"] is not None
            and macd_hist_1m > 0
            and macd_hist_1m < macd_data_1m["histogram_prev"]):
        score += 1
        factors.append(f"MACD гистограмма падает Min1 ({macd_hist_1m:+.6f})")

    # ── 18. Цена сильно растянута выше EMA21 (Min5) ───────────────────────────
    ema_extension_pct = None
    if ema_data.get("ema_slow"):
        ema_extension_pct = (price - ema_data["ema_slow"]) / ema_data["ema_slow"] * 100
        if ema_extension_pct > 1.5:
            score += 1
            factors.append(f"Цена растянута над EMA21 Min5 (+{ema_extension_pct:.2f}%)")

    # ── 19. Серия красных свечей (Min1, 3+ подряд) ────────────────────────────
    red_streak = 0
    if len(closes_1m) >= 3 and len(opens_1m) >= 3:
        for i in range(-1, -4, -1):
            if closes_1m[i] < opens_1m[i]:
                red_streak += 1
            else:
                break
        if red_streak >= 3:
            score += 1
            factors.append(f"Серия красных свечей Min1 ({red_streak} подряд)")

    # ── 20. StochRSI разворачивается вниз из экстремума (Min1) ───────────────
    stoch_turn = False
    if len(prices_1m) >= 30:
        stoch_prev = calculate_stoch_rsi(prices_1m[:-1])
        if (stoch_rsi_val is not None and stoch_prev is not None
                and stoch_prev > 0.85 and stoch_rsi_val < stoch_prev - 0.05):
            stoch_turn = True
            score += 1
            factors.append(f"StochRSI разворот вниз Min1 ({stoch_prev:.2f} → {stoch_rsi_val:.2f})")

    # ── Цели по шорту: уровни Фибоначчи от 24h High к Low ────────────────────
    fib     = calculate_fibonacci_levels(high24, low24)
    target1 = fib["fib_382"]
    target2 = fib["fib_618"]

    day_context = get_24h_context(sym, price)

    return {
        "score":          score,
        "max_score":      20,
        "factors":        factors,
        "triggered":      score >= min_score,
        "rsi":            rsi,
        "rsi_15m":        rsi_15m,
        "rsi_15m_label":  rsi_15m_label,
        "stoch_rsi":      stoch_rsi_val,
        "bb_pct":         bb_pct,
        "macd_hist":      macd_hist,
        "macd_hist_1m":   macd_hist_1m,
        "accel":          accel,
        "atr":            atr,
        "wick_ratio":     wick_ratio,
        "ema_extension_pct": ema_extension_pct,
        "red_streak":     red_streak,
        "stoch_turn":     stoch_turn,
        "target1":        target1,
        "target2":        target2,
        "fib":            fib,
        "high24":         high24,
        "low24":          low24,
        "candle_pattern": candle_pattern,
        "ema_gap":        ema_data.get("gap_pct"),
        "vol_signal":     vol_signal,
        "day_context":    day_context,
    }


# ================================================================
#  TELEGRAM HELPERS
# ================================================================

async def _tg_post(method: str, payload: dict) -> Optional[dict]:
    try:
        async with _session.post(
            f"{TG}/{method}", json=payload, timeout=aiohttp.ClientTimeout(total=20)
        ) as resp:
            data = await resp.json()
            if not data.get("ok"):
                log.warning("Telegram %s error: %s", method, data)
            return data
    except Exception as e:
        log.error("Telegram %s: %s", method, e)
        return None


async def send_message(text: str, chat_id, reply_markup=None):
    payload = {"chat_id": chat_id, "text": text, "parse_mode": "HTML"}
    if reply_markup:
        payload["reply_markup"] = reply_markup
    await _tg_post("sendMessage", payload)


async def send_document(chat_id, filename: str, content: str, caption: str = ""):
    try:
        form = aiohttp.FormData()
        form.add_field("chat_id", str(chat_id))
        form.add_field("caption", caption)
        form.add_field("document", content.encode(), filename=filename, content_type="text/csv")
        async with _session.post(f"{TG}/sendDocument", data=form,
                                  timeout=aiohttp.ClientTimeout(total=30)) as resp:
            return await resp.json()
    except Exception as e:
        log.error("sendDocument: %s", e)


async def broadcast(text: str, reply_markup=None):
    tasks = [send_message(text, cid, reply_markup) for cid in db_get_subscribers()]
    await asyncio.gather(*tasks, return_exceptions=True)


# ================================================================
#  v25.10: УРОВНИ ПОДДЕРЖКИ/СОПРОТИВЛЕНИЯ — 5 независимых факторов
# ================================================================
#
# Ни один метод сам по себе не даёт надёжный уровень — поэтому итоговые
# линии на графике это КЛАСТЕРЫ близких цен, в которых сошлось НЕСКОЛЬКО
# независимых факторов (confluence). Чем больше факторов совпало в одной
# точке — тем выше её "сила" и тем более приоритетно она показывается на
# графике вместо соседних одиночных, неподтверждённых кандидатов. Это тот
# же принцип, которым пользуются трейдеры вручную: "хороший" уровень —
# это тот, который одновременно является и структурным разворотом, и
# зоной объёма, и классическим pivot'ом, а не просто одна случайная свеча.
#
# Учтённые факторы:
#   1) Фрактальные экстремумы (свинг-хай/лоу) в нескольких окнах (2/3/5
#      баров с каждой стороны) — структура цены. Чем в более широком окне
#      точка остаётся экстремумом, тем значимее разворот в этом месте.
#   2) Профиль объёма (Volume Profile): цена разбивается на бины, в
#      каждый суммируется объём по типичной цене свечи (H+L+C)/3 — бины
#      с наибольшим объёмом (Point of Control и вторичные HVN) отмечают
#      зоны, где реально прошло больше всего сделок.
#   3) Классические Pivot Points (Floor Trader) по ПРЕДЫДУЩИМ суткам
#      (Day1, независимо от ТФ самого графика) — PP/R1-R3/S1-S3.
#   4) Крайние хай/лоу видимого окна графика — очевидные зоны разворота.
#   5) "Круглые" психологические уровни, шаг подобран под масштаб цены
#      (1/2/2.5/5 × 10^n), чтобы работать что для BTC (~65000), что для
#      мелких монет (~0.00002).


def _fractal_sr_points(highs: list[float], lows: list[float]) -> list[tuple[float, float, str]]:
    """Фрактальные свинг-хай/лоу в нескольких окнах.
    Возвращает (цена, вес, сторона) где сторона 'R' — от хая, 'S' — от лоя.
    Вес растёт с шириной окна: более широкое подтверждение = более
    значимый разворот структуры цены."""
    n = len(highs)
    out: list[tuple[float, float, str]] = []
    for k in (2, 3, 5):
        if n < 2 * k + 1:
            continue
        for i in range(k, n - k):
            window_h = highs[i - k:i + k + 1]
            window_l = lows[i - k:i + k + 1]
            if highs[i] == max(window_h) and window_h.count(highs[i]) == 1:
                out.append((highs[i], float(k), "R"))
            if lows[i] == min(window_l) and window_l.count(lows[i]) == 1:
                out.append((lows[i], float(k), "S"))
    return out


def _volume_profile_sr_points(highs: list[float], lows: list[float],
                               closes: list[float], volumes: list[float]) -> list[tuple[float, float, str]]:
    """Точки высокого объёма (Volume Profile): гистограмма объёма по цене,
    локальные максимумы которой (Point of Control + вторичные HVN) —
    кандидаты в уровни. Сторона определяется относительно текущей цены."""
    n = len(closes)
    if n < 5 or not any(volumes):
        return []
    lo, hi = min(lows), max(highs)
    if hi <= lo:
        return []
    bins  = 30
    width = (hi - lo) / bins
    vol_bins = [0.0] * bins

    def _bin_idx(p: float) -> int:
        idx = int((p - lo) / width) if width > 0 else 0
        return max(0, min(bins - 1, idx))

    for i in range(n):
        typical = (highs[i] + lows[i] + closes[i]) / 3
        vol_bins[_bin_idx(typical)] += volumes[i]

    max_vol = max(vol_bins) or 1.0
    current = closes[-1]
    out: list[tuple[float, float, str]] = []
    for i, v in enumerate(vol_bins):
        if v <= 0:
            continue
        left  = vol_bins[i - 1] if i > 0 else 0.0
        right = vol_bins[i + 1] if i < bins - 1 else 0.0
        # локальный максимум гистограммы объёма (HVN), не слишком мелкий
        if v >= left and v >= right and v >= max_vol * 0.35:
            price  = lo + width * (i + 0.5)
            weight = 3.0 if v == max_vol else 2.0   # POC весомее вторичных узлов
            out.append((price, weight, "R" if price >= current else "S"))
    return out


def calculate_pivot_points(high: float, low: float, close: float) -> dict:
    """Классические Floor Trader Pivot Points по предыдущему периоду
    (используются данные предыдущих суток) — общепринятая формула
    технического анализа, не зависящая от ТФ самого графика."""
    pp = (high + low + close) / 3
    r1 = 2 * pp - low
    s1 = 2 * pp - high
    r2 = pp + (high - low)
    s2 = pp - (high - low)
    r3 = high + 2 * (pp - low)
    s3 = low - 2 * (high - pp)
    return {"pp": pp, "r1": r1, "r2": r2, "r3": r3, "s1": s1, "s2": s2, "s3": s3}


def _round_number_sr_levels(lo: float, hi: float) -> list[float]:
    """'Круглые' психологические уровни в видимом диапазоне графика.
    Шаг подбирается под масштаб цены (1/2/2.5/5 × 10^n) — тот же приём,
    которым графические библиотеки расставляют деления оси — поэтому
    работает одинаково хорошо что для BTC (~65000), что для мелких монет
    (~0.00002), без единого захардкоженного числа."""
    rng = hi - lo
    if rng <= 0:
        return []
    raw_step  = rng / 6
    magnitude = 10 ** math.floor(math.log10(raw_step))
    step = raw_step
    for m in (1, 2, 2.5, 5, 10):
        cand = m * magnitude
        if cand >= raw_step:
            step = cand
            break
    if step <= 0:
        return []
    levels = []
    x = math.floor(lo / step) * step
    while x <= hi + step:
        if lo <= x <= hi:
            levels.append(round(x, 12))
        x += step
    return levels


SR_MAX_LEVELS = getattr(config, "SR_MAX_LEVELS", 3)   # макс. уровней поддержки/сопротивления на графике

# ================================================================
#  v25.11: КРУПНЫЕ ЛИМИТНЫЕ ЗАЯВКИ (СТЕНЫ) В СТАКАНЕ — НА ГРАФИК
# ================================================================
# Показывает на графике реальные крупные лимитные заявки (bid/ask "стены")
# из стакана Bybit (category=linear), а не расчётные уровни S/R. Данные —
# отдельный дешёвый запрос /v5/market/orderbook с коротким кэшем (TTL
# WALL_CACHE_TTL, по умолчанию 15с) — не нагружает Bybit на каждый тик,
# так как вызывается только при построении графика (по сигналу/команде).
WALL_ORDERBOOK_DEPTH = getattr(config, "WALL_ORDERBOOK_DEPTH", 200)   # глубина стакана (уровней с каждой стороны)
WALL_TOP_N            = getattr(config, "WALL_TOP_N", 4)              # макс. "стен" с каждой стороны на графике свечей
WALL_NEAREST_N        = getattr(config, "WALL_NEAREST_N", 5)          # v25.12: макс. ближайших "стен" с каждой стороны на отдельном графике /walls
WALL_MIN_USD          = getattr(config, "WALL_MIN_USD", 20_000)       # мин. объём заявки в USDT, чтобы считаться "стеной"
WALL_MIN_REL_MULT     = getattr(config, "WALL_MIN_REL_MULT", 3.0)     # мин. во сколько раз крупнее медианной заявки в стакане
WALL_CACHE_TTL        = getattr(config, "WALL_CACHE_TTL", 15)         # сек., кэш ответа стакана
ALERT_WALLS_N         = getattr(config, "ALERT_WALLS_N", 3)           # v25.13: макс. ближайших "стен" с каждой стороны в тексте уведомлений роста/падения/24ч


async def _fetch_bybit_orderbook(symbol: str, limit: int = WALL_ORDERBOOK_DEPTH) -> dict:
    """
    Возвращает {"bids": [(price, qty), ...], "asks": [(price, qty), ...]}
    (список уровней стакана Bybit, category=linear). Пустые списки при
    любой ошибке/блокировке — вызывающий код никогда не падает и просто
    не рисует стены на графике.
    """
    empty = {"bids": [], "asks": []}
    if _bybit_block_active():
        return empty
    try:
        async with _get_bybit_sem():
            async with _session.get(
                "https://api.bybit.com/v5/market/orderbook",
                params={"category": "linear", "symbol": symbol, "limit": str(limit)},
                timeout=aiohttp.ClientTimeout(total=10),
            ) as r:
                status = r.status
                raw    = await r.text()
        if _looks_like_bybit_block(status, raw):
            _note_bybit_block(raw)
            return empty
        data = json.loads(raw)
        if data.get("retCode") == 0:
            result = data.get("result", {}) or {}
            bids = [(float(p), float(q)) for p, q in result.get("b", []) if float(q) > 0]
            asks = [(float(p), float(q)) for p, q in result.get("a", []) if float(q) > 0]
            return {"bids": bids, "asks": asks}
    except Exception as e:
        log.debug("Bybit orderbook %s: %s", symbol, e)
    return empty


async def get_bybit_orderbook(symbol: str, limit: int = WALL_ORDERBOOK_DEPTH) -> dict:
    cache_key = f"ob:{symbol}:{limit}"
    now       = time.time()
    cached    = _kline_cache.get(cache_key)
    if cached and now - cached["ts"] < WALL_CACHE_TTL:
        return cached["data"]
    data = await _fetch_bybit_orderbook(symbol, limit)
    if data["bids"] or data["asks"]:
        _kline_cache[cache_key] = {"ts": now, "data": data}
    return data


def _qualifying_walls(levels: list[tuple[float, float]]) -> list[dict]:
    """
    Общий фильтр "это реально стена": объём в USDT >= WALL_MIN_USD И объём
    в базовой монете превышает медианный размер заявки на переданных
    уровнях стакана минимум в WALL_MIN_REL_MULT раз. Используется как для
    графика со свечами (в пределах видимого окна цен), так и для отдельного
    графика ближайших заявок (по всему стакану) — логика одна и та же.
    """
    if not levels:
        return []
    qtys = sorted(q for _, q in levels)
    mid = len(qtys) // 2
    median_qty = qtys[mid] if len(qtys) % 2 else (qtys[mid - 1] + qtys[mid]) / 2
    median_qty = median_qty or 1e-12
    out = []
    for p, q in levels:
        usd = p * q
        if usd >= WALL_MIN_USD and q >= median_qty * WALL_MIN_REL_MULT:
            out.append({"price": p, "qty": q, "usd": usd})
    return out


def find_large_limit_orders(orderbook: dict, lo: float, hi: float) -> dict:
    """
    Не более WALL_TOP_N сильнейших (по объёму в USDT) "стен" с каждой
    стороны, только внутри видимого диапазона цен графика [lo, hi] —
    используется для наложения на свечной график.
    """
    out = {"bids": [], "asks": []}
    for side in ("bids", "asks"):
        levels = [(p, q) for p, q in orderbook.get(side, []) if lo <= p <= hi]
        walls = _qualifying_walls(levels)
        walls.sort(key=lambda w: w["usd"], reverse=True)
        out[side] = walls[:WALL_TOP_N]
    return out


def find_nearest_large_limit_orders(orderbook: dict, current_price: float, n: int = WALL_NEAREST_N) -> dict:
    """
    v25.12: та же самая логика отбора "это реально стена" (см.
    _qualifying_walls), но без ограничения по видимому окну цен графика —
    берётся ВЕСЬ загруженный стакан, а результат — это n БЛИЖАЙШИХ к
    текущей цене крупных заявок с каждой стороны (bid и ask отдельно),
    отсортированных по цене от текущей цены наружу. Используется для
    отдельного графика /walls.
    """
    out = {"bids": [], "asks": []}
    for side in ("bids", "asks"):
        walls = _qualifying_walls(list(orderbook.get(side, [])))
        walls.sort(key=lambda w: abs(w["price"] - current_price))
        out[side] = walls[:n]
    return out


async def format_walls_alert_line(sym: str, current_price: float) -> str:
    """
    v25.13: компактная строка "🧱 Крупные заявки: SELL ..., BUY ..." с
    ТОЧНЫМИ ЦЕНАМИ ближайших к текущей цене крупных лимитных заявок (не
    более ALERT_WALLS_N с каждой стороны) — для уведомлений роста/падения/
    24ч. Логика отбора "это реально стена" ровно та же, что и на графиках
    (см. _qualifying_walls): объём >= WALL_MIN_USD USDT И объём в базовой
    монете >= медианного объёма заявки в стакане * WALL_MIN_REL_MULT.
    Данные — тот же кэшированный get_bybit_orderbook (TTL=15с), что и у
    графиков — лишнего запроса на каждый сигнал не создаёт, если график
    уже запрашивал стакан по этой монете недавно.
    Возвращает "" при любой проблеме (нет данных, нет подходящих заявок) —
    остальной текст уведомления при этом не меняется и не теряется.
    """
    try:
        ob = await get_bybit_orderbook(sym)
        if not ob["bids"] and not ob["asks"]:
            return ""
        walls = find_nearest_large_limit_orders(ob, current_price, ALERT_WALLS_N)
        if not walls["bids"] and not walls["asks"]:
            return ""
        parts = []
        asks_sorted = sorted(walls["asks"], key=lambda w: w["price"])
        bids_sorted = sorted(walls["bids"], key=lambda w: w["price"], reverse=True)
        if asks_sorted:
            asks_txt = ", ".join(
                f"{_fmt_level_price(w['price'])} ({_fmt_usd(w['usd'])})" for w in asks_sorted
            )
            parts.append(f"SELL {asks_txt}")
        if bids_sorted:
            bids_txt = ", ".join(
                f"{_fmt_level_price(w['price'])} ({_fmt_usd(w['usd'])})" for w in bids_sorted
            )
            parts.append(f"BUY {bids_txt}")
        if not parts:
            return ""
        return "\n🧱 Крупные заявки: " + "  |  ".join(parts)
    except Exception as e:
        log.debug("format_walls_alert_line(%s): %s", sym, e)
        return ""


def calculate_support_resistance(
    highs: list[float], lows: list[float], closes: list[float], volumes: list[float],
    daily_hlc: Optional[tuple[float, float, float]] = None,
    max_levels: int = SR_MAX_LEVELS,
) -> dict:
    """
    Многофакторный расчёт уровней поддержки/сопротивления для видимого
    окна графика (см. заголовок раздела — 5 факторов). Возвращает
    {"support": [...], "resistance": [...]} — списки словарей
    {"price": float, "weight": float, "sources": set[str]}, отсортированные
    так, что ближний к текущей цене уровень идёт первым; максимум
    max_levels в каждую сторону.
    """
    n = len(closes)
    if n < 10:
        return {"support": [], "resistance": []}

    current  = closes[-1]
    lo, hi   = min(lows), max(highs)
    if current <= 0 or hi <= lo:
        return {"support": [], "resistance": []}

    candidates: list[tuple[float, float, str, str]] = []   # (цена, вес, сторона, источник)

    for price, weight, side in _fractal_sr_points(highs, lows):
        candidates.append((price, weight, side, "structure"))

    for price, weight, side in _volume_profile_sr_points(highs, lows, closes, volumes):
        candidates.append((price, weight, side, "volume"))

    # Крайние хай/лоу видимого окна — весомый уровень сам по себе
    candidates.append((hi, 3.0, "R", "range"))
    candidates.append((lo, 3.0, "S", "range"))

    if daily_hlc:
        piv = calculate_pivot_points(*daily_hlc)
        weights = {"pp": 2.5, "r1": 2.0, "s1": 2.0, "r2": 1.5, "s2": 1.5, "r3": 1.0, "s3": 1.0}
        margin = (hi - lo) * 0.15
        for key, price in piv.items():
            if lo - margin <= price <= hi + margin:
                if key == "pp":
                    side = "S" if price < current else "R"
                else:
                    side = "R" if key.startswith("r") else "S"
                candidates.append((price, weights[key], side, "pivot"))

    for price in _round_number_sr_levels(lo, hi):
        # самый слабый по весу фактор сам по себе — усиливает другие уровни
        # только при реальном совпадении (confluence), не создаёт свои
        candidates.append((price, 0.7, "S" if price < current else "R", "round"))

    if not candidates:
        return {"support": [], "resistance": []}

    # ── Кластеризация: уровни ближе друг к другу, чем tol от текущей
    # цены, схлопываются в один (цена — среднее взвешенное по силе, вес —
    # сумма). Совпадение (confluence) нескольких факторов в одной точке —
    # самый надёжный признак реального уровня, поэтому именно это и
    # определяет итоговую точность и приоритет отображаемых линий.
    tol = max(current * 0.003, (hi - lo) * 0.006)
    candidates.sort(key=lambda c: c[0])
    clusters: list[dict] = []
    for price, weight, side, source in candidates:
        if clusters and abs(price - clusters[-1]["price"]) <= tol:
            cl = clusters[-1]
            total = cl["weight"] + weight
            cl["price"]  = (cl["price"] * cl["weight"] + price * weight) / total
            cl["weight"] = total
            cl["sources"].add(source)
        else:
            clusters.append({"price": price, "weight": weight, "sources": {source}})

    for cl in clusters:
        if len(cl["sources"]) > 1:
            cl["weight"] *= 1.0 + 0.25 * (len(cl["sources"]) - 1)

    support    = sorted((c for c in clusters if c["price"] < current), key=lambda c: c["weight"], reverse=True)
    resistance = sorted((c for c in clusters if c["price"] > current), key=lambda c: c["weight"], reverse=True)

    def _pick_top(levels: list[dict]) -> list[dict]:
        chosen: list[dict] = []
        min_gap = max(current * 0.004, tol)
        for c in levels:
            if all(abs(c["price"] - p["price"]) >= min_gap for p in chosen):
                chosen.append(c)
            if len(chosen) >= max_levels:
                break
        return chosen

    top_support    = _pick_top(support)
    top_resistance = _pick_top(resistance)

    # Для отображения на графике — ближний к текущей цене уровень первым
    top_support.sort(key=lambda c: c["price"], reverse=True)
    top_resistance.sort(key=lambda c: c["price"])

    return {"support": top_support, "resistance": top_resistance}


def _fmt_level_price(price: float) -> str:
    """Цена уровня для подписи на графике без научной нотации — даже для
    монет с ценой в районе тысячных/стотысячных долей (SHIB, PEPE и т.п.),
    где обычный формат :g уходит в scientific notation и плохо читается."""
    if price == 0:
        return "0"
    abs_p = abs(price)
    if abs_p >= 1000:
        return f"{price:,.1f}".replace(",", " ")
    if abs_p >= 1:
        return f"{price:.4f}".rstrip("0").rstrip(".")
    digits = max(4, -int(math.floor(math.log10(abs_p))) + 3)
    return f"{price:.{digits}f}"


# ================================================================
#  v25: СКРИНШОТ ГРАФИКА (генерируется прямо в Python, в реальном
#  времени, по свежим свечам Bybit 1H — визуально похоже на сам Bybit)
# ================================================================

_matplotlib_warned = False   # чтобы не спамить лог, если matplotlib не установлен


async def generate_chart_png(sym: str) -> Optional[bytes]:
    """
    Строит свечной график таймфрейма CHART_TF (по умолчанию 1H, как на самом
    Bybit) прямо в Python по свежим свечам с Bybit (get_bybit_klines — тот же
    кэшированный источник, что и для индикаторов, TTL=60с, лишних запросов
    не создаёт). Тёмная тема + зелёные/красные свечи, похоже на сам Bybit.

    Возвращает None при любой проблеме (нет matplotlib, нет данных и т.п.) —
    вызывающий код в этом случае просто шлёт обычное текстовое сообщение,
    сигнал никогда не теряется из-за графика.
    """
    global _matplotlib_warned
    try:
        import matplotlib
        matplotlib.use("Agg")
        import matplotlib.pyplot as plt
        from matplotlib.patches import Rectangle
    except Exception as e:
        if not _matplotlib_warned:
            log.warning("matplotlib недоступен — графики в сигналах отключены: %s", e)
            _matplotlib_warned = True
        return None

    try:
        data = await get_bybit_klines(sym, CHART_TF, CHART_TF_CANDLES.get(CHART_TF, 72))
        opens, highs, lows, closes = data["opens"], data["highs"], data["lows"], data["closes"]
        volumes = data["volumes"]
        n = len(closes)
        if n < 2:
            return None

        # v25.10: уровни поддержки/сопротивления — считаются по тем же
        # свечам, что уже загружены для самого графика (без доп. запроса),
        # плюс предыдущие сутки (Day1) отдельно — только для классических
        # pivot points, тот же кэшированный get_bybit_klines (TTL=60с), так
        # что при обычной частоте сигналов лишней нагрузки на Bybit не даёт.
        daily_hlc = None
        try:
            daily = await get_bybit_klines(sym, "Day1", 3)
            if len(daily["closes"]) >= 2:
                # последняя свеча — текущие незакрытые сутки, для pivot
                # нужны именно ПРЕДЫДУЩИЕ завершённые сутки
                daily_hlc = (daily["highs"][-2], daily["lows"][-2], daily["closes"][-2])
        except Exception as e:
            log.debug("generate_chart_png(%s) daily pivot: %s", sym, e)
        try:
            sr_levels = calculate_support_resistance(highs, lows, closes, volumes, daily_hlc)
        except Exception as e:
            log.debug("generate_chart_png(%s) S/R: %s", sym, e)
            sr_levels = {"support": [], "resistance": []}

        up_color, down_color = "#0ECB81", "#F6465D"
        bg_color, grid_color, txt_color = "#161A1E", "#2B3139", "#848E9C"
        sr_r_color, sr_s_color = "#FFB74D", "#4FC3F7"   # сопротивление/поддержка — не путать с красными/зелёными свечами
        wall_bid_color, wall_ask_color = "#26E07F", "#FF5C5C"  # крупные лимитные заявки (стены стакана): bid/ask

        fig, ax = plt.subplots(figsize=(9, 5), dpi=130)
        fig.patch.set_facecolor(bg_color)
        ax.set_facecolor(bg_color)

        lo, hi   = min(lows), max(highs)
        min_body = max((hi - lo) * 0.0015, 1e-12)
        width    = 0.62

        for i in range(n):
            color = up_color if closes[i] >= opens[i] else down_color
            ax.plot([i, i], [lows[i], highs[i]], color=color, linewidth=1.1, zorder=2, solid_capstyle="round")
            body_lo = min(opens[i], closes[i])
            body_h  = max(abs(closes[i] - opens[i]), min_body)
            ax.add_patch(Rectangle(
                (i - width / 2, body_lo), width, body_h,
                facecolor=color, edgecolor=color, linewidth=0.4, zorder=3,
            ))

        # v25.10: правый отступ под подписи цены уровней поддержки/
        # сопротивления (сами линии рисуются чуть ниже, после сетки/осей).
        margin_x = max(8, round(n * 0.14))
        margin_x_left = max(6, round(n * 0.11))   # v25.11: отступ слева под подписи стен стакана
        ax.set_xlim(-1 - margin_x_left, n - 1 + margin_x)
        pad = (hi - lo) * 0.06 or hi * 0.01 or 1.0
        ax.set_ylim(lo - pad, hi + pad)
        ax.grid(True, color=grid_color, linewidth=0.5, alpha=0.55, axis="y")
        ax.tick_params(colors=txt_color, labelsize=8)
        for spine in ax.spines.values():
            spine.set_color(grid_color)
        ax.set_xticks([])

        # v25.11: крупные лимитные заявки (стены) из реального стакана Bybit —
        # отдельный лёгкий запрос с коротким кэшем (см. get_bybit_orderbook),
        # любая ошибка просто даёт пустые списки, график всё равно строится.
        walls = {"bids": [], "asks": []}
        try:
            ob = await get_bybit_orderbook(sym)
            if ob["bids"] or ob["asks"]:
                walls = find_large_limit_orders(ob, lo - pad, hi + pad)
        except Exception as e:
            log.debug("generate_chart_png(%s) walls: %s", sym, e)

        # v25.10: уровни поддержки/сопротивления — горизонтальная линия на
        # всю ширину графика + подпись цены в отступе справа. Цвета
        # отдельные от красных/зелёных свечей и от жёлтой линии текущей
        # цены, чтобы типы линий не путались между собой визуально.
        label_x = n - 1 + margin_x * 0.22
        for lvl in sr_levels["resistance"]:
            p = lvl["price"]
            if not (lo - pad <= p <= hi + pad):
                continue   # pivot-уровень может быть чуть за пределами видимого окна — не рисуем
            ax.axhline(p, color=sr_r_color, linewidth=1.0, linestyle=(0, (4, 2)), alpha=0.8, zorder=3.3)
            ax.text(label_x, p, f" R {_fmt_level_price(p)}", color=sr_r_color, fontsize=7.3,
                    ha="left", va="center", zorder=5, clip_on=False,
                    bbox=dict(boxstyle="round,pad=0.15", facecolor=bg_color, edgecolor="none", alpha=0.8))
        for lvl in sr_levels["support"]:
            p = lvl["price"]
            if not (lo - pad <= p <= hi + pad):
                continue
            ax.axhline(p, color=sr_s_color, linewidth=1.0, linestyle=(0, (4, 2)), alpha=0.8, zorder=3.3)
            ax.text(label_x, p, f" S {_fmt_level_price(p)}", color=sr_s_color, fontsize=7.3,
                    ha="left", va="center", zorder=5, clip_on=False,
                    bbox=dict(boxstyle="round,pad=0.15", facecolor=bg_color, edgecolor="none", alpha=0.8))

        # v25.11: рисуем стены отдельно от S/R — сплошная (не пунктирная)
        # линия на всю ширину + подпись СЛЕВА от графика (у S/R подпись
        # справа), чтобы визуально не путать расчётные уровни S/R с реальными
        # крупными заявками в стакане. Толщина/прозрачность растёт вместе с
        # объёмом заявки в USDT.
        wall_label_x = -1 - margin_x_left * 0.22
        max_wall_usd = max([w["usd"] for w in walls["bids"] + walls["asks"]], default=0)
        for side, color, tag in (("bids", wall_bid_color, "🧱 BUY"), ("asks", wall_ask_color, "🧱 SELL")):
            for w in walls[side]:
                p = w["price"]
                rel = (w["usd"] / max_wall_usd) if max_wall_usd else 1.0
                lw = 1.3 + rel * 2.2
                alpha = 0.5 + rel * 0.35
                ax.axhline(p, color=color, linewidth=lw, alpha=min(alpha, 0.9), zorder=3.2)
                ax.text(wall_label_x, p, f"{tag} {_fmt_usd(w['usd'])} ", color=color,
                        fontsize=7, ha="right", va="center", zorder=5, clip_on=False,
                        bbox=dict(boxstyle="round,pad=0.15", facecolor=bg_color, edgecolor="none", alpha=0.75))

        last_price = closes[-1]
        last_color = up_color if closes[-1] >= opens[0] else down_color
        ax.axhline(last_price, color="#F0B90B", linewidth=0.8, linestyle="--", alpha=0.85, zorder=4)
        tf_label = CHART_TF_LABELS.get(CHART_TF, CHART_TF)
        ax.set_title(
            f"{sym}   •   {tf_label}   •   {last_price:g}",
            color="#EAECEF", fontsize=12, loc="left", fontweight="bold", pad=10,
        )
        ax.text(
            0.99, 0.02, "Bybit", transform=ax.transAxes, color=txt_color,
            fontsize=8, ha="right", va="bottom", alpha=0.8,
        )

        buf = io.BytesIO()
        fig.tight_layout()
        fig.savefig(buf, format="png", facecolor=bg_color)
        plt.close(fig)
        buf.seek(0)
        return buf.getvalue()
    except Exception as e:
        log.warning("generate_chart_png(%s): %s", sym, e)
        return None


async def generate_orderbook_wall_chart(sym: str) -> Optional[bytes]:
    """
    v25.12: ОТДЕЛЬНЫЙ график — только 5 (WALL_NEAREST_N) ближайших к текущей
    цене крупных лимитных заявок с каждой стороны (bid/ask), горизонтальные
    полосы-"бары", отсортированные по цене. Логика отбора "это реально
    стена" — та же самая, что и на свечном графике (см. _qualifying_walls):
    объём >= WALL_MIN_USD USDT И объём в базовой монете >= медианного
    объёма заявки в стакане * WALL_MIN_REL_MULT. У каждой полосы подписана
    ТОЧНАЯ ЦЕНА заявки и её объём в USD. Текущая цена показана отдельной
    пунктирной линией с подписью.

    Возвращает None при любой проблеме (нет matplotlib, нет стакана, нет ни
    одной заявки, прошедшей фильтр и т.п.) — вызывающий код в этом случае
    просто сообщает, что подходящих крупных заявок сейчас нет.
    """
    global _matplotlib_warned
    try:
        import matplotlib
        matplotlib.use("Agg")
        import matplotlib.pyplot as plt
    except Exception as e:
        if not _matplotlib_warned:
            log.warning("matplotlib недоступен — графики в сигналах отключены: %s", e)
            _matplotlib_warned = True
        return None

    try:
        ob = await get_bybit_orderbook(sym)
        if not ob["bids"] and not ob["asks"]:
            return None

        best_bid = max((p for p, _ in ob["bids"]), default=None)
        best_ask = min((p for p, _ in ob["asks"]), default=None)
        if best_bid and best_ask:
            current_price = (best_bid + best_ask) / 2
        else:
            current_price = best_bid or best_ask
        if not current_price:
            return None

        walls = find_nearest_large_limit_orders(ob, current_price, WALL_NEAREST_N)
        if not walls["bids"] and not walls["asks"]:
            return None

        bg_color, grid_color, txt_color = "#161A1E", "#2B3139", "#848E9C"
        wall_bid_color, wall_ask_color = "#26E07F", "#FF5C5C"

        # Сверху вниз — от самой высокой цены (дальние ask) к самой низкой
        # (дальние bid), текущая цена — граница между ask- и bid-заявками.
        rows = (
            [("ask", w) for w in sorted(walls["asks"], key=lambda w: w["price"], reverse=True)]
            + [("bid", w) for w in sorted(walls["bids"], key=lambda w: w["price"], reverse=True)]
        )
        n_rows = len(rows)
        max_usd = max((w["usd"] for _, w in rows), default=0) or 1.0

        fig, ax = plt.subplots(figsize=(8, max(3.2, 0.62 * n_rows + 1.4)), dpi=130)
        fig.patch.set_facecolor(bg_color)
        ax.set_facecolor(bg_color)

        for i, (side, w) in enumerate(rows):
            y = n_rows - 1 - i   # первая строка (самая высокая цена) — сверху
            color = wall_ask_color if side == "ask" else wall_bid_color
            width = w["usd"] if side == "ask" else -w["usd"]
            ax.barh(y, width, color=color, alpha=0.85, height=0.6, zorder=3)
            price_txt = _fmt_level_price(w["price"])
            label = f"{price_txt}   {_fmt_usd(w['usd'])}"
            if side == "ask":
                ax.text(width + max_usd * 0.02, y, label, color=color, fontsize=8.5,
                        ha="left", va="center", zorder=4)
            else:
                ax.text(width - max_usd * 0.02, y, label, color=color, fontsize=8.5,
                        ha="right", va="center", zorder=4)

        ax.axvline(0, color=grid_color, linewidth=1.0, zorder=2)
        ax.set_xlim(-max_usd * 1.55, max_usd * 1.55)
        ax.set_ylim(-1, n_rows)
        ax.set_yticks([])
        ax.set_xticks([])
        for spine in ax.spines.values():
            spine.set_visible(False)

        ax.text(-max_usd * 1.5, n_rows - 0.3, "🧱 SELL (ask)", color=wall_ask_color,
                fontsize=9, ha="left", va="top", fontweight="bold")
        ax.text(max_usd * 1.5, n_rows - 0.3, "🧱 BUY (bid)", color=wall_bid_color,
                fontsize=9, ha="right", va="top", fontweight="bold")

        cur_price_txt = _fmt_level_price(current_price)
        ax.set_title(
            f"{sym}   •   крупные заявки в стакане   •   цена {cur_price_txt}",
            color="#EAECEF", fontsize=11.5, loc="left", fontweight="bold", pad=10,
        )
        ax.text(
            0.99, -0.06, "Bybit orderbook", transform=ax.transAxes, color=txt_color,
            fontsize=8, ha="right", va="top", alpha=0.8,
        )

        buf = io.BytesIO()
        fig.tight_layout()
        fig.savefig(buf, format="png", facecolor=bg_color)
        plt.close(fig)
        buf.seek(0)
        return buf.getvalue()
    except Exception as e:
        log.warning("generate_orderbook_wall_chart(%s): %s", sym, e)
        return None


async def send_photo(chat_id, photo_bytes: bytes, caption: str = "") -> Optional[dict]:
    try:
        form = aiohttp.FormData()
        form.add_field("chat_id", str(chat_id))
        if caption:
            form.add_field("caption", caption)
            form.add_field("parse_mode", "HTML")
        form.add_field("photo", photo_bytes, filename="chart.png", content_type="image/png")
        async with _session.post(f"{TG}/sendPhoto", data=form,
                                  timeout=aiohttp.ClientTimeout(total=30)) as resp:
            return await resp.json()
    except Exception as e:
        log.error("sendPhoto: %s", e)
        return None


async def broadcast_photo(photo_bytes: bytes, caption: str = ""):
    tasks = [send_photo(cid, photo_bytes, caption) for cid in db_get_subscribers()]
    await asyncio.gather(*tasks, return_exceptions=True)


TELEGRAM_CAPTION_LIMIT = 1024  # официальный лимит Telegram на подпись к фото


async def broadcast_with_chart(sym: str, text: str):
    """
    Отправляет алерт как фото со свечным графиком 1H (если график включён
    кнопкой 🖼 и успешно сгенерировался), иначе — полностью безопасный
    fallback на обычное текстовое сообщение (сигнал никогда не теряется).
    Если текст длиннее лимита подписи Telegram (актуально для разворотов
    с длинным списком факторов) — фото уходит без подписи, а следом
    отдельным сообщением идёт полный текст.
    """
    if CHART_ENABLED:
        try:
            png = await generate_chart_png(sym)
        except Exception as e:
            log.warning("broadcast_with_chart(%s): %s", sym, e)
            png = None
        if png:
            if len(text) <= TELEGRAM_CAPTION_LIMIT:
                await broadcast_photo(png, caption=text)
            else:
                await broadcast_photo(png, caption="")
                await broadcast(text)
            return
    await broadcast(text)


# ================================================================
#  v25.1: ЛИКВИДАЦИИ (Bybit WS allLiquidation.{symbol})
#  Только данные для строки в сигналах роста/падения/разворота — никаких
#  новых команд/кнопок/фильтров. Полностью изолированный фоновый процесс:
#  любая ошибка WS просто означает отсутствие строки в сигнале, остальной
#  бот не затрагивается.
# ================================================================

def _fmt_usd(v: float) -> str:
    if v >= 1_000_000:
        return f"${v / 1_000_000:.2f}M"
    if v >= 1_000:
        return f"${v / 1_000:.1f}K"
    return f"${v:.0f}"


def _liq_prune(sym: str, now: float):
    events = _liq_events.get(sym)
    if not events:
        return
    cutoff = now - LIQ_MAX_STORE_SEC
    kept = [e for e in events if e[0] >= cutoff]
    if kept:
        _liq_events[sym] = kept
    else:
        _liq_events.pop(sym, None)


def get_liq_summary(sym: str, window_sec: int = LIQ_WINDOW_SEC) -> Optional[dict]:
    """
    Сводка по ликвидациям монеты за последние window_sec секунд:
    {"long_usd": ..., "short_usd": ..., "total_usd": ..., "count": ...}.
    None — если по монете не было ни одного события (WS ещё не успел
    накопить данные, либо соединение недоступно) — сигналы никогда не
    ждут эти данные и не блокируются их отсутствием.

    S="Buy"  → ликвидирован ЛОНГ  (принудительная продажа → давление вниз)
    S="Sell" → ликвидирован ШОРТ (принудительная покупка → давление вверх)
    (см. официальную документацию Bybit V5 allLiquidation: поле S — это
    сторона ЛИКВИДИРОВАННОЙ ПОЗИЦИИ, а не сторона исполненного ордера)
    """
    events = _liq_events.get(sym)
    if not events:
        return None
    cutoff = time.time() - window_sec
    long_usd = short_usd = 0.0
    count = 0
    for ts, side, usd in events:
        if ts < cutoff:
            continue
        count += 1
        if side == "Buy":
            long_usd += usd
        else:
            short_usd += usd
    if count == 0:
        return None
    return {"long_usd": long_usd, "short_usd": short_usd, "total_usd": long_usd + short_usd, "count": count}


def format_liq_line(sym: str, window_sec: int = LIQ_WINDOW_SEC) -> str:
    """Готовая строка (с ведущим \\n) для сигнала, либо '' если данных нет."""
    info = get_liq_summary(sym, window_sec)
    if not info:
        return ""
    long_s, short_s = _fmt_usd(info["long_usd"]), _fmt_usd(info["short_usd"])
    if info["long_usd"] > info["short_usd"] * LIQ_DOMINANCE_RATIO:
        verdict = " — лонги доминируют 🔴"
    elif info["short_usd"] > info["long_usd"] * LIQ_DOMINANCE_RATIO:
        verdict = " — шорты доминируют 🟢"
    else:
        verdict = ""
    return f"\n🧨 Ликвидации {_fmt_dur(window_sec)}: лонги {long_s} │ шорты {short_s}{verdict}"


async def liquidation_listener():
    """
    Держит одно WS-соединение к Bybit (allLiquidation.*) и копит события в
    _liq_events. При смене списка символов (liq_symbols, обновляется из
    monitor()) — переподключается, чтобы синхронизировать подписки.
    Никогда не поднимает исключение наружу — любая ошибка лишь откладывает
    переподключение (экспоненциальный backoff, максимум 120с).
    """
    backoff = 5
    while True:
        try:
            if not liq_symbols or _session is None:
                await asyncio.sleep(5)
                continue

            subscribed_for = set(liq_symbols)
            async with _session.ws_connect(
                LIQ_WS_URL, heartbeat=20, timeout=aiohttp.ClientTimeout(total=None),
            ) as ws:
                log.info("Ликвидации: WS подключен, символов: %d", len(subscribed_for))
                subs = [f"allLiquidation.{s}" for s in sorted(subscribed_for)]
                for i in range(0, len(subs), LIQ_SUB_CHUNK):
                    chunk = subs[i:i + LIQ_SUB_CHUNK]
                    await ws.send_json({"op": "subscribe", "args": chunk})
                    await asyncio.sleep(0.15)
                backoff = 5
                resub_deadline = time.time() + 1800  # раз в 30 мин — ресинхронизация подписок

                async for msg in ws:
                    if msg.type == aiohttp.WSMsgType.TEXT:
                        try:
                            payload = json.loads(msg.data)
                        except Exception:
                            continue
                        topic = payload.get("topic", "")
                        if topic.startswith("allLiquidation."):
                            now = time.time()
                            for item in payload.get("data", []) or []:
                                try:
                                    sym   = item.get("s")
                                    side  = item.get("S")
                                    qty   = float(item.get("v", 0))
                                    price = float(item.get("p", 0))
                                except (TypeError, ValueError):
                                    continue
                                if not sym or side not in ("Buy", "Sell") or qty <= 0 or price <= 0:
                                    continue
                                _liq_events.setdefault(sym, []).append((now, side, qty * price))
                                _liq_prune(sym, now)
                    elif msg.type in (aiohttp.WSMsgType.CLOSED, aiohttp.WSMsgType.ERROR):
                        break

                    # Символы обновились (monitor() раз в 30 мин тянет новый список)
                    # или подошёл плановый ресаб — переподключаемся с новыми topics.
                    if time.time() >= resub_deadline or liq_symbols != subscribed_for:
                        break
        except asyncio.CancelledError:
            raise
        except Exception as e:
            log.warning("Ликвидации WS: %s (переподключение через %dс)", e, backoff)
            await asyncio.sleep(backoff)
            backoff = min(backoff * 2, 120)
            continue
        await asyncio.sleep(1)


def _rsi_signal_status_line(bold: bool = True) -> str:
    """
    Человекочитаемое описание текущего состояния RSI-фильтра роста/падения
    (вынесено в одну функцию, чтобы /menu, 🆘 Помощь и 📊 Статус не дублировали
    одну и ту же условную логику трижды).
    """
    if not RSI_SIGNAL_FILTER_ENABLED:
        return "<b>ВЫКЛЮЧЕН</b>" if bold else "ВЫКЛЮЧЕН"
    tf = RSI_TF_LABELS.get(RSI_SIGNAL_TF, RSI_SIGNAL_TF)
    if bold:
        return f"ТФ <b>{tf}</b>, порог ≥<b>{RSI_SIGNAL_LEVEL}</b>"
    return f"ТФ {tf}, порог ≥{RSI_SIGNAL_LEVEL}"


def _rsi_reversal_status_line(bold: bool = True) -> str:
    """То же самое, но для жёсткого RSI-гейта разворота (RSI_REVERSAL_TF / REVERSAL_RSI_OB)."""
    if not RSI_REVERSAL_FILTER_ENABLED:
        return "<b>ВЫКЛЮЧЕН</b>" if bold else "ВЫКЛЮЧЕН"
    tf = RSI_TF_LABELS.get(RSI_REVERSAL_TF, RSI_REVERSAL_TF)
    if bold:
        return f"ТФ <b>{tf}</b>, порог ≥<b>{REVERSAL_RSI_OB}</b>"
    return f"ТФ {tf}, порог ≥{REVERSAL_RSI_OB}"


def reply_keyboard():
    return {
        "keyboard": [
            # Строка 1 — Управление мониторингом
            ["⏸ Пауза", "▶️ Продолжить", "📊 Статус"],
            # Строка 2 — Порог роста/падения для алертов (включая 15%)
            ["📈 0.2%", "📈 5%", "📈 10%", "📈 15%", "📈 20%"],
            # Строка 3 — Временное окно
            ["⏱ 5 мин", "⏱ 1 час", "⏱ 4 ч", "⏱ 1 д"],
            # Строка 4 — Сигналы и история
            ["📋 История", "🏆 Топ-5", "📤 Экспорт"],
            # Строка 5 — Разворот + кулдауны
            ["🔄 Развороты", "⚙️ Настройки разворота", "🗑 Кулдауны"],
            # Строка 6 — Порог разворота (быстрая настройка, из 20 факторов)
            ["🎚 Порог 3/20", "🎚 Порог 5/20", "🎚 Порог 7/20"],
            ["🎚 Порог 9/20", "🎚 Порог 12/20", "🎚 Порог 15/20"],
            # Строка 7 — Процент роста для разворотных сигналов в шорт
            ["📉 Разворот 1%", "📉 Разворот 5%", "📉 Разворот 10%", "📉 Разворот 15%", "📉 Разворот 25%"],
            # Строка 8 — Период окна для расчёта роста разворота
            ["🕐 Разворот 5м", "🕐 Разворот 30м", "🕐 Разворот 1ч", "🕐 Разворот 4ч", "🕐 Разворот 1д"],
            # Строка 9 — Управление процессом
            ["🔄 Полный перезапуск", "🆘 Помощь"],
            # Строка 10 — RSI-фильтр обычных сигналов роста/падения: вкл/выкл
            ["🚦 RSI-фильтр ВЫКЛ", "🚦 RSI-фильтр ВКЛ"],
            # Строка 11 — RSI-фильтр обычных сигналов: таймфрейм
            ["📊 RSI ТФ 5м", "📊 RSI ТФ 15м", "📊 RSI ТФ 1ч", "📊 RSI ТФ 4ч", "📊 RSI ТФ 12ч", "📊 RSI ТФ 1д"],
            # Строка 12 — RSI-фильтр обычных сигналов: порог "не меньше"
            ["📊 RSI ≥50", "📊 RSI ≥60", "📊 RSI ≥70", "📊 RSI ≥80", "📊 RSI ≥90", "📊 RSI ≥95"],
            # Строка 13 — RSI разворота: таймфрейм (отдельно от сигналов роста/падения)
            ["🔄📊 RSI ТФ 5м", "🔄📊 RSI ТФ 15м", "🔄📊 RSI ТФ 1ч", "🔄📊 RSI ТФ 4ч", "🔄📊 RSI ТФ 12ч", "🔄📊 RSI ТФ 1д"],
            # Строка 14 — RSI разворота: порог "не меньше"
            ["🔄📊 RSI ≥50", "🔄📊 RSI ≥60", "🔄📊 RSI ≥70", "🔄📊 RSI ≥80", "🔄📊 RSI ≥90", "🔄📊 RSI ≥95"],
            # Строка 15 — RSI-гейт разворота: вкл/выкл (полностью отключает порог+ТФ)
            ["🔄🚦 RSI разворота ВЫКЛ", "🔄🚦 RSI разворота ВКЛ"],
            # Строка 16 — v25: скриншот графика (свечи) в сигналах: вкл/выкл
            ["🖼 График ВЫКЛ", "🖼 График ВКЛ"],
            # Строка 16.1 — v25: таймфрейм графика в сигналах
            ["🖼📊 ТФ 1м", "🖼📊 ТФ 15м", "🖼📊 ТФ 1ч", "🖼📊 ТФ 2ч", "🖼📊 ТФ 4ч", "🖼📊 ТФ 1д"],
            # Строка 17 — v25: регулировка порога роста/падения по изменению за
            # 24ч (как на Bybit): свой % от 1 до 500 + вкл/выкл самой регулировки
            ["📈 Свой % (24ч)", "📈🚦 24ч-регулировка ВЫКЛ", "📈🚦 24ч-регулировка ВКЛ"],
        ],
        "resize_keyboard": True,
        "persistent":      True,
    }


# ================================================================
#  ALERT FORMATTING
# ================================================================

def _fmt_dur(sec: int) -> str:
    if sec >= 3600:
        return f"{sec // 3600}ч {(sec % 3600) // 60}м"
    if sec >= 60:
        return f"{sec // 60}м"
    return f"{sec}с"


def alert_emoji(growth: float) -> str:
    a = abs(growth)
    if a >= 20:
        return "💥"
    if a >= 10:
        return "🔥"
    return "🚀" if growth > 0 else "📉"


def _btc_window_growth(now: float, window_sec: int) -> Optional[float]:
    """% изменение BTCUSDT за то же окно, что и у сигнала — для корреляции."""
    hist = price_history.get("BTCUSDT")
    if not hist:
        return None
    recent = [(t, p) for t, p in hist if now - t <= window_sec]
    if len(recent) < 2:
        return None
    old_p = recent[0][1]
    if old_p <= 0:
        return None
    return (recent[-1][1] - old_p) / old_p * 100


def calculate_cvd(klines_1m: dict, window_candles: int = CVD_WINDOW_CANDLES) -> Optional[dict]:
    """
    v25.5: приближённый CVD (Cumulative Volume Delta) по уже загруженным
    свечам Min1 — БЕЗ дополнительного HTTP-запроса. Точного тика "покупка
    маркетом / продажа маркетом" REST-эндпоинт Bybit не отдаёт, поэтому
    используется стандартное приближение: оборот свечи (turnover, USDT)
    целиком относится к покупателям, если свеча закрылась выше открытия
    (close > open), и к продавцам — если ниже. Этого достаточно, чтобы
    показать явный перекос спроса/предложения за последние N минут, не
    претендуя на точность настоящего agg-trades CVD.
    Возвращает {"delta_usd": ..., "buy_usd": ..., "sell_usd": ...} или None,
    если данных недостаточно.
    """
    opens     = (klines_1m or {}).get("opens") or []
    closes    = (klines_1m or {}).get("closes") or []
    turnovers = (klines_1m or {}).get("turnovers") or []
    n = min(len(opens), len(closes), len(turnovers))
    if n < 2:
        return None
    opens, closes, turnovers = opens[-window_candles:], closes[-window_candles:], turnovers[-window_candles:]
    buy_usd = sell_usd = 0.0
    for o, c, t in zip(opens, closes, turnovers):
        if c >= o:
            buy_usd += t
        else:
            sell_usd += t
    if buy_usd == 0 and sell_usd == 0:
        return None
    return {"delta_usd": buy_usd - sell_usd, "buy_usd": buy_usd, "sell_usd": sell_usd}


def format_cvd_line(klines_1m: dict, window_candles: int = CVD_WINDOW_CANDLES) -> str:
    """Готовая строка CVD (без ведущего \\n) или '' если посчитать нечего."""
    cvd = calculate_cvd(klines_1m, window_candles)
    if not cvd:
        return ""
    delta = cvd["delta_usd"]
    verdict = "покупатели доминируют 🟢" if delta > 0 else "продавцы доминируют 🔴" if delta < 0 else "баланс"
    return f"📊 CVD ({window_candles}м): <b>{_fmt_usd_signed(delta)}</b> — {verdict}"


def _fmt_usd_signed(val: float) -> str:
    sign = "+" if val >= 0 else "-"
    return f"{sign}{_fmt_usd(abs(val))}"


# ── v25.5: Long/Short Ratio (доля лонгов/шортов среди аккаунтов) ────────────
# Bybit отдаёт это ТОЛЬКО по одной монете за запрос (нет bulk-эндпоинта, в
# отличие от тикеров), поэтому запрашивается по требованию — только для
# монеты, по которой сработал сигнал, а не для всех монет каждый тик — и
# кэшируется на LSR_TTL_SEC, чтобы повторные срабатывания подряд по одной
# монете не долбили API лишний раз.
_lsr_cache: dict[str, dict] = {}


async def get_long_short_ratio(symbol: str, period: str = "5min") -> Optional[dict]:
    """
    GET /v5/market/account-ratio — доля аккаунтов в лонгах/шортах по монете.
    Возвращает {"buy_ratio": 0..1, "sell_ratio": 0..1} последней точки, либо
    None при недоступности данных (не критично — чисто информационная
    добавка, ничего не блокирует).
    """
    now    = time.time()
    cached = _lsr_cache.get(symbol)
    if cached and now - cached["ts"] < LSR_TTL_SEC:
        return cached["data"]
    if _bybit_block_active():
        return None
    result: Optional[dict] = None
    try:
        async with _get_bybit_sem():
            async with _session.get(
                "https://api.bybit.com/v5/market/account-ratio",
                params={"category": "linear", "symbol": symbol, "period": period, "limit": "1"},
                timeout=aiohttp.ClientTimeout(total=8),
            ) as r:
                status = r.status
                raw    = await r.text()
        if _looks_like_bybit_block(status, raw):
            _note_bybit_block(raw)
            return None
        data = json.loads(raw)
        if data.get("retCode") == 0:
            rows = data.get("result", {}).get("list", [])
            if rows:
                row = rows[0]
                buy_ratio  = float(row.get("buyRatio", 0) or 0)
                sell_ratio = float(row.get("sellRatio", 0) or 0)
                if buy_ratio > 0 or sell_ratio > 0:
                    result = {"buy_ratio": buy_ratio, "sell_ratio": sell_ratio}
    except Exception as e:
        log.debug("get_long_short_ratio %s: %s", symbol, e)
    # Кэшируем и отрицательный результат (None) тоже — иначе при постоянной
    # недоступности эндпоинта для проблемной монеты запрос повторялся бы
    # на КАЖДОЕ срабатывание сигнала, а не раз в LSR_TTL_SEC.
    _lsr_cache[symbol] = {"ts": now, "data": result}
    if len(_lsr_cache) > 2000:
        stale = [k for k, v in _lsr_cache.items() if now - v["ts"] > LSR_TTL_SEC * 10]
        for k in stale:
            _lsr_cache.pop(k, None)
    return result


def format_lsr_line(lsr: Optional[dict]) -> str:
    """Готовая строка Long/Short Ratio (без ведущего \\n) или '' если нет данных."""
    if not lsr:
        return ""
    buy_pct, sell_pct = lsr["buy_ratio"] * 100, lsr["sell_ratio"] * 100
    lean = "лонги преобладают" if buy_pct > sell_pct else "шорты преобладают" if sell_pct > buy_pct else "паритет"
    return f"⚖️ Лонг/Шорт: <b>{buy_pct:.1f}% / {sell_pct:.1f}%</b> — {lean}"


def format_spread_line(spread_info: Optional[tuple]) -> str:
    """
    spread_info — кортеж (спред_в_%, bid, ask) из get_prices().
    Готовая строка (без ведущего \\n) или '' если данных нет.
    Показывается только когда спред заметный (>= SPREAD_WIDE_PCT) — при
    обычном узком спреде строка не добавляет полезной информации к сигналу.
    """
    if not spread_info:
        return ""
    pct, bid, ask = spread_info
    if pct < SPREAD_WIDE_PCT:
        return ""
    return f"💧 Спред: <b>{pct:.3f}%</b> (bid {bid:g} / ask {ask:g}) — низкая ликвидность стакана"


def format_funding_line(
    funding_val: Optional[float],
    funding_next_ts: Optional[float],
    now: float,
) -> str:
    """
    v25.4: единая строка фандинга (ставки финансирования) для бессрочных
    фьючерсов Bybit — переиспользуется и в сигналах роста/падения/разворота
    (через build_extra_market_context), и в отдельном уведомлении
    "Изменение за 24ч", чтобы формат везде был идентичным.

    funding_val — ставка в % (уже умножена на 100 в get_prices, знак сохранён
    как есть у Bybit: положительная ставка → лонги платят шортам,
    отрицательная → шорты платят лонгам).
    funding_next_ts — время следующей выплаты фандинга (unix-время, секунды),
    используется только для строки "след. через Xч Yм"; если недоступно —
    просто не показываем этот фрагмент (не считаем ошибкой).

    Возвращает строку БЕЗ ведущего \\n (или '' если funding_val is None) —
    вызывающий код сам решает, добавлять ли перенос строки/добавлять в parts.
    """
    if funding_val is None:
        return ""
    if funding_val > FUNDING_NEUTRAL_EPS_PCT:
        hint = "лонги платят шортам"
    elif funding_val < -FUNDING_NEUTRAL_EPS_PCT:
        hint = "шорты платят лонгам"
    else:
        hint = "нейтрально"
    next_s = ""
    if funding_next_ts is not None:
        remain = funding_next_ts - now
        if remain > 0:
            h = int(remain // 3600)
            m = int((remain % 3600) // 60)
            next_s = f" · след. через {h}ч{m:02d}м"
    return f"💰 Фандинг: <b>{funding_val:+.4f}%</b> — {hint}{next_s}"


def build_extra_market_context(
    sym: str,
    growth: float,
    now: float,
    klines_1m: dict,
    klines_5m: dict,
    klines_15m: dict,
    oi_val: Optional[float] = None,
    oi_change_pct: Optional[float] = None,
    funding_val: Optional[float] = None,
    funding_next_ts: Optional[float] = None,
    spread_info: Optional[tuple] = None,
    lsr: Optional[dict] = None,
) -> str:
    """
    v25.1: единый блок доп. рыночных данных для сигналов роста/падения/
    разворота — мульти-ТФ подтверждение, корреляция с BTC, открытый
    интерес, ликвидации.
    v25.4: + фандинг (ставка финансирования, format_funding_line).
    v25.5: + спред bid/ask (format_spread_line, только если спред широкий),
    + CVD за последние N свечей Min1 (format_cvd_line, без доп. запроса),
    + Long/Short Ratio (format_lsr_line, запрашивается заранее и передаётся
    готовым значением — сам HTTP-запрос делает monitor(), а не эта функция,
    т.к. build_extra_market_context остаётся синхронной).
    Это ЧИСТО ИНФОРМАЦИОННЫЙ блок — ничего не фильтрует и не блокирует сами
    сигналы, только добавляет контекст.
    Возвращает строку с ведущим \\n (или '' если добавить нечего).
    """
    parts: list[str] = []

    # ── Мульти-ТФ подтверждение (по уже загруженным свечам, без доп. сети) ──
    def _tf_growth(k: dict) -> Optional[float]:
        c = (k or {}).get("closes") or []
        if len(c) < 2 or c[0] == 0:
            return None
        return (c[-1] - c[0]) / c[0] * 100

    g1, g5, g15 = _tf_growth(klines_1m), _tf_growth(klines_5m), _tf_growth(klines_15m)
    marks, confirmed, total_known = [], 0, 0
    for label, g in (("1м", g1), ("5м", g5), ("15м", g15)):
        if g is None:
            marks.append(f"{label}—")
            continue
        total_known += 1
        if (g > 0) == (growth > 0) and abs(g) > TF_CONFIRM_MIN_ABS_PCT:
            marks.append(f"{label}✅")
            confirmed += 1
        else:
            marks.append(f"{label}❌")
    if total_known:
        parts.append(f"🕐 Подтверждение ТФ: {' '.join(marks)} ({confirmed}/{total_known})")

    # ── Корреляция с BTC ─────────────────────────────────────────────────
    if sym != "BTCUSDT":
        btc_g = _btc_window_growth(now, current_window)
        if btc_g is not None:
            same_dir = (btc_g > 0) == (growth > 0)
            if abs(btc_g) >= BTC_CORR_SYNC_PCT and same_dir:
                parts.append(f"₿ BTC синхронно: <b>{btc_g:+.2f}%</b> — похоже на движение всего рынка")
            elif abs(btc_g) < BTC_CORR_FLAT_PCT:
                parts.append(f"₿ BTC почти не двигался ({btc_g:+.2f}%) — движение локальное, по монете")
            else:
                parts.append(f"₿ BTC: {btc_g:+.2f}% ({'та же сторона' if same_dir else 'в противофазе'})")

    # ── Открытый интерес (OI) ────────────────────────────────────────────
    if oi_val is not None:
        oi_s = f"📦 OI: <code>{oi_val:,.0f}$</code>".replace(",", " ")
        if oi_change_pct is not None and abs(oi_change_pct) >= OI_CHANGE_MIN_PCT:
            same_dir_oi = (oi_change_pct > 0) == (growth > 0)
            hint = (
                "подтверждает движение новыми позициями" if (same_dir_oi and oi_change_pct > 0)
                else "закрытие позиций / шорт-сквиз" if oi_change_pct < 0
                else "расхождение с ценой"
            )
            oi_s += f"  <b>{oi_change_pct:+.2f}%</b> — {hint}"
        parts.append(oi_s)

    # ── Фандинг (ставка финансирования) ─────────────────────────────────
    funding_line = format_funding_line(funding_val, funding_next_ts, now)
    if funding_line:
        parts.append(funding_line)

    # ── CVD (приближённая дельта объёма покупок/продаж, по Min1) ─────────
    cvd_line = format_cvd_line(klines_1m)
    if cvd_line:
        parts.append(cvd_line)

    # ── Long/Short Ratio (доля аккаунтов в лонгах/шортах) ────────────────
    lsr_line = format_lsr_line(lsr)
    if lsr_line:
        parts.append(lsr_line)

    # ── Спред bid/ask (только если заметно широкий — низкая ликвидность) ─
    spread_line = format_spread_line(spread_info)
    if spread_line:
        parts.append(spread_line)

    # ── Ликвидации ───────────────────────────────────────────────────────
    liq_line = format_liq_line(sym)
    if liq_line:
        parts.append(liq_line.lstrip("\n"))

    return ("\n" + "\n".join(parts)) if parts else ""


def format_pct24h_line(pct24h: Optional[float]) -> str:
    """
    v25.5: раньше эта же строка была продублирована один-в-один в
    format_growth_alert и format_drop_alert (и по смыслу в БЛОКе 1.6) —
    вынесена в общий хелпер. Возвращает строку С ведущим \\n (или '' если
    значения нет) — используется как есть внутри f-строк тех же форматов.
    """
    return f"\n🌐 24ч: <b>{pct24h:+.2f}%</b> (Bybit)" if pct24h is not None else ""


def _fmt_macd_value(macd: Optional[float]) -> str:
    """v25.5: общий хелпер — было продублировано в growth/drop алертах."""
    return f"{macd:+.6f}" if macd is not None else "—"


def _fmt_rsi_value(rsi: Optional[float]) -> str:
    """v25.5: общий хелпер — было продублировано в growth/drop алертах."""
    return f"{rsi:.1f}" if rsi is not None else "—"


def format_rsi_tf_line(
    rsi_tf_label: Optional[str],
    rsi_tf_val: Optional[float],
    rsi_tf_level: Optional[float] = None,
) -> str:
    """
    v25.5: общий хелпер для необязательной строки "📊 RSI <ТФ>: ..." в
    сигналах роста/падения — раньше был почти идентичный код в обеих
    функциях. rsi_tf_level=None (как для падения — порога не существует в
    принципе) просто не добавляет "(порог ≥...)". Возвращает строку С
    ведущим \\n (или '' если данных нет).
    """
    if not rsi_tf_label or rsi_tf_val is None:
        return ""
    lvl_s = f" (порог ≥{rsi_tf_level:g})" if rsi_tf_level is not None else ""
    return f"\n📊 RSI {rsi_tf_label}: <code>{rsi_tf_val:.1f}</code>{lvl_s}"


def format_growth_alert(
    sym: str, price: float, growth: float,
    rsi: Optional[float], macd: Optional[float], source: str,
    rsi_trend: Optional[str] = None,
    accel: Optional[float]   = None,
    duration_sec: int        = 0,
    breakout: Optional[str]  = None,
    day_context: Optional[str] = None,
    rsi_tf_label: Optional[str] = None,
    rsi_tf_val:   Optional[float] = None,
    rsi_tf_level: Optional[float] = None,
    pct24h: Optional[float] = None,
    extra: Optional[str] = None,
) -> str:
    emoji = alert_emoji(growth)
    sign  = "+" if growth > 0 else ""
    label = "Рост" if growth > 0 else "Падение"

    rsi_s    = _fmt_rsi_value(rsi)
    rsi_hint = (" ⚠️ перекуплен" if rsi is not None and rsi >= 70
                else " ⚠️ перепродан" if rsi is not None and rsi <= 30
                else "")
    rsi_t    = f" {rsi_trend}" if rsi_trend else ""
    macd_s   = _fmt_macd_value(macd)

    rsi_tf_s = format_rsi_tf_line(rsi_tf_label, rsi_tf_val, rsi_tf_level)

    accel_s = ""
    if accel is not None:
        if accel >= 2.0:
            accel_s = f"\n⚡ Ускорение: <b>×{accel:.1f}</b> 🔥"
        elif accel >= 1.3:
            accel_s = f"\n⚡ Ускорение: ×{accel:.1f}"
        elif accel < 0.7:
            accel_s = f"\n🐢 Замедление: ×{accel:.1f}"

    pct24h_s = format_pct24h_line(pct24h)

    head24_s = f" | 24ч: {pct24h:+.2f}%" if pct24h is not None else ""
    return (
        f"<b>{sym} {sign}{growth:.2f}%{head24_s}</b>\n"
        f"{emoji} <b>СИГНАЛ — {label.upper()}</b>\n\n"
        f"🪙 <b>{sym}</b>  [{source}]\n"
        f"💵 Цена: <code>{price}</code>\n"
        f"📈 {label}: <b>{sign}{growth:.2f}%</b> за {_fmt_dur(duration_sec)}\n"
        f"📊 RSI: <code>{rsi_s}</code>{rsi_t}{rsi_hint}\n"
        f"〽️ MACD: <code>{macd_s}</code>"
        f"{rsi_tf_s}"
        f"{accel_s}"
        f"{pct24h_s}"
        f"{extra or ''}"
        f"{chr(10) + breakout if breakout else ''}"
        f"{chr(10) + day_context if day_context else ''}"
        f"\n\n📋 <code>{sym}</code>"
    )


format_alert = format_growth_alert  # обратная совместимость


def format_drop_alert(
    sym: str, price: float, growth: float,
    rsi: Optional[float], macd: Optional[float], source: str,
    rsi_trend: Optional[str] = None,
    duration_sec: int        = 0,
    breakout: Optional[str]  = None,
    day_context: Optional[str] = None,
    rsi_tf_label: Optional[str] = None,
    rsi_tf_val:   Optional[float] = None,
    rsi_tf_level: Optional[float] = None,
    pct24h: Optional[float] = None,
    extra: Optional[str] = None,
) -> str:
    a     = abs(growth)
    emoji = "💥" if a >= 20 else "🔥" if a >= 10 else "📉"
    rsi_s = _fmt_rsi_value(rsi)
    hint  = " ⚠️ перепродан" if (rsi is not None and rsi <= 30) else ""
    rsi_t = f" {rsi_trend}" if rsi_trend else ""
    macd_s = _fmt_macd_value(macd)

    # Для падения порога RSI не существует в принципе (фильтр действует
    # только на рост) — показываем чистое значение без "порога", без
    # придуманной формулы "100 - уровень", которая раньше тут стояла и
    # никогда не отражала реальную логику фильтрации.
    rsi_tf_s = format_rsi_tf_line(rsi_tf_label, rsi_tf_val, rsi_tf_level=None)

    pct24h_s = format_pct24h_line(pct24h)

    head24_s = f" | 24ч: {pct24h:+.2f}%" if pct24h is not None else ""
    return (
        f"<b>{sym} {growth:.2f}%{head24_s}</b>\n"
        f"{emoji} <b>ПАДЕНИЕ</b>\n\n"
        f"🪙 <b>{sym}</b>  [{source}]\n"
        f"💵 Цена: <code>{price}</code>\n"
        f"📉 Падение: <b>{growth:.2f}%</b> за {_fmt_dur(duration_sec)}\n"
        f"📊 RSI: <code>{rsi_s}</code>{rsi_t}{hint}\n"
        f"〽️ MACD: <code>{macd_s}</code>"
        f"{rsi_tf_s}"
        f"{pct24h_s}"
        f"{extra or ''}"
        f"{chr(10) + breakout if breakout else ''}"
        f"{chr(10) + day_context if day_context else ''}"
        f"\n\n📋 <code>{sym}</code> #падение"
    )


def format_reversal_alert(
    sym:          str,
    price:        float,
    growth:       float,
    source:       str,
    rev:          dict,
    duration_sec: int = 0,
    window_sec:   int = 3600,
    pct24h:       Optional[float] = None,
    extra:        Optional[str] = None,
) -> str:
    score      = rev["score"]
    max_score  = rev.get("max_score", 20)
    factors    = rev["factors"]
    rsi        = rev.get("rsi")
    rsi_15m    = rev.get("rsi_15m")
    rsi15_lbl  = rev.get("rsi_15m_label", "15m")
    stoch      = rev.get("stoch_rsi")
    bb_pct     = rev.get("bb_pct")
    macd_h     = rev.get("macd_hist")
    atr        = rev.get("atr")
    target1    = rev.get("target1")
    target2    = rev.get("target2")
    candle     = rev.get("candle_pattern")
    ema_gap    = rev.get("ema_gap")
    wick_ratio = rev.get("wick_ratio")
    vol_sig    = rev.get("vol_signal", "")
    day_ctx    = rev.get("day_context", "")

    if score >= 12:
        confidence, hdr = "🔴 ВЫСОКАЯ", "🚨"
    elif score >= 7:
        confidence, hdr = "🟠 СРЕДНЯЯ", "⚠️"
    else:
        confidence, hdr = "🟡 СЛАБАЯ", "🔄"

    rsi_s    = f"{rsi:.1f}"       if rsi        is not None else "—"
    rsi15_s  = f"{rsi_15m:.1f}"   if rsi_15m    is not None else "—"
    stoch_s  = f"{stoch:.2f}"     if stoch      is not None else "—"
    bb_s     = f"{bb_pct:.2f}"    if bb_pct     is not None else "—"
    macd_s   = f"{macd_h:+.6f}"   if macd_h     is not None else "—"
    atr_s    = f"{atr:.6f}"       if atr        is not None else "—"
    ema_s    = f"{ema_gap:+.2f}%" if ema_gap    is not None else "—"
    wick_s   = f"{wick_ratio:.2f}" if wick_ratio is not None else "—"

    factors_s = "\n".join(f"  {i+1}. {_esc(f)}" for i, f in enumerate(factors)) if factors else "  —"

    t1_s      = f"<code>{target1:.4g}</code>" if target1 else "—"
    t2_s      = f"<code>{target2:.4g}</code>" if target2 else "—"
    fib       = rev.get("fib", {})
    t3_s      = f"<code>{fib['fib_500']:.4g}</code>" if fib.get("fib_500") else "—"

    candle_s  = f"\n🕯 Паттерн: <b>{candle}</b>" if candle  else ""
    vol_s     = f"\n{_esc(vol_sig)}"              if vol_sig else ""
    day_ctx_s = f"\n{day_ctx}"                    if day_ctx else ""
    pct24h_s  = format_pct24h_line(pct24h)

    head24_s = f" | 24ч: {pct24h:+.2f}%" if pct24h is not None else ""
    return (
        f"<b>{sym} +{growth:.2f}%{head24_s}</b>\n"
        f"{hdr} <b>РАЗВОРОТ НА ШОРТ</b>\n"
        f"━━━━━━━━━━━━━━━━━━━━\n"
        f"🪙 <b>{sym}</b>  [{source}]\n"
        f"💵 Цена: <code>{price}</code>\n"
        f"📈 Рост до разворота: <b>+{growth:.2f}%</b> за {_fmt_dur(window_sec)}"
        f"{pct24h_s}\n\n"
        f"🎯 Уверенность: {confidence}  [{score}/{max_score}]\n\n"
        f"<b>Сработавшие факторы ({score}/{max_score}):</b>\n{factors_s}\n\n"
        f"<b>Индикаторы:</b>\n"
        f"  RSI 1m: <code>{rsi_s}</code>  │  RSI {rsi15_lbl}: <code>{rsi15_s}</code> (порог &gt;{REVERSAL_RSI_OB})\n"
        f"  StochRSI: <code>{stoch_s}</code>  │  BB%: <code>{bb_s}</code>\n"
        f"  MACD: <code>{macd_s}</code>  │  ATR: <code>{atr_s}</code>\n"
        f"  EMA gap: <code>{ema_s}</code>  │  Wick: <code>{wick_s}</code>"
        f"{candle_s}"
        f"{vol_s}"
        f"{day_ctx_s}"
        f"{extra or ''}\n\n"
        f"<b>🎯 Цели шорта (Фибо):</b>\n"
        f"  Цель 1 (38.2%): {t1_s}\n"
        f"  Цель 2 (50.0%): {t3_s}\n"
        f"  Цель 3 (61.8%): {t2_s}"
    )


# ================================================================
#  SYMBOLS
# ================================================================

async def get_symbols() -> set[str]:
    """
    Список всех активных бессрочных USDT-фьючерсов (linear perpetual)
    напрямую с официального Bybit V5 API, с пагинацией через cursor
    (на одной странице Bybit отдаёт максимум 1000 инструментов).

    v25.5: каждая страница запрашивается через _bybit_get_with_retry (до 3
    попыток с backoff) — кратковременный сетевой сбой на одной из страниц
    больше не обнуляет весь список монет, накопленный на предыдущих страницах.
    """
    symbols: set[str] = set()
    try:
        cursor = ""
        while True:
            params = {"category": "linear", "limit": "1000"}
            if cursor:
                params["cursor"] = cursor
            status, raw = await _bybit_get_with_retry(
                "https://api.bybit.com/v5/market/instruments-info",
                params=params,
                timeout_sec=20,
            )
            if status != 200:
                if _bybit_block_active():
                    log.debug("get_symbols: пропущено — активна блокировка Bybit ещё %.0fс", _bybit_block_remaining())
                else:
                    log.error("get_symbols: HTTP %s (после ретраев) — останавливаемся на уже собранных %d монетах", status, len(symbols))
                break
            try:
                data = json.loads(raw)
            except Exception as je:
                log.error("get_symbols: не удалось разобрать JSON (%s)", je)
                break
            if data.get("retCode") != 0:
                log.error("get_symbols: retCode=%s msg=%s", data.get("retCode"), data.get("retMsg"))
                break
            result = data.get("result", {})
            for item in result.get("list", []):
                if (
                    item.get("quoteCoin") == "USDT"
                    and item.get("contractType") == "LinearPerpetual"
                    and item.get("status") == "Trading"
                ):
                    symbols.add(item["symbol"])
            cursor = result.get("nextPageCursor") or ""
            if not cursor:
                break
    except Exception as e:
        log.error("get_symbols: %s", e)
    log.info("Всего монет (Bybit бессрочные фьючерсы): %d", len(symbols))
    return symbols


# ================================================================
#  PRICES
# ================================================================

async def _bybit_get_with_retry(
    url: str,
    params: dict,
    retries: int = 3,
    backoff_base: float = 1.0,
    timeout_sec: float = 10,
) -> tuple[Optional[int], str]:
    """
    v25.5: обёртка над _session.get с ретраями и экспоненциальным backoff.
    Используется для "общих" запросов, критичных для целого тика мониторинга
    (тикеры сразу по всем монетам, список символов) — кратковременный сбой
    сети/rate-limit не должен приводить к полному пропуску всего тика.
    Per-symbol запросы (klines, RSI и т.п.) сюда намеренно не переведены —
    там сбой одной монеты не критичен и уже штатно обрабатывается через
    try/except с fallback на пустые данные, без задержки всего цикла.

    Возвращает (status, raw_text). Если все попытки упали сетевым
    исключением (не HTTP-статусом) — status=None, raw_text="".
    v25.7: если активна общая блокировка Bybit (_bybit_block_active) —
    не делает НИ ОДНОГО запроса, сразу возвращает (None, ""). Если ответ
    сам оказался блокировкой ("Access too frequent") — не тратит оставшиеся
    попытки, сразу выставляет cool-down (_note_bybit_block) и возвращает
    результат как есть.
    """
    if _bybit_block_active():
        return None, ""
    last_exc: Optional[Exception] = None
    status: Optional[int] = None
    raw = ""
    for attempt in range(1, retries + 1):
        try:
            async with _session.get(
                url, params=params, timeout=aiohttp.ClientTimeout(total=timeout_sec),
            ) as r:
                status = r.status
                raw    = await r.text()
            if status == 200:
                return status, raw
            if _looks_like_bybit_block(status, raw):
                _note_bybit_block(raw)
                return status, raw
            if attempt == retries:
                return status, raw
            log.warning(
                "%s: HTTP %s (попытка %d/%d) — повтор через %.1fс",
                url, status, attempt, retries, backoff_base * attempt,
            )
        except Exception as e:
            last_exc = e
            if attempt == retries:
                break
            log.warning(
                "%s: %s (попытка %d/%d) — повтор через %.1fс",
                url, e, attempt, retries, backoff_base * attempt,
            )
        await asyncio.sleep(backoff_base * attempt)
    if last_exc is not None:
        log.error("%s: не удалось выполнить запрос за %d попыток: %s", url, retries, last_exc)
        return None, ""
    return status, raw


async def get_prices(symbols: set[str]) -> tuple[dict, dict, dict, dict, dict, dict]:
    """
    Текущие цены сразу по всем бессрочным USDT-фьючерсам Bybit —
    один HTTP-запрос вместо отдельных вызовов на каждую монету.

    v25: дополнительно разбирает поле price24hPcnt из того же самого ответа
    тикера — официальное изменение цены за 24ч, как показывается на самом
    Bybit.
    v25.1: дополнительно разбирает поле openInterestValue (открытый интерес
    в USDT) — тоже приходит "бесплатно" в том же ответе тикера, без единого
    лишнего HTTP-запроса.
    v25.4: дополнительно разбирает поля fundingRate и nextFundingTime —
    ставка финансирования (фандинг) для бессрочных фьючерсов, тоже приходит
    в том же ответе тикера, без единого лишнего HTTP-запроса. fundingRate
    от Bybit приходит долей (например "0.0001" = 0.01%), поэтому переводим
    в проценты умножением на 100 — точно так же, как уже сделано для
    price24hPcnt. nextFundingTime приходит как строка миллисекунд unix-эпохи
    — переводим в секунды (float), чтобы дальше было удобно сравнивать с
    time.time().
    v25.5: дополнительно разбирает bid1Price/ask1Price (лучшие цены в
    стакане) из того же тикера — тоже без доп. запроса — и считает спред в %
    от средней цены. Сам HTTP-запрос теперь идёт через
    _bybit_get_with_retry (до 3 попыток с backoff) — кратковременный сетевой
    сбой больше не приводит к полному пропуску тика мониторинга.
    """
    prices:  dict = {}
    sources: dict = {}
    pct24h:  dict = {}
    oi:      dict = {}
    funding: dict = {}   # {symbol: (ставка_в_%, nextFundingTime_в_секундах_или_None)}
    spread:  dict = {}   # {symbol: (спред_в_%, bid, ask)}
    try:
        status, raw = await _bybit_get_with_retry(
            "https://api.bybit.com/v5/market/tickers",
            params={"category": "linear"},
        )
        if status != 200:
            if _bybit_block_active():
                log.debug(
                    "get_prices: пропущено — активна блокировка Bybit ещё %.0fс",
                    _bybit_block_remaining(),
                )
            else:
                log.error(
                    "get_prices: HTTP %s от Bybit (не 200, после ретраев). Тело ответа (первые 500 симв.): %s",
                    status, raw[:500].replace("\n", " "),
                )
            return prices, sources, pct24h, oi, funding, spread
        try:
            data = json.loads(raw)
        except Exception as je:
            log.error(
                "get_prices: не удалось разобрать JSON (%s). Тело ответа (первые 500 симв.): %s",
                je, raw[:500].replace("\n", " "),
            )
            return prices, sources, pct24h, oi, funding, spread

        if data.get("retCode") == 0:
            for item in data.get("result", {}).get("list", []):
                sym = item.get("symbol")
                if sym not in symbols:
                    continue
                try:
                    price = float(item.get("lastPrice") or 0)
                except (TypeError, ValueError):
                    continue
                if price > 0:
                    prices[sym]  = price
                    sources[sym] = "Bybit"
                try:
                    raw_pcnt = item.get("price24hPcnt")
                    if raw_pcnt not in (None, ""):
                        pct24h[sym] = float(raw_pcnt) * 100.0
                except (TypeError, ValueError):
                    pass
                try:
                    raw_oi = item.get("openInterestValue")
                    if raw_oi not in (None, ""):
                        oi[sym] = float(raw_oi)
                except (TypeError, ValueError):
                    pass
                try:
                    raw_funding = item.get("fundingRate")
                    if raw_funding not in (None, ""):
                        funding_pct = float(raw_funding) * 100.0
                        raw_next = item.get("nextFundingTime")
                        next_ts = None
                        if raw_next not in (None, "", "0"):
                            try:
                                next_ts = float(raw_next) / 1000.0
                            except (TypeError, ValueError):
                                next_ts = None
                        funding[sym] = (funding_pct, next_ts)
                except (TypeError, ValueError):
                    pass
                try:
                    raw_bid = item.get("bid1Price")
                    raw_ask = item.get("ask1Price")
                    if raw_bid not in (None, "") and raw_ask not in (None, ""):
                        bid, ask = float(raw_bid), float(raw_ask)
                        mid = (bid + ask) / 2
                        if bid > 0 and ask > 0 and mid > 0:
                            spread[sym] = ((ask - bid) / mid * 100.0, bid, ask)
                except (TypeError, ValueError):
                    pass
        else:
            # v25.3: раньше тут писалось только "retCode=None msg=None", что
            # никак не объясняло причину — теперь дополнительно печатаем сырое
            # тело ответа (обрезанное), чтобы было видно РЕАЛЬНУЮ причину:
            # гео-блокировка IP хостинга Bybit'ом (частое явление на
            # Railway/Heroku/AWS/GCP), rate-limit, обслуживающая страница и т.п.
            log.error(
                "get_prices: retCode=%s msg=%s | тело ответа (первые 500 симв.): %s",
                data.get("retCode"), data.get("retMsg"), raw[:500].replace("\n", " "),
            )
    except Exception as e:
        log.error("get_prices: %s", e)
    return prices, sources, pct24h, oi, funding, spread



# ================================================================
#  MONITOR
# ================================================================

async def monitor():
    global current_percent, current_window, signals_count, reversal_count, checks_count, last_check_time
    global REVERSAL_GROWTH_MIN_PCT
    global liq_symbols

    symbols          = await get_symbols()
    liq_symbols       = set(symbols)   # v25.1: список для подписки на ликвидации (WS)
    last_symbols_upd = time.time()
    _cache_load_levels()

    if _state_restore_source:
        _settings_restored_note = f"♻️ Настройки восстановлены из {_state_restore_source}."
    else:
        _settings_restored_note = "ℹ️ Ни резервной копии в Telegram, ни state.json не найдено — используются настройки по умолчанию из config.py."
    await broadcast(
        f"✅ <b>Бот запущен</b> (v25.8)\n{_settings_restored_note}"
    )

    while True:
        if monitor_paused:
            await asyncio.sleep(2)
            continue

        try:
            now             = time.time()
            checks_count   += 1
            last_check_time = now

            if PROM_AVAILABLE:
                PROM_CHECKS.inc()
                PROM_COINS.set(len(price_history))

            if now - last_symbols_upd >= 1800:
                new_sym = await get_symbols()
                if new_sym:
                    symbols = new_sym
                    liq_symbols = set(symbols)   # v25.1: ресинхронизация подписок на ликвидации
                    log.info("Монеты обновлены: %d", len(symbols))
                last_symbols_upd = now

            prices, sources, pct24h_map, oi_map, funding_map, spread_map = await get_prices(symbols)
            funding_cache.update(funding_map)

            for sym, price in prices.items():
                await asyncio.sleep(0)

                if price <= 0:
                    continue

                hist = price_history.setdefault(sym, [])
                hist.append((now, price))
                cutoff = max(current_window * 2, 86400)
                price_history[sym] = [(t, p) for t, p in hist if now - t <= cutoff]

                recent = [(t, p) for t, p in price_history[sym] if now - t <= current_window]
                source = sources.get(sym, "UNKNOWN")
                pct24h_val = pct24h_map.get(sym)   # v25: изменение за 24ч как на Bybit (может быть None)

                # v25.1: открытый интерес (OI) — трекается для ВСЕХ монет каждый тик,
                # аналогично price_history, чтобы % изменения был доступен для любого
                # сигнала, который сработает по этой монете (без доп. HTTP-запросов —
                # значение уже пришло в этом же тикере get_prices).
                oi_val = oi_map.get(sym)
                oi_change_pct = None
                if oi_val is not None:
                    oi_hist = oi_history.setdefault(sym, [])
                    oi_hist.append((now, oi_val))
                    oi_history[sym] = [(t, v) for t, v in oi_hist if now - t <= cutoff]
                    oi_recent = [(t, v) for t, v in oi_history[sym] if now - t <= current_window]
                    if len(oi_recent) >= 2 and oi_recent[0][1] > 0:
                        oi_change_pct = (oi_val - oi_recent[0][1]) / oi_recent[0][1] * 100

                # v25.4: фандинг — как и pct24h, чисто информационное значение
                # из уже полученного тикера (без доп. HTTP-запроса).
                funding_info = funding_map.get(sym)
                funding_val, funding_next_ts = funding_info if funding_info else (None, None)

                # v25.5: спред bid/ask — тоже из уже полученного тикера.
                spread_info = spread_map.get(sym)

                if len(recent) < MIN_SAMPLES:
                    continue

                old_price = recent[0][1]
                if old_price <= 0:
                    continue

                growth    = (price - old_price) / old_price * 100
                direction = 1 if growth > 0 else -1

                # ── Пиковый рост за окно разворота — чистая локальная арифметика
                # по уже накопленной истории цен, БЕЗ единого HTTP-запроса. Нужно
                # посчитать ДО фильтра ниже, а не внутри БЛОКА 1, как было раньше —
                # именно на этом значении строится сам фильтр.
                peak_growth = get_peak_growth(sym, price, REVERSAL_WINDOW_SEC)

                # ══════════════════════════════════════════════════════════════
                #  РАННИЙ ФИЛЬТР ПЕРЕД klines/RSI (v25.7)
                # ══════════════════════════════════════════════════════════════
                # РАНЬШЕ klines_multi (3 HTTP-запроса: Min1+Min5+Min15) запрашивался
                # для КАЖДОЙ монеты с достаточной историей на КАЖДОМ тике — то есть
                # для всех нескольких сотен бессрочных фьючерсов Bybit каждые
                # config.INTERVAL секунд, даже если монета вообще не приближается ни
                # к одному порогу и заведомо не даст ни одного уведомления в этом
                # тике. Именно это было главной причиной HTTP 403 "Access too
                # frequent" от Bybit — не отдельные новые фичи (funding/CVD/спред и
                # т.п. и так уже брались бесплатно из тикера get_prices), а именно
                # этот безусловный веер из сотен×3 запросов на klines.
                #
                # Теперь считаем ТРИ независимых условия, при которых монета вообще
                # МОЖЕТ дать хоть одно уведомление в этом тике — используя только
                # то, что уже посчитано без сети (growth, peak_growth, pct24h_val,
                # кулдауны). Если ни одно не выполняется — дальше по этой монете в
                # этом тике делать нечего, пропускаем БЕЗ единого запроса klines.
                # Сами условия — точная копия проверок внутри БЛОКОВ 1/1.6/2 ниже
                # (включая кулдауны), поэтому набор реально отправляемых уведомлений
                # не меняется ни на одно сообщение — меняется только то, что для
                # заведомо неинтересных монет мы не тратим на них сеть и CPU.
                _last_rev_ts_g = _reversal_cooldown.get(sym, 0)
                needs_reversal = (
                    (peak_growth >= REVERSAL_GROWTH_MIN_PCT or growth >= REVERSAL_GROWTH_MIN_PCT)
                    and now - _last_rev_ts_g >= REVERSAL_COOLDOWN_SEC
                )
                _last_p24_g = _pct24h_notify_cooldown.get(sym, 0)
                needs_pct24h = (
                    PCT24H_GATE_ENABLED and pct24h_val is not None
                    and abs(pct24h_val) >= CUSTOM_PCT_24H
                    and now - _last_p24_g >= PCT24H_NOTIFY_COOLDOWN_SEC
                )
                needs_growth_drop = abs(growth) >= current_percent

                if not (needs_reversal or needs_pct24h or needs_growth_drop):
                    continue

                # ── Свечи Bybit: 3 таймфрейма параллельно ─────────────────────
                # (сюда доходят только монеты, прошедшие фильтр выше)
                klines_all  = await get_bybit_klines_multi(sym)
                klines_1m   = klines_all["klines_1m"]
                klines_5m   = klines_all["klines_5m"]
                klines_15m  = klines_all["klines_15m"]
                closes      = klines_1m["closes"]

                rsi = calculate_rsi(closes) if closes else None
                if rsi is None:
                    vals = [p for _, p in price_history[sym][-120:]]
                    rsi  = calculate_rsi(vals)

                # ════════════════════════════════════════════════════════════
                #  БЛОК 1: РАЗВОРОТ НА ШОРТ
                #  Условие: пиковый рост за REVERSAL_WINDOW_SEC >= REVERSAL_GROWTH_MIN_PCT
                #  Фильтр дублей: кулдаун + смена цены + рост скора
                # ════════════════════════════════════════════════════════════
                if peak_growth >= REVERSAL_GROWTH_MIN_PCT or growth >= REVERSAL_GROWTH_MIN_PCT:
                    last_rev_ts = _reversal_cooldown.get(sym, 0)
                    if now - last_rev_ts >= REVERSAL_COOLDOWN_SEC:
                        # ── Жёсткий гейт по RSI разворота (кнопки 🔄📊 / 🔄🚦) ──
                        # Полностью НЕЗАВИСИМ от 20-факторной системы score:
                        # разворотный сигнал в принципе не рассматривается, пока
                        # RSI на выбранном таймфрейме не достигнет порога (>=).
                        # Это доп. условие, а не замена факторам — сами factors
                        # по-прежнему считаются и влияют на confidence/REVERSAL_MIN_SCORE.
                        # Если гейт выключен кнопкой 🔄🚦 — пропускаем сам HTTP-запрос
                        # за RSI на RSI_REVERSAL_TF (экономия сети) и не блокируем
                        # сигнал; фактор 15 внутри detect_short_reversal при этом не
                        # исчезает — падает на встроенный fallback (RSI по Min15 из
                        # уже загруженных klines_15m, без доп. сети).
                        if RSI_REVERSAL_FILTER_ENABLED:
                            rsi_reversal_val = await get_rsi_for_tf(sym, RSI_REVERSAL_TF)
                            rsi_reversal_gate = (
                                rsi_reversal_val is not None
                                and rsi_reversal_val >= REVERSAL_RSI_OB
                            )
                        else:
                            rsi_reversal_val = None
                            rsi_reversal_gate = True

                        rev = detect_short_reversal(
                            sym, price, klines_1m, klines_5m, klines_15m,
                            recent, growth, rsi, REVERSAL_MIN_SCORE,
                            rsi_reversal_tf=rsi_reversal_val,
                            rsi_reversal_tf_label=RSI_TF_LABELS.get(RSI_REVERSAL_TF, RSI_REVERSAL_TF),
                        ) if rsi_reversal_gate else {"triggered": False}

                        if rsi_reversal_gate and rev["triggered"]:
                            # ── Умный фильтр повторов ───────────────────────
                            last_info = _reversal_last.get(sym)
                            if last_info:
                                price_change = abs(price - last_info["price"]) / last_info["price"] * 100
                                score_delta  = rev["score"] - last_info["score"]
                                # Пропускаем если: цена почти не изменилась И скор не вырос значительно
                                if (price_change < REVERSAL_REPEAT_PRICE_PCT
                                        and score_delta < REVERSAL_REPEAT_SCORE_DELTA):
                                    log.debug(
                                        "REVERSAL skip %s: price_chg=%.2f%% score_delta=%d",
                                        sym, price_change, score_delta,
                                    )
                                    continue
                            # ── Отправляем сигнал ───────────────────────────
                            duration = calculate_growth_duration(recent)
                            # v25.5: Long/Short Ratio запрашивается только сейчас, при
                            # реальном срабатывании сигнала (не на каждый тик по каждой
                            # монете) — экономит запросы и не задерживает основной цикл.
                            lsr_info = await get_long_short_ratio(sym)
                            walls_line_rev = await format_walls_alert_line(sym, price)
                            rev_text = format_reversal_alert(
                                sym, price, max(growth, peak_growth), source, rev,
                                duration_sec=duration,
                                window_sec=REVERSAL_WINDOW_SEC,
                                pct24h=pct24h_val,
                                extra=build_extra_market_context(
                                    sym, max(growth, peak_growth), now,
                                    klines_1m, klines_5m, klines_15m,
                                    oi_val=oi_val, oi_change_pct=oi_change_pct,
                                    funding_val=funding_val, funding_next_ts=funding_next_ts,
                                    spread_info=spread_info, lsr=lsr_info,
                                ) + walls_line_rev,
                            )
                            _reversal_cooldown[sym] = now
                            _reversal_last[sym] = {"price": price, "score": rev["score"], "ts": now}
                            db_save_reversal(
                                sym, price, rev["score"], rev["factors"],
                                rev["rsi"], rev["macd_hist"],
                                rev["stoch_rsi"], rev["bb_pct"],
                                rev["atr"], rev["target1"], rev["target2"], source,
                            )
                            await broadcast_with_chart(sym, rev_text)
                            reversal_count += 1
                            if PROM_AVAILABLE:
                                PROM_REVERSALS.inc()
                            log.info("REVERSAL %s score=%d/20 peak=%.2f%% price_chg=%.2f%%",
                                     sym, rev["score"], peak_growth,
                                     abs(price - last_info["price"]) / last_info["price"] * 100 if last_info else 0.0)
                            continue  # не дублируем обычным алертом

                # ════════════════════════════════════════════════════════════
                #  БЛОК 1.6: ОТДЕЛЬНОЕ УВЕДОМЛЕНИЕ ОБ ИЗМЕНЕНИИ ЗА 24Ч (как на Bybit)
                # ════════════════════════════════════════════════════════════
                # v25.2: полностью самостоятельный тип уведомления. Кнопки
                # 📈 Свой % (24ч) и 📈🚦 24ч-регулировка ВЫКЛ/ВКЛ управляют ТОЛЬКО
                # этим блоком — не влияют на обычные сигналы роста/падения
                # (БЛОК 2) и не влияют на разворот (БЛОК 1). В сигналах
                # роста/падения/разворота строка "🌐 24ч: ..." остаётся как
                # была — чисто информационная, независимо от этой регулировки.
                # Отправляется через broadcast_with_chart — как и остальные
                # сигналы, прикладывает скриншот графика (если 🖼 включён).
                if PCT24H_GATE_ENABLED and pct24h_val is not None and abs(pct24h_val) >= CUSTOM_PCT_24H:
                    last_p24 = _pct24h_notify_cooldown.get(sym, 0)
                    if now - last_p24 >= PCT24H_NOTIFY_COOLDOWN_SEC:
                        p24_emoji = "🚀" if pct24h_val > 0 else "📉"
                        p24_label = "РОСТ" if pct24h_val > 0 else "ПАДЕНИЕ"
                        rsi_str24 = f"{rsi:.1f}" if rsi is not None else "—"
                        # v25.4/v25.5: те же информационные строки, что и в остальных
                        # уведомлениях — ничего не фильтруют, просто единый формат.
                        funding_line24 = format_funding_line(funding_val, funding_next_ts, now)
                        cvd_line24     = format_cvd_line(klines_1m)
                        lsr_line24     = format_lsr_line(await get_long_short_ratio(sym))
                        spread_line24  = format_spread_line(spread_info)
                        extra24 = "".join(
                            f"\n{line}" for line in (funding_line24, cvd_line24, lsr_line24, spread_line24) if line
                        )
                        extra24 += await format_walls_alert_line(sym, price)
                        p24_text = (
                            f"🌐 <b>ИЗМЕНЕНИЕ ЗА 24Ч — {p24_label}</b> (как на Bybit)\n\n"
                            f"🪙 <b>{sym}</b>  [{source}]\n"
                            f"💵 Цена: <code>{price}</code>\n"
                            f"{p24_emoji} 24ч: <b>{pct24h_val:+.2f}%</b>  "
                            f"(порог {CUSTOM_PCT_24H}%)\n"
                            f"📊 RSI: <code>{rsi_str24}</code>"
                            f"{extra24}\n\n"
                            f"📋 <code>{sym}</code> #24ч"
                        )
                        _pct24h_notify_cooldown[sym] = now
                        await broadcast_with_chart(sym, p24_text)
                        log.info("24h notify: %s %+.2f%% (порог %.1f%%)", sym, pct24h_val, CUSTOM_PCT_24H)

                # ════════════════════════════════════════════════════════════
                #  БЛОК 2: ОБЫЧНЫЕ АЛЕРТЫ (РОСТ / ПАДЕНИЕ)
                # ════════════════════════════════════════════════════════════
                # v25.2: обычные сигналы роста/падения снова фильтруются ТОЛЬКО
                # внутренним порогом current_percent/current_window — так же,
                # как было до v25. Регулировка по 24ч (кнопки 📈🚦) на эти
                # сигналы больше не влияет — см. БЛОК 1.6 выше.
                active_pct_threshold = current_percent

                if abs(growth) < active_pct_threshold:
                    level = _cache_get_level(sym)
                    if level and level["direction"] != direction:
                        _cache_clear_level(sym)
                    continue

                # RSI-фильтр направления (настраиваемый ТФ и порог через кнопки,
                # можно полностью отключить кнопкой 🚦 RSI-фильтр ВЫКЛ).
                # РОСТ:    уведомление приходит только если RSI >= RSI_SIGNAL_LEVEL.
                # ПАДЕНИЕ: порог никогда не блокирует сигнал — значение RSI лишь
                #          показывается в тексте сообщения.
                # Когда фильтр выключен — пропускаем сам HTTP-запрос за RSI на
                # отдельном ТФ (если RSI_SIGNAL_TF != Min1), а не только проверку.
                rsi_filter_val = None
                if RSI_SIGNAL_FILTER_ENABLED:
                    rsi_filter_val = rsi if RSI_SIGNAL_TF == "Min1" else await get_rsi_for_tf(sym, RSI_SIGNAL_TF)
                    if direction == 1 and rsi_filter_val is not None and rsi_filter_val < RSI_SIGNAL_LEVEL:
                        continue

                last_sent = _alert_cooldown.get(sym, 0)
                if now - last_sent < ALERT_COOLDOWN_SEC:
                    continue

                level = _cache_get_level(sym)
                if level:
                    prev_price = level["alert_price"]
                    prev_dir   = level["direction"]
                    if prev_dir == direction:
                        if abs(price - prev_price) / prev_price * 100 < active_pct_threshold:
                            continue

                vals      = [p for _, p in price_history[sym][-120:]]
                macd_data = calculate_macd_full(vals)
                macd      = macd_data["histogram"]
                # RSI-тренд из уже загруженных свечей — не делаем лишний HTTP запрос
                rsi_trend = calculate_rsi_trend(closes) if len(closes) >= 17 else None
                accel     = calculate_acceleration(recent)
                duration  = calculate_growth_duration(recent)
                breakout  = check_24h_breakout(sym, price)
                day_ctx   = get_24h_context(sym, price)

                rsi_tf_label_show = (
                    RSI_TF_LABELS.get(RSI_SIGNAL_TF)
                    if RSI_SIGNAL_FILTER_ENABLED and RSI_SIGNAL_TF != "Min1"
                    else None
                )
                # v25.5: Long/Short Ratio — только сейчас, при реальном срабатывании
                # обычного сигнала роста/падения (не на каждый тик по каждой монете).
                lsr_info = await get_long_short_ratio(sym)
                walls_line = await format_walls_alert_line(sym, price)
                if direction == 1:
                    text = format_growth_alert(
                        sym, price, growth, rsi, macd, source,
                        rsi_trend=rsi_trend, accel=accel,
                        duration_sec=duration, breakout=breakout, day_context=day_ctx,
                        rsi_tf_label=rsi_tf_label_show, rsi_tf_val=rsi_filter_val,
                        rsi_tf_level=RSI_SIGNAL_LEVEL if rsi_tf_label_show else None,
                        pct24h=pct24h_val,
                        extra=build_extra_market_context(
                            sym, growth, now, klines_1m, klines_5m, klines_15m,
                            oi_val=oi_val, oi_change_pct=oi_change_pct,
                            funding_val=funding_val, funding_next_ts=funding_next_ts,
                            spread_info=spread_info, lsr=lsr_info,
                        ) + walls_line,
                    )
                else:
                    text = format_drop_alert(
                        sym, price, growth, rsi, macd, source,
                        rsi_trend=rsi_trend, duration_sec=duration,
                        breakout=breakout, day_context=day_ctx,
                        rsi_tf_label=rsi_tf_label_show, rsi_tf_val=rsi_filter_val,
                        rsi_tf_level=None,  # порог не применяется к падению — только значение RSI
                        pct24h=pct24h_val,
                        extra=build_extra_market_context(
                            sym, growth, now, klines_1m, klines_5m, klines_15m,
                            oi_val=oi_val, oi_change_pct=oi_change_pct,
                            funding_val=funding_val, funding_next_ts=funding_next_ts,
                            spread_info=spread_info, lsr=lsr_info,
                        ) + walls_line,
                    )

                _cache_set_level(sym, price, direction)
                _alert_cooldown[sym] = now
                db_save_alert(sym, price, growth, rsi, macd, source)
                await broadcast_with_chart(sym, text)
                signals_count += 1

                if PROM_AVAILABLE:
                    PROM_SIGNALS.inc()

                log.info("Signal: %s %+.2f%% [%s]", sym, growth, source)

            await asyncio.sleep(config.INTERVAL)

            # ── Периодическая очистка памяти ──────────────────────────────────
            if checks_count % 200 == 0:
                # Удаляем устаревшие кулдауны
                stale_ts = now - max(ALERT_COOLDOWN_SEC, REVERSAL_COOLDOWN_SEC) * 3
                for d in (_alert_cooldown, _reversal_cooldown, _pct24h_notify_cooldown):
                    stale = [k for k, v in d.items() if v < stale_ts]
                    for k in stale:
                        d.pop(k, None)
            if checks_count % 1000 == 0:
                # Удаляем монеты без свежих данных
                cutoff2 = now - max(current_window * 4, 86400 * 2)
                dead = [s for s, h in price_history.items() if not h or h[-1][0] < cutoff2]
                for s in dead:
                    price_history.pop(s, None)
                    oi_history.pop(s, None)         # v25.1
                    _liq_events.pop(s, None)        # v25.1
                    funding_cache.pop(s, None)       # v25.4
                if dead:
                    log.info("price_history: удалено %d мёртвых монет", len(dead))

        except asyncio.CancelledError:
            raise
        except Exception as e:
            log.exception("monitor error: %s", e)
            await asyncio.sleep(5)


# ================================================================
#  COMMAND HANDLERS
# ================================================================

async def handle_message(msg: dict):
    """
    Тонкая обёртка: после КАЖДОГО обработанного сообщения (независимо от
    того, какая кнопка/команда сработала и через какой early-return внутри
    _handle_message_impl это произошло) сохраняет все настройки в state.json.
    Так любое изменение через кнопку сохраняется сразу же, а не только раз в
    30с из save_state_loop — и переживает перезапуск/сбой/редеплой.
    """
    try:
        await _handle_message_impl(msg)
    finally:
        save_state()


async def _handle_message_impl(msg: dict):
    global current_percent, current_window, monitor_paused
    global REVERSAL_MIN_SCORE, REVERSAL_RSI_OB, REVERSAL_STOCH_OB, REVERSAL_STOCH_EXT
    global REVERSAL_BB_OB, REVERSAL_MACD_SLOPE, REVERSAL_ACCEL, REVERSAL_HIGH_MARGIN
    global REVERSAL_MOMENTUM, REVERSAL_COOLDOWN_SEC, REVERSAL_ATR_MULT, REVERSAL_VOL_RATIO
    global REVERSAL_GROWTH_MIN_PCT, REVERSAL_WINDOW_SEC
    global REVERSAL_REPEAT_PRICE_PCT, REVERSAL_REPEAT_SCORE_DELTA
    global RSI_SIGNAL_TF, RSI_SIGNAL_LEVEL, RSI_REVERSAL_TF, RSI_SIGNAL_FILTER_ENABLED
    global RSI_REVERSAL_FILTER_ENABLED
    global CHART_ENABLED, CHART_TF, CUSTOM_PCT_24H, PCT24H_GATE_ENABLED, _awaiting_input

    text    = msg.get("text", "").strip()
    chat_id = msg["chat"]["id"]

    if chat_id != int(config.CHAT_ID):
        if text in ("/start", "/subscribe"):
            db_add_subscriber(chat_id)
            await send_message(
                "🚀 <b>Crypto Alert Bot v25</b>\n\n"
                "✅ Вы подписаны на сигналы.\n"
                "Для отписки: /unsubscribe",
                chat_id,
            )
        elif text == "/unsubscribe":
            db_remove_subscriber(chat_id)
            await send_message("❌ Вы отписались от сигналов", chat_id)
        else:
            await send_message("⛔ Нет доступа к управлению. /subscribe — подписаться на алерты.", chat_id)
        return

    # v25.2: режим ожидания ручного ввода числа после кнопки 📈 Свой % (24ч).
    # Проверяется в первую очередь, чтобы обычное сообщение с числом (без
    # команды) не попало мимо цели ни в один другой обработчик ниже.
    if _awaiting_input == "custom_pct_24h":
        _awaiting_input = None
        try:
            val = float(text.replace(",", ".").replace("%", "").strip())
            assert 1.0 <= val <= 500.0
            CUSTOM_PCT_24H = val
            status_hint = (
                f"Уведомление об изменении за 24ч <b>включено</b> — придёт отдельным "
                f"сообщением, как только 24ч-изменение цены (как на Bybit) по какой-то "
                f"монете превысит этот порог."
                if PCT24H_GATE_ENABLED else
                f"⚠️ Уведомление об изменении за 24ч сейчас <b>выключено</b> (кнопка 📈🚦) "
                f"— порог сохранён, но пока не применяется."
            )
            await send_message(
                f"✅ Порог отдельного уведомления «Изменение за 24ч»: <b>{CUSTOM_PCT_24H}%</b>\n"
                f"{status_hint}\n\n"
                f"На сигналы роста/падения/разворота это НЕ влияет — там строка "
                f"«🌐 24ч: ...» просто информационная.",
                chat_id,
            )
        except Exception:
            await send_message("❌ Введите число от 1 до 500, например: 12.5", chat_id)
        return

    if text == "📈 Свой % (24ч)":
        _awaiting_input = "custom_pct_24h"
        await send_message(
            f"✏️ Введите порог отдельного уведомления «Изменение за 24ч», число от "
            f"<b>1</b> до <b>500</b> (например: 12.5).\nТекущий: <b>{CUSTOM_PCT_24H}%</b>\n\n"
            f"На обычные сигналы роста/падения/разворота порог не влияет.",
            chat_id,
        )
        return

    if text.startswith("/set_pct24h"):
        try:
            val = float(text.split()[1].replace(",", "."))
            assert 1.0 <= val <= 500.0
            CUSTOM_PCT_24H = val
            await send_message(
                f"✅ Порог отдельного уведомления «Изменение за 24ч»: <b>{CUSTOM_PCT_24H}%</b>",
                chat_id,
            )
        except Exception:
            await send_message(f"❌ /set_pct24h 12.5  (число от 1 до 500)\nТекущее: {CUSTOM_PCT_24H}%", chat_id)
        return

    if text.startswith("/funding"):
        # v25.5: раньше funding_cache только писался (для строки в сигналах), но
        # нигде не читался по требованию — теперь можно посмотреть текущую ставку
        # по любой отслеживаемой монете вручную, не дожидаясь сигнала по ней.
        parts_cmd = text.split()
        if len(parts_cmd) < 2:
            await send_message("❌ Использование: /funding BTCUSDT", chat_id)
            return
        sym_q = parts_cmd[1].upper()
        if not sym_q.endswith("USDT"):
            sym_q += "USDT"
        cached = funding_cache.get(sym_q)
        if not cached:
            await send_message(
                f"❌ Нет данных по фандингу для <b>{sym_q}</b> — либо такой монеты нет "
                f"среди отслеживаемых бессрочных USDT-фьючерсов Bybit, либо ещё не было "
                f"ни одного цикла опроса после запуска бота (подождите ~{config.INTERVAL}с).",
                chat_id,
            )
            return
        f_val, f_next = cached
        line = format_funding_line(f_val, f_next, time.time())
        await send_message(f"💰 <b>{sym_q}</b>\n{line}", chat_id)
        return

    if text.startswith("/walls"):
        # v25.12: отдельный график — 5 ближайших к текущей цене крупных
        # лимитных заявок (стен) с каждой стороны стакана. Логика отбора
        # "это реально стена" — та же, что и на обычном свечном графике.
        parts_cmd = text.split()
        if len(parts_cmd) < 2:
            await send_message("❌ Использование: /walls BTCUSDT", chat_id)
            return
        sym_q = parts_cmd[1].upper()
        if not sym_q.endswith("USDT"):
            sym_q += "USDT"
        try:
            png = await generate_orderbook_wall_chart(sym_q)
        except Exception as e:
            log.warning("/walls %s: %s", sym_q, e)
            png = None
        if not png:
            await send_message(
                f"❌ Сейчас нет крупных лимитных заявок (≥ ${WALL_MIN_USD:,.0f}, минимум "
                f"в {WALL_MIN_REL_MULT:g}× больше медианной заявки в стакане) по "
                f"<b>{sym_q}</b>, либо стакан недоступен.".replace(",", " "),
                chat_id,
            )
            return
        await send_photo(chat_id, png)
        return

    if text == "📈🚦 24ч-регулировка ВЫКЛ":
        PCT24H_GATE_ENABLED = False
        await send_message(
            "🚫 Отдельное уведомление «Изменение за 24ч» <b>выключено</b> — присылаться "
            "не будет. На сигналы роста/падения/разворота это не влияет — они как "
            "фильтровались обычным порогом (<b>current_percent</b>), так и фильтруются, "
            "строка «🌐 24ч: ...» в них как показывалась, так и продолжает показываться.",
            chat_id,
        )
        return

    if text == "📈🚦 24ч-регулировка ВКЛ":
        PCT24H_GATE_ENABLED = True
        await send_message(
            f"✅ Отдельное уведомление «Изменение за 24ч» <b>включено</b> — придёт, как "
            f"только 24ч-изменение цены (как на Bybit, поле price24hPcnt) по какой-то "
            f"монете превысит <b>{CUSTOM_PCT_24H}%</b>. На обычные сигналы "
            f"роста/падения/разворота это не влияет.",
            chat_id,
        )
        return

    if text == "🖼 График ВЫКЛ":
        CHART_ENABLED = False
        await send_message(
            "🚫 Скриншот графика в сигналах <b>выключен</b> — сигналы роста, падения "
            "и разворота теперь приходят только текстом.",
            chat_id,
        )
        return

    if text == "🖼 График ВКЛ":
        CHART_ENABLED = True
        await send_message(
            f"✅ Скриншот графика в сигналах <b>включён</b> — свечи "
            f"{CHART_TF_LABELS.get(CHART_TF, CHART_TF)}, как на Bybit, "
            f"будут прикладываться к сигналам роста, падения и разворота.",
            chat_id,
        )
        return

    _chart_tf_map = {
        "🖼📊 ТФ 1м":  "Min1",
        "🖼📊 ТФ 15м": "Min15",
        "🖼📊 ТФ 1ч":  "Min60",
        "🖼📊 ТФ 2ч":  "Hour2",
        "🖼📊 ТФ 4ч":  "Hour4",
        "🖼📊 ТФ 1д":  "Day1",
    }
    if text in _chart_tf_map:
        CHART_TF = _chart_tf_map[text]
        off_hint = "\n⚠️ График сейчас ВЫКЛЮЧЕН (🖼) — ТФ сохранён, применится, когда включите." \
            if not CHART_ENABLED else ""
        await send_message(
            f"✅ Таймфрейм графика в сигналах: <b>{CHART_TF_LABELS.get(CHART_TF, CHART_TF)}</b>"
            f"{off_hint}",
            chat_id,
        )
        return

    if text in ("/start", "/menu"):
        await send_message(
            f"🚀 <b>Crypto Alert Bot v25</b>\n\n"
            f"📈 Порог роста: <b>{current_percent}%</b>\n"
            f"⏱ Период алертов: <b>{current_window // 60} мин</b>\n"
            f"🔄 Порог разворота: <b>{REVERSAL_MIN_SCORE}/20 факторов</b>\n"
            f"📉 Рост для разворота: <b>{REVERSAL_GROWTH_MIN_PCT}%</b> за <b>{_fmt_dur(REVERSAL_WINDOW_SEC)}</b>\n"
            f"📊 RSI-фильтр РОСТА: {_rsi_signal_status_line()} (падение — без порога, только значение)\n"
            f"🔄📊 RSI-гейт разворота: {_rsi_reversal_status_line()} (не зависит от 20 факторов)\n"
            f"🖼 График в сигналах: {'<b>ВКЛЮЧЕН</b>' if CHART_ENABLED else '<b>ВЫКЛЮЧЕН</b>'} "
            f"({CHART_TF_LABELS.get(CHART_TF, CHART_TF)}, как на Bybit)\n"
            f"🌐 Уведомление «24ч» (отдельное): {'<b>ВКЛЮЧЕНО</b>' if PCT24H_GATE_ENABLED else '<b>ВЫКЛЮЧЕНО</b>'} "
            f"— порог <b>{CUSTOM_PCT_24H}%</b> (не влияет на сигналы роста/падения/разворота)\n"
            f"🧨 Ликвидации WS: {'<b>подключены</b>' if _liq_events else '<b>ещё копят данные…</b>'}\n"
            f"{'⏸ Пауза активна' if monitor_paused else '▶️ Мониторинг активен'}\n\n"
            f"ℹ️ В сигналы роста/падения/разворота теперь автоматически добавляются: "
            f"мульти-ТФ подтверждение (1м/5м/15м), корреляция с BTC, открытый интерес "
            f"и ликвидации — это просто доп. данные в тексте сигнала, без новых кнопок.\n\n"
            f"<b>Кнопки управления:</b>\n"
            f"  📈 0.2%/5%/10%/15%/20% — порог роста/падения алертов\n"
            f"  📉 Разворот 1-25% — минимальный рост для шорт-разворота\n"
            f"  🕐 Разворот 5м-1д — период расчёта роста разворота\n"
            f"  ⏱ 5 мин/1 час/4 ч/1 д — период окна алертов\n"
            f"  🎚 Порог 3-12/20 — чувствительность (кол-во факторов)\n"
            f"  🚦 RSI-фильтр ВЫКЛ/ВКЛ — полностью отключить/включить проверку "
            f"RSI (и её ТФ) для сигналов роста/падения\n"
            f"  📊 RSI ТФ 5м-1д — таймфрейм RSI для сигнала РОСТА\n"
            f"  📊 RSI ≥50-95 — порог RSI: уведомление о РОСТЕ только если RSI ≥ порога. "
            f"На ПАДЕНИЕ порог не действует — RSI просто показывается в сообщении\n"
            f"  🔄🚦 RSI разворота ВЫКЛ/ВКЛ — полностью отключить/включить "
            f"жёсткий RSI-гейт разворота (порог и его ТФ)\n"
            f"  🔄📊 RSI ТФ 5м-1д — таймфрейм RSI-гейта разворота\n"
            f"  🔄📊 RSI ≥50-95 — порог RSI-гейта разворота: уведомление о развороте "
            f"приходит, только если RSI на выбранном ТФ ≥ порога — НЕЗАВИСИМО от "
            f"набранных факторов разворота\n"
            f"  🖼 График ВЫКЛ/ВКЛ — прикладывать ли скриншот свечного графика "
            f"{CHART_TF_LABELS.get(CHART_TF, CHART_TF)} (как на Bybit) к сигналам роста/падения/разворота\n"
            f"  🖼📊 ТФ 1м/15м/1ч/2ч/4ч/1д — таймфрейм графика, который прикладывается к сигналам\n"
            f"  📈 Свой % (24ч) — порог 1–500% для ОТДЕЛЬНОГО уведомления «Изменение "
            f"за 24ч» (не влияет на сигналы роста/падения/разворота)\n"
            f"  📈🚦 24ч-регулировка ВЫКЛ/ВКЛ — вкл/выкл этого отдельного уведомления; "
            f"строка «🌐 24ч: ...» в сигналах роста/падения/разворота показывается "
            f"всегда, независимо от этой кнопки\n"
            f"  ⚙️ Настройки разворота — все параметры\n"
            f"  🔄 Полный перезапуск — перезапуск без потери данных\n"
            f"  🆘 Помощь — краткая справка\n\n"
            f"<b>Команды:</b>\n"
            f"  /set_percent 2.5 — порог алертов (%)\n"
            f"  /set_window 60 — период алертов (мин)\n"
            f"  /set_pct24h 12.5 — порог регулировки по 24ч (1–500%)\n"
            f"  /rev_score 5 — порог факторов разворота\n"
            f"  /rev_growth 5 — % роста для разворота\n"
            f"  /rev_window 60 — период роста разворота (мин)\n"
            f"  /rev_cooldown 5 — кулдаун разворота (мин)\n"
            f"  /restart — перезапуск мониторинга",
            chat_id,
            reply_markup=reply_keyboard(),
        )
        return

    _pct_map = {
        "📈 0.2%": 0.2, "📈 5%": 5.0, "📈 10%": 10.0, "📈 15%": 15.0, "📈 20%": 20.0,
    }
    if text in _pct_map:
        current_percent = _pct_map[text]
        await send_message(f"✅ Порог роста/падения: <b>{current_percent}%</b>", chat_id)
        return

    _win_map = {
        "⏱ 5 мин": (300,   "5 мин"),
        "⏱ 1 час": (3600,  "1 час"),
        "⏱ 4 ч":   (14400, "4 часа"),
        "⏱ 1 д":   (86400, "1 день"),
    }
    if text in _win_map:
        current_window, label = _win_map[text]
        await send_message(f"✅ Период: <b>{label}</b>", chat_id)
        return

    if text == "⏸ Пауза":
        monitor_paused = True
        await send_message("⏸ Мониторинг приостановлен", chat_id)
        return

    if text == "▶️ Продолжить":
        monitor_paused = False
        await send_message("▶️ Мониторинг возобновлён", chat_id)
        return

    if text in ("🆘 Помощь", "/help"):
        await send_message(
            f"🚀 <b>Crypto Alert Bot v25 — справка</b>\n\n"
            f"📈 Порог роста: <b>{current_percent}%</b>\n"
            f"⏱ Период алертов: <b>{current_window // 60} мин</b>\n"
            f"🔄 Порог разворота: <b>{REVERSAL_MIN_SCORE}/20 факторов</b>\n"
            f"📊 RSI РОСТА: {_rsi_signal_status_line()} (падение — без порога)\n"
            f"🔄📊 RSI-гейт разворота: {_rsi_reversal_status_line()} (вне факторов)\n"
            f"🖼 График в сигналах: {'ВКЛЮЧЕН' if CHART_ENABLED else 'ВЫКЛЮЧЕН'}\n"
            f"🌐 Уведомление «24ч» (отдельное): {'ВКЛЮЧЕНО' if PCT24H_GATE_ENABLED else 'ВЫКЛЮЧЕНО'} "
            f"(порог {CUSTOM_PCT_24H}%, на сигналы роста/падения/разворота не влияет)\n\n"
            f"<b>Кнопки управления:</b>\n"
            f"  📈 0.2%/5%/10%/15%/20% — порог роста/падения алертов\n"
            f"  📉 Разворот 1-25% — мин. рост для шорт-разворота\n"
            f"  🕐 Разворот 5м-1д — период расчёта роста разворота\n"
            f"  ⏱ 5 мин/1 час/4 ч/1 д — период окна алертов\n"
            f"  🎚 Порог 3-12/20 — чувствительность разворота\n"
            f"  🚦 RSI-фильтр ВЫКЛ/ВКЛ — отключить/включить RSI-проверку роста/падения\n"
            f"  📊 RSI ТФ / RSI ≥ — таймфрейм и порог RSI для РОСТА (на падение не влияет)\n"
            f"  🔄🚦 RSI разворота ВЫКЛ/ВКЛ — отключить/включить жёсткий RSI-гейт разворота\n"
            f"  🔄📊 RSI ТФ / RSI ≥ — таймфрейм и порог жёсткого RSI-гейта разворота\n"
            f"  🖼 График ВЫКЛ/ВКЛ — скриншот свечного графика (как на Bybit) в сигналах\n"
            f"  🖼📊 ТФ 1м/15м/1ч/2ч/4ч/1д — таймфрейм графика в сигналах\n"
            f"  📈 Свой % (24ч) — порог 1–500% ОТДЕЛЬНОГО уведомления «Изменение за 24ч»\n"
            f"  📈🚦 24ч-регулировка ВЫКЛ/ВКЛ — вкл/выкл этого уведомления; на сигналы "
            f"роста/падения/разворота (и строку 🌐24ч в них) не влияет\n"
            f"  ⚙️ Настройки разворота — все параметры детально\n"
            f"  🔄 Полный перезапуск — перезапуск процесса без потери данных\n\n"
            f"<b>Доп. команды:</b>\n"
            f"  /funding BTCUSDT — текущая ставка фандинга по монете вручную\n"
            f"  /walls BTCUSDT — 5 ближайших крупных лимитных заявок в стакане с ценами\n\n"
            f"Полный список команд: /menu",
            chat_id,
        )
        return

    if text in ("🔄 Полный перезапуск", "/restart"):
        last = _restart_cooldown.get(chat_id, 0)
        if time.time() - last < RESTART_COOLDOWN_SEC:
            wait_left = int(RESTART_COOLDOWN_SEC - (time.time() - last))
            await send_message(f"⏳ Подождите {wait_left}с перед повторным перезапуском", chat_id)
            return
        _restart_cooldown[chat_id] = time.time()
        await send_message(
            "🔄 <b>Перезапуск мониторинга...</b>\n"
            "ℹ️ История цен, БД и настройки сохраняются — перезапускается только процесс анализа.",
            chat_id,
        )
        asyncio.create_task(_cmd_full_restart(chat_id))
        return

    if text in ("📊 Статус", "/status"):
        uptime = int(time.time() - start_time)
        d, r   = divmod(uptime, 86400)
        h, r   = divmod(r, 3600)
        m      = r // 60
        await send_message(
            f"📊 <b>СТАТУС</b>\n\n"
            f"🟢 Аптайм: {d}д {h}ч {m}м\n"
            f"🪙 Монет в истории: {len(price_history)}\n"
            f"🔔 Сигналов: {signals_count}\n"
            f"🔄 Разворотов: {reversal_count}\n"
            f"🔄 Порог разворота: {REVERSAL_MIN_SCORE}/20\n"
            f"📉 Рост для разворота: {REVERSAL_GROWTH_MIN_PCT}% за {_fmt_dur(REVERSAL_WINDOW_SEC)}\n"
            f"📊 RSI сигналов: {_rsi_signal_status_line(bold=False)}\n"
            f"🔄📊 RSI разворота: {_rsi_reversal_status_line(bold=False)}\n"
            f"🔄 Кулдаун разворота: {REVERSAL_COOLDOWN_SEC // 60} мин\n"
            f"📈 Порог роста: {current_percent}%\n"
            f"⏱ Период: {current_window // 60} мин\n"
            f"🖼 График в сигналах: {'ВКЛЮЧЕН' if CHART_ENABLED else 'ВЫКЛЮЧЕН'} "
            f"({CHART_TF_LABELS.get(CHART_TF, CHART_TF)})\n"
            f"🌐 Уведомление «24ч»: {'ВКЛЮЧЕНО' if PCT24H_GATE_ENABLED else 'ВЫКЛЮЧЕНО'} "
            f"(порог {CUSTOM_PCT_24H}%)\n"
            f"⚡ Интервал: {config.INTERVAL} сек\n"
            f"👥 Подписчиков: {len(db_get_subscribers())}\n"
            f"{'⏸ Пауза' if monitor_paused else '▶️ Активен'}",
            chat_id,
        )
        return

    if text in ("📋 История", "/history"):
        asyncio.create_task(_cmd_history(chat_id))
        return

    if text in ("🏆 Топ-5", "/top5"):
        asyncio.create_task(_cmd_top5(chat_id))
        return

    if text in ("📤 Экспорт", "/export"):
        asyncio.create_task(_cmd_export(chat_id))
        return

    if text in ("🗑 Кулдауны", "/clear_cooldowns"):
        _cache_clear_all()
        _reversal_cooldown.clear()
        _reversal_last.clear()
        await send_message("🗑 Кулдауны, уровни и история разворотов сброшены", chat_id)
        return

    if text in ("🔄 Развороты", "/reversals"):
        asyncio.create_task(_cmd_reversals(chat_id))
        return

    if text in ("⚙️ Настройки разворота", "⚙️ Разворот", "/reversal_settings"):
        await _cmd_reversal_settings(chat_id)
        return

    if text in ("🗄 БД Статистика", "/db_stats"):
        s = db_stats()
        await send_message(
            f"🗄 <b>База данных</b>\n\n"
            f"📋 Алертов: {s['alerts']}\n"
            f"📅 Старейший: {s['oldest_alert']}\n"
            f"🪙 Уровней: {s['levels']}\n"
            f"🔄 Разворотов: {s['reversals']}\n"
            f"💾 Размер: {s['size_mb']:.2f} МБ",
            chat_id,
        )
        return

    if text in ("🧹 БД Очистка", "/db_cleanup"):
        deleted = db_cleanup()
        total   = sum(deleted.values())
        await send_message(
            f"🧹 <b>Очистка завершена</b>\n\n"
            f"📋 Алертов: -{deleted.get('alerts', 0)}\n"
            f"📍 Уровней: -{deleted.get('alert_levels', 0)}\n"
            f"📊 price_stats: -{deleted.get('price_stats', 0)}\n"
            f"🔄 Разворотов: -{deleted.get('reversal_signals', 0)}\n"
            f"────────────────\n"
            f"🗑 Итого: {total} строк\n"
            f"💾 Размер: {db_size_mb():.2f} МБ",
            chat_id,
        )
        return

    # Кнопки % роста для разворотных сигналов в шорт
    _rev_growth_map = {
        "📉 Разворот 1%":  1.0,
        "📉 Разворот 5%":  5.0,
        "📉 Разворот 10%": 10.0,
        "📉 Разворот 15%": 15.0,
        "📉 Разворот 25%": 25.0,
    }
    if text in _rev_growth_map:
        REVERSAL_GROWTH_MIN_PCT = _rev_growth_map[text]
        await send_message(
            f"✅ Порог роста для разворота в шорт: <b>{REVERSAL_GROWTH_MIN_PCT}%</b>\n"
            f"ℹ️ Разворотный детектор сработает если рост ≥ {REVERSAL_GROWTH_MIN_PCT}%",
            chat_id,
        )
        return

    if text.startswith("/rev_growth"):
        try:
            val = float(text.split()[1])
            assert 0.1 <= val <= 100.0
            REVERSAL_GROWTH_MIN_PCT = val
            await send_message(
                f"✅ Порог роста для разворота: <b>{val}%</b>", chat_id
            )
        except Exception:
            await send_message(
                f"❌ /rev_growth 5   (текущее: {REVERSAL_GROWTH_MIN_PCT}%)\n"
                f"Диапазон: 0.1–100", chat_id
            )
        return

    # Кнопки периода окна для расчёта роста разворота
    _rev_window_map = {
        "🕐 Разворот 5м":  (300,   "5 минут"),
        "🕐 Разворот 30м": (1800,  "30 минут"),
        "🕐 Разворот 1ч":  (3600,  "1 час"),
        "🕐 Разворот 4ч":  (14400, "4 часа"),
        "🕐 Разворот 1д":  (86400, "1 день"),
    }
    if text in _rev_window_map:
        REVERSAL_WINDOW_SEC, label = _rev_window_map[text]
        await send_message(
            f"✅ Окно роста для разворота: <b>{label}</b>\n"
            f"ℹ️ Разворотный детектор смотрит рост монеты за последние <b>{label}</b>.\n"
            f"Порог: <b>{REVERSAL_GROWTH_MIN_PCT}%</b> за этот период.",
            chat_id,
        )
        return

    if text.startswith("/rev_window"):
        try:
            val = int(text.split()[1])
            assert 1 <= val <= 10080
            REVERSAL_WINDOW_SEC = val * 60
            await send_message(
                f"✅ Окно разворота: <b>{val} мин</b> ({_fmt_dur(REVERSAL_WINDOW_SEC)})", chat_id
            )
        except Exception:
            cur = _fmt_dur(REVERSAL_WINDOW_SEC)
            await send_message(
                f"❌ /rev_window 60   (в минутах, 1–10080)\nТекущее: {cur}", chat_id
            )
        return

    if text.startswith("/rev_price_pct"):
        try:
            val = float(text.split()[1])
            assert 0.1 <= val <= 20.0
            REVERSAL_REPEAT_PRICE_PCT = val
            await send_message(
                f"✅ Мин. смена цены между разворотами: <b>{val}%</b>\n"
                f"ℹ️ Повторный сигнал по монете только если цена изменилась на ≥{val}%",
                chat_id,
            )
        except Exception:
            await send_message(
                f"❌ /rev_price_pct 3   (текущее: {REVERSAL_REPEAT_PRICE_PCT}%)\nДиапазон: 0.1–20",
                chat_id,
            )
        return

    if text.startswith("/rev_score_delta"):
        try:
            val = int(text.split()[1])
            assert 0 <= val <= 20
            REVERSAL_REPEAT_SCORE_DELTA = val
            await send_message(
                f"✅ Мин. прирост скора между разворотами: <b>+{val}</b>\n"
                f"ℹ️ Повторный сигнал только если скор вырос на ≥{val} факторов",
                chat_id,
            )
        except Exception:
            await send_message(
                f"❌ /rev_score_delta 2   (текущее: {REVERSAL_REPEAT_SCORE_DELTA})\nДиапазон: 0–20",
                chat_id,
            )
        return

    # Быстрая настройка порога разворота через кнопки (из 20 факторов)
    _rev_score_map = {
        "🎚 Порог 3/20":  3,
        "🎚 Порог 5/20":  5,
        "🎚 Порог 7/20":  7,
        "🎚 Порог 9/20":  9,
        "🎚 Порог 12/20": 12,
        "🎚 Порог 15/20": 15,
    }
    if text in _rev_score_map:
        REVERSAL_MIN_SCORE = _rev_score_map[text]  # global объявлен выше — OK
        await send_message(
            f"✅ Порог разворота: <b>{REVERSAL_MIN_SCORE}/20 факторов</b>\n"
            f"ℹ️ Агрессивный: 3, Мягкий: 5, Стандарт: 7, Строгий: 9, "
            f"Очень строгий: 12, Экстремальный: 15",
            chat_id,
        )
        return

    # ── RSI-фильтр обычных сигналов роста/падения: вкл/выкл ────────────────────
    if text == "🚦 RSI-фильтр ВЫКЛ":
        RSI_SIGNAL_FILTER_ENABLED = False
        await send_message(
            "✅ RSI-фильтр РОСТА/ПАДЕНИЯ: <b>ВЫКЛЮЧЕН</b>\n"
            "ℹ️ Алерты теперь идут только по порогу % (кнопки 📈) — RSI и его ТФ "
            "больше не проверяются и не запрашиваются (доп. запрос к бирже за RSI "
            "пропускается, если ТФ не Min1).",
            chat_id,
        )
        return

    if text == "🚦 RSI-фильтр ВКЛ":
        RSI_SIGNAL_FILTER_ENABLED = True
        await send_message(
            f"✅ RSI-фильтр РОСТА/ПАДЕНИЯ: <b>ВКЛЮЧЕН</b>\n"
            f"ℹ️ {_rsi_signal_status_line()} (действует только на сигналы РОСТА; "
            f"на ПАДЕНИЕ порог не влияет — RSI лишь показывается в сообщении).",
            chat_id,
        )
        return

    # ── RSI-фильтр обычных сигналов роста/падения: таймфрейм ──────────────────
    _rsi_signal_tf_map = {
        "📊 RSI ТФ 5м":  "Min5",
        "📊 RSI ТФ 15м": "Min15",
        "📊 RSI ТФ 1ч":  "Min60",
        "📊 RSI ТФ 4ч":  "Hour4",
        "📊 RSI ТФ 12ч": "Hour12",
        "📊 RSI ТФ 1д":  "Day1",
    }
    if text in _rsi_signal_tf_map:
        RSI_SIGNAL_TF = _rsi_signal_tf_map[text]
        lbl = RSI_TF_LABELS.get(RSI_SIGNAL_TF, RSI_SIGNAL_TF)
        off_hint = "\n⚠️ Фильтр сейчас ВЫКЛЮЧЕН (🚦) — ТФ сохранён, но не применяется." \
            if not RSI_SIGNAL_FILTER_ENABLED else ""
        await send_message(
            f"✅ RSI-фильтр РОСТА: ТФ <b>{lbl}</b>\n"
            f"ℹ️ Действует только на сигналы РОСТА. Данные берутся напрямую "
            f"с Bybit (точный RSI по выбранному ТФ).\n"
            f"Текущий порог: ≥{RSI_SIGNAL_LEVEL}{off_hint}",
            chat_id,
        )
        return

    # ── RSI-фильтр обычных сигналов роста/падения: порог "не меньше" ──────────
    _rsi_signal_lvl_map = {
        "📊 RSI ≥50": 50.0, "📊 RSI ≥60": 60.0, "📊 RSI ≥70": 70.0,
        "📊 RSI ≥80": 80.0, "📊 RSI ≥90": 90.0, "📊 RSI ≥95": 95.0,
    }
    if text in _rsi_signal_lvl_map:
        RSI_SIGNAL_LEVEL = _rsi_signal_lvl_map[text]
        off_hint = "\n⚠️ Фильтр сейчас ВЫКЛЮЧЕН (🚦) — порог сохранён, но не применяется." \
            if not RSI_SIGNAL_FILTER_ENABLED else ""
        await send_message(
            f"✅ RSI-порог РОСТА: <b>≥{RSI_SIGNAL_LEVEL}</b>\n"
            f"ℹ️ Уведомление о РОСТЕ проходит только если RSI ≥ {RSI_SIGNAL_LEVEL}.\n"
            f"На ПАДЕНИЕ этот порог больше не действует — уведомления о падении "
            f"приходят без фильтра по RSI, значение RSI просто показывается в сообщении.\n"
            f"Работает на ТФ {RSI_TF_LABELS.get(RSI_SIGNAL_TF, RSI_SIGNAL_TF)}.{off_hint}",
            chat_id,
        )
        return

    # ── RSI-гейт РАЗВОРОТА: вкл/выкл ────────────────────────────────────────────
    if text == "🔄🚦 RSI разворота ВЫКЛ":
        RSI_REVERSAL_FILTER_ENABLED = False
        await send_message(
            "✅ RSI-гейт РАЗВОРОТА: <b>ВЫКЛЮЧЕН</b>\n"
            "ℹ️ Разворотный сигнал больше не блокируется требованием "
            "\"RSI на выбранном ТФ ≥ порога\" — решение принимает только "
            "REVERSAL_MIN_SCORE из 20 факторов. Доп. запрос к бирже за RSI на "
            "отдельном ТФ теперь пропускается; фактор 15 при этом не исчезает — "
            "использует свой запасной расчёт RSI по Min15 из уже загруженных "
            "данных (без сети).",
            chat_id,
        )
        return

    if text == "🔄🚦 RSI разворота ВКЛ":
        RSI_REVERSAL_FILTER_ENABLED = True
        await send_message(
            f"✅ RSI-гейт РАЗВОРОТА: <b>ВКЛЮЧЕН</b>\n"
            f"ℹ️ {_rsi_reversal_status_line()} — жёсткое условие, независимое "
            f"от 20 факторов: разворот проверяется, только если RSI на "
            f"выбранном ТФ достиг порога.",
            chat_id,
        )
        return

    # ── RSI-фильтр РАЗВОРОТА: таймфрейм (отдельно от обычных сигналов) ────────
    _rsi_rev_tf_map = {
        "🔄📊 RSI ТФ 5м":  "Min5",
        "🔄📊 RSI ТФ 15м": "Min15",
        "🔄📊 RSI ТФ 1ч":  "Min60",
        "🔄📊 RSI ТФ 4ч":  "Hour4",
        "🔄📊 RSI ТФ 12ч": "Hour12",
        "🔄📊 RSI ТФ 1д":  "Day1",
    }
    if text in _rsi_rev_tf_map:
        RSI_REVERSAL_TF = _rsi_rev_tf_map[text]
        lbl = RSI_TF_LABELS.get(RSI_REVERSAL_TF, RSI_REVERSAL_TF)
        off_hint = ("\n⚠️ Гейт сейчас ВЫКЛЮЧЕН (🔄🚦) — ТФ сохранён, но не применяется."
                    if not RSI_REVERSAL_FILTER_ENABLED else "")
        await send_message(
            f"✅ ТФ RSI-гейта разворота: <b>{lbl}</b>\n"
            f"ℹ️ Это жёсткое условие, НЕЗАВИСИМОЕ от 20 факторов разворота: "
            f"уведомление о развороте отправляется, только если RSI на ТФ {lbl} "
            f"≥ порога. Если условие не выполнено — разворот не проверяется вообще, "
            f"даже если факторы набрали высокий score.\n"
            f"Текущий порог: ≥{REVERSAL_RSI_OB}{off_hint}",
            chat_id,
        )
        return

    # ── RSI-фильтр РАЗВОРОТА: порог "не меньше" ────────────────────────────────
    _rsi_rev_lvl_map = {
        "🔄📊 RSI ≥50": 50.0, "🔄📊 RSI ≥60": 60.0, "🔄📊 RSI ≥70": 70.0,
        "🔄📊 RSI ≥80": 80.0, "🔄📊 RSI ≥90": 90.0, "🔄📊 RSI ≥95": 95.0,
    }
    if text in _rsi_rev_lvl_map:
        REVERSAL_RSI_OB = _rsi_rev_lvl_map[text]  # global объявлен выше — OK
        off_hint = ("\n⚠️ Гейт сейчас ВЫКЛЮЧЕН (🔄🚦) — порог сохранён, но не применяется."
                    if not RSI_REVERSAL_FILTER_ENABLED else "")
        await send_message(
            f"✅ Порог RSI-гейта разворота: <b>≥{REVERSAL_RSI_OB}</b>\n"
            f"ℹ️ Жёсткое условие, независимое от 20 факторов: уведомление о "
            f"развороте приходит, только если RSI на ТФ "
            f"{RSI_TF_LABELS.get(RSI_REVERSAL_TF, RSI_REVERSAL_TF)} ≥ {REVERSAL_RSI_OB}. "
            f"Также по-прежнему влияет на факторы 1 (Min1) и 15 (старший ТФ) "
            f"внутри 20-факторного score.{off_hint}",
            chat_id,
        )
        return

    # ── Команды настройки разворота ───────────────────────────────────────────
    _rev_cmds = {
        "/rev_score":    ("REVERSAL_MIN_SCORE",   int,   1,    20,    "Порог факторов",           "/20"),
        "/rev_rsi":      ("REVERSAL_RSI_OB",      float, 50,   95,    "RSI перекупленность",      ""),
        "/rev_stoch":    ("REVERSAL_STOCH_OB",    float, 0.5,  1.0,   "StochRSI порог",           ""),
        "/rev_bb":       ("REVERSAL_BB_OB",       float, 0.8,  1.5,   "Боллинджер %B порог",      ""),
        "/rev_accel":    ("REVERSAL_ACCEL",       float, 0.1,  1.0,   "Порог замедления",         ""),
        "/rev_momentum": ("REVERSAL_MOMENTUM",    float, -5.0, 0,     "Порог моментума",          "%"),
        "/rev_cooldown": ("REVERSAL_COOLDOWN_SEC",int,   1,    1440,  "Кулдаун разворота (мин)",  " мин"),
        "/rev_atr":      ("REVERSAL_ATR_MULT",    float, 1.0,  10.0,  "ATR-перегрев множитель",   "x"),
        "/rev_vol":      ("REVERSAL_VOL_RATIO",   float, 0.1,  1.0,   "Порог слабого объёма",     ""),
    }
    for cmd, (var, typ, vmin, vmax, label, sfx) in _rev_cmds.items():
        if text.startswith(cmd):
            try:
                raw = text.split()[1]
                val = typ(raw)
                assert vmin <= val <= vmax
                # Для кулдауна храним в секундах, но вводим/показываем в минутах
                stored = val * 60 if var == "REVERSAL_COOLDOWN_SEC" else val
                globals()[var] = stored
                await send_message(f"✅ {label}: <b>{val}{sfx}</b>", chat_id)
            except Exception:
                current = globals()[var]
                if var == "REVERSAL_COOLDOWN_SEC":
                    current = current // 60  # показываем в минутах, как вводится
                await send_message(
                    f"❌ {cmd} {vmin}…{vmax}  (текущее: {current})", chat_id
                )
            return

    if text.startswith("/rev_high"):
        try:
            val = float(text.split()[1])
            assert 0.0 <= val <= 5.0
            REVERSAL_HIGH_MARGIN = 1.0 - val / 100  # global объявлен выше — OK
            await send_message(f"✅ Зона отбоя от хая: <b>{val}%</b> (margin={REVERSAL_HIGH_MARGIN:.4f})", chat_id)
        except Exception:
            cur_pct = round((1.0 - REVERSAL_HIGH_MARGIN) * 100, 2)
            await send_message(f"❌ /rev_high 0.2  (отступ от хая в %, 0–5)\nТекущее: {cur_pct}%", chat_id)
        return

    if text.startswith("/set_percent"):
        try:
            val = float(text.split()[1])
            assert 0.01 <= val <= 100
            current_percent = val
            await send_message(f"✅ Новый порог: <b>{val}%</b>", chat_id)
        except Exception:
            await send_message("❌ /set_percent 2.5", chat_id)
        return

    if text.startswith("/set_window"):
        try:
            val = int(text.split()[1])
            assert 1 <= val <= 10080
            current_window = val * 60
            await send_message(f"✅ Новый период: <b>{val} мин</b>", chat_id)
        except Exception:
            await send_message("❌ /set_window 60  (в минутах)", chat_id)
        return

    if text == "/subscribe":
        db_add_subscriber(chat_id)
        await send_message("✅ Вы в списке получателей", chat_id)
        return

    await send_message(
        "❓ Неизвестная команда.\n/menu — открыть панель управления",
        chat_id,
        reply_markup=reply_keyboard(),
    )


async def _cmd_history(chat_id):
    rows = db_recent_alerts(10)
    if not rows:
        await send_message("📋 История пуста", chat_id)
        return
    lines = ["📋 <b>Последние 10 сигналов:</b>\n"]
    for r in rows:
        ts    = time.strftime("%d.%m %H:%M", time.localtime(r["ts"]))
        sign  = "🚀" if r["growth"] > 0 else "📉"
        rsi_s = f"RSI {r['rsi']:.1f}" if r["rsi"] is not None else "RSI —"
        lines.append(f"{sign} <b>{r['symbol']}</b> {r['growth']:+.2f}% | {rsi_s} | {ts}")
    await send_message("\n".join(lines), chat_id)


async def _cmd_top5(chat_id):
    rows = db_top_signals(5)
    if not rows:
        await send_message("🏆 Нет данных за 24ч", chat_id)
        return
    lines = ["🏆 <b>Топ-5 сигналов за 24ч:</b>\n"]
    for i, r in enumerate(rows, 1):
        ts   = time.strftime("%H:%M", time.localtime(r["ts"]))
        sign = "🚀" if r["growth"] > 0 else "📉"
        lines.append(f"{i}. {sign} <b>{r['symbol']}</b> {r['growth']:+.2f}% [{r['source']}] {ts}")
    await send_message("\n".join(lines), chat_id)


async def _cmd_reversals(chat_id):
    rows = db_recent_reversals(8)
    if not rows:
        await send_message("🔄 Разворотных сигналов пока нет", chat_id)
        return
    lines = ["🔄 <b>Последние 8 разворотов:</b>\n"]
    for r in rows:
        ts      = time.strftime("%d.%m %H:%M", time.localtime(r["ts"]))
        score   = r["score"]
        lvl     = "🔴" if score >= 12 else "🟠" if score >= 7 else "🟡"
        factors = r["factors"]
        # Экранируем HTML-символы в тексте фактора
        f_brief = factors[0].replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;") if factors else "—"
        t1_s    = f" → цель {r['target1']:.4g}" if r.get("target1") else ""
        lines.append(
            f"{lvl} <b>{r['symbol']}</b> [{score}/20] {ts}{t1_s}\n"
            f"   <i>{f_brief}</i>"
        )
    await send_message("\n".join(lines), chat_id)


async def _cmd_reversal_settings(chat_id):
    high_pct = round((1.0 - REVERSAL_HIGH_MARGIN) * 100, 2)
    await send_message(
        f"⚙️ <b>Настройки детектора разворота (20 факторов)</b>\n"
        f"━━━━━━━━━━━━━━━━━━━━━━\n\n"
        f"<b>Скоринг:</b>\n"
        f"  Порог:         <code>{REVERSAL_MIN_SCORE}/20</code>  → /rev_score 5\n\n"
        f"<b>RSI-гейт разворота (НЕЗАВИСИМ от факторов):</b>\n"
        f"  Статус: {_rsi_reversal_status_line()}  → кнопки 🔄🚦 RSI разворота ВЫКЛ/ВКЛ\n"
        f"  ТФ и порог:  → кнопки 🔄📊 RSI ТФ / RSI ≥\n"
        f"  Пока гейт включён — без выполнения этого условия разворотный сигнал "
        f"не отправляется, даже при высоком score. Кнопкой 🔄🚦 можно полностью "
        f"отключить проверку и порог/ТФ — тогда решение принимают только "
        f"20 факторов.\n\n"
        f"<b>Пороги факторов:</b>\n"
        f"  RSI OB (1m):   <code>&gt; {REVERSAL_RSI_OB}</code>     → /rev_rsi 70\n"
        f"  StochRSI:      <code>&gt; {REVERSAL_STOCH_OB}</code>   → /rev_stoch 0.80\n"
        f"  Боллинджер:    <code>&gt; {REVERSAL_BB_OB}</code>      → /rev_bb 1.0\n"
        f"  Замедление:    <code>&lt; {REVERSAL_ACCEL}</code>      → /rev_accel 0.5\n"
        f"  Моментум:      <code>&lt; {REVERSAL_MOMENTUM}%</code>  → /rev_momentum -0.5\n"
        f"  ATR-перегрев:  <code>&gt; {REVERSAL_ATR_MULT}×</code> → /rev_atr 3.0\n"
        f"  Объём слабый:  <code>&lt; {REVERSAL_VOL_RATIO:.0%}</code>  → /rev_vol 0.7\n"
        f"  Зона хая 24h:  <code>{high_pct}%</code>        → /rev_high 0.2\n"
        f"  Wick Rejection:<code>&gt; 0.55</code>         (фиксировано)\n\n"
        f"<b>Факторы 13-16:</b>\n"
        f"  13. OBV-дивергенция (Min15, цена↑ OBV↓)\n"
        f"  14. Wick Rejection Ratio &gt;0.55 (Min1)\n"
        f"  15. RSI {RSI_TF_LABELS.get(RSI_REVERSAL_TF, RSI_REVERSAL_TF)} &gt;{REVERSAL_RSI_OB} (настраиваемый старший таймфрейм)\n"
        f"  16. Lower Highs (Min5, 3+ убывающих хая)\n\n"
        f"<b>Факторы 17-20:</b>\n"
        f"  17. MACD гистограмма падает (Min1)\n"
        f"  18. Цена растянута &gt;1.5% над EMA21 (Min5)\n"
        f"  19. Серия из 3+ красных свечей (Min1)\n"
        f"  20. StochRSI разворот вниз из экстремума (Min1)\n\n"
        f"<b>Кулдаун и фильтр дублей:</b>\n"
        f"  Кулдаун:         <code>{REVERSAL_COOLDOWN_SEC // 60} мин</code>  → /rev_cooldown 30\n"
        f"  Мин. смена цены: <code>{REVERSAL_REPEAT_PRICE_PCT}%</code>  → /rev_price_pct 3\n"
        f"  Мин. рост скора: <code>+{REVERSAL_REPEAT_SCORE_DELTA}</code>      → /rev_score_delta 2\n\n"
        f"<b>Пресеты:</b>\n"
        f"  Агрессивный: /rev_score 3  /rev_cooldown 10  /rev_price_pct 1\n"
        f"  Стандарт:    /rev_score 7  /rev_cooldown 30  /rev_price_pct 3\n"
        f"  Строгий:     /rev_score 12  /rev_cooldown 60  /rev_price_pct 5",
        chat_id,
    )


async def _cmd_full_restart(chat_id):
    """Перезапускает asyncio-задачу monitor() без потери price_history/БД/настроек."""
    try:
        await restart_monitor(reason=f"кнопка перезапуска (chat_id={chat_id})")
        await send_message(
            "✅ <b>Мониторинг перезапущен</b>\n"
            f"🪙 Монет в истории: {len(price_history)} (сохранены)\n"
            f"🔔 Сигналов всего: {signals_count} | 🔄 Разворотов: {reversal_count}",
            chat_id,
        )
    except Exception as e:
        log.exception("_cmd_full_restart: %s", e)
        await send_message(f"❌ Ошибка перезапуска: {_esc(str(e))}", chat_id)


async def _cmd_export(chat_id):
    await send_message("📤 Генерирую CSV...", chat_id)
    csv_data = db_export_csv(500)
    fname    = f"alerts_{time.strftime('%Y%m%d_%H%M%S')}.csv"
    await send_document(chat_id, fname, csv_data, caption="📤 Последние 500 алертов")


# ================================================================
#  TELEGRAM LOOP
# ================================================================

async def get_updates() -> tuple[list, bool]:
    """Возвращает (updates, ok). ok=False при ошибке (включая 409 Conflict)."""
    global offset
    try:
        async with _session.get(
            f"{TG}/getUpdates",
            params={"timeout": 30, "offset": offset},
            timeout=aiohttp.ClientTimeout(total=35),
        ) as resp:
            data = await resp.json()
        if data.get("ok"):
            results = data["result"]
            if results:
                log.info("get_updates: получено %d обновлений, offset=%s", len(results), offset)
            return results, True
        else:
            log.warning("get_updates not ok: %s", data)
            return [], False
    except Exception as e:
        log.error("get_updates: %s", e)
        return [], False


async def telegram_loop():
    global offset
    # При ошибках (включая 409 Conflict) делаем экспоненциальный backoff,
    # вместо мгновенного повтора каждые 0.2с. Без этого бот при конфликте
    # двух инстансов бомбардирует Telegram запросами ~5 раз в секунду,
    # что не даёт конфликту разрешиться и забивает логи.
    backoff_sec      = 1.0
    BACKOFF_MAX      = 30.0
    consecutive_409  = 0
    CONFLICT_WARN_EVERY = 10  # предупреждать в чат не на каждой ошибке, а раз в N подряд

    while True:
        try:
            updates, ok = await get_updates()
            if ok:
                if backoff_sec != 1.0 or consecutive_409 > 0:
                    log.info("get_updates восстановлен после %d ошибок подряд", consecutive_409)
                backoff_sec     = 1.0
                consecutive_409 = 0
                for update in updates:
                    offset = update["update_id"] + 1
                    log.info("UPDATE: %s", update)
                    if "message" in update:
                        asyncio.create_task(handle_message(update["message"]))
                    elif "callback_query" in update:
                        asyncio.create_task(handle_message(update["callback_query"]["message"]))
                await asyncio.sleep(0.2)
            else:
                consecutive_409 += 1
                if consecutive_409 == CONFLICT_WARN_EVERY:
                    # Шлём владельцу предупреждение один раз, не на каждую ошибку,
                    # чтобы не заспамить чат при длительном конфликте инстансов.
                    try:
                        await send_message(
                            "⚠️ <b>Постоянный 409 Conflict от Telegram</b>\n"
                            f"Уже {consecutive_409} ошибок подряд.\n"
                            "Похоже, где-то ещё запущен второй процесс этого бота "
                            "с тем же токеном (другой сервер/Railway-сервис/локальный запуск). "
                            "Найдите и остановите дублирующий инстанс.",
                            int(config.CHAT_ID),
                        )
                    except Exception:
                        pass  # если и эта отправка не проходит — не критично, увидим в логах
                await asyncio.sleep(backoff_sec)
                backoff_sec = min(BACKOFF_MAX, backoff_sec * 2)
        except Exception as e:
            log.exception("telegram_loop: %s", e)
            await asyncio.sleep(backoff_sec)
            backoff_sec = min(BACKOFF_MAX, backoff_sec * 2)


# ================================================================
#  BACKGROUND TASKS
# ================================================================

async def heartbeat():
    while True:
        log.info("♥ alive | signals=%d | reversals=%d | coins=%d",
                 signals_count, reversal_count, len(price_history))
        await asyncio.sleep(300)


async def save_state_loop():
    while True:
        try:
            save_state()
        except Exception:
            pass
        await asyncio.sleep(30)


async def db_cleanup_loop():
    last_vacuum = time.time()
    while True:
        await asyncio.sleep(6 * 3600)
        try:
            deleted = db_cleanup()
            total   = sum(deleted.values())
            stats   = db_stats()
            log.info("DB cleanup: удалено %d | %.2f МБ | алертов %d",
                     total, stats["size_mb"], stats["alerts"])
            now = time.time()
            if total > 0 and now - last_vacuum >= DB_VACUUM_INTERVAL_H * 3600:
                log.info("DB VACUUM...")
                await asyncio.get_event_loop().run_in_executor(None, db_vacuum)
                last_vacuum = now
                log.info("VACUUM done | %.2f МБ", db_size_mb())
        except Exception as e:
            log.exception("db_cleanup_loop: %s", e)


# ================================================================
#  WATCHDOG
# ================================================================

WATCHDOG_TIMEOUT = 90


async def restart_monitor(reason: str = "ручной перезапуск") -> None:
    """
    Безопасно перезапускает задачу monitor() БЕЗ сброса данных:
    price_history, БД, настройки (current_percent, REVERSAL_*, кулдауны)
    остаются нетронутыми — только пересоздаётся asyncio.Task.
    """
    global monitor_task, last_check_time
    log.warning("Перезапуск monitor(): %s", reason)
    if monitor_task and not monitor_task.done():
        monitor_task.cancel()
        try:
            await monitor_task
        except asyncio.CancelledError:
            pass
        except Exception as e:
            log.exception("restart_monitor: ошибка при отмене старой задачи: %s", e)
    monitor_task    = asyncio.create_task(monitor())
    last_check_time = time.time()


async def watchdog():
    global monitor_task, last_check_time
    await asyncio.sleep(60)
    while True:
        try:
            if not monitor_paused:
                stall = (time.time() - last_check_time) if last_check_time else 0
                if stall > WATCHDOG_TIMEOUT:
                    log.warning("Watchdog: завис %.0f с — перезапуск", stall)
                    await broadcast(f"⚠️ Watchdog: завис {stall:.0f}с. Перезапуск...")
                    await restart_monitor(reason=f"watchdog: завис {stall:.0f}с")
                    await broadcast("✅ Monitor перезапущен")
        except Exception as e:
            log.exception("watchdog: %s", e)
        await asyncio.sleep(30)


# ================================================================
#  SHUTDOWN
# ================================================================

_shutdown_event: asyncio.Event | None = None


def notify_sync(text: str) -> None:
    """
    Отправляет сообщение в Telegram БЕЗ asyncio/aiohttp — синхронно, через
    стандартную библиотеку (urllib). Нужна там, где event loop уже
    останавливается/остановлен или сессия aiohttp недоступна: критический
    сбой во внешнем retry-цикле (см. низ файла), сигнал завершения,
    неперехваченное исключение до входа в основной async-код. Никогда не
    бросает исключения наружу — по дизайну "бот не работает" не должно само
    по себе уронить процесс ещё раз.
    """
    try:
        import urllib.request
        import urllib.parse
        data = urllib.parse.urlencode({
            "chat_id": int(config.CHAT_ID),
            "text": text,
            "parse_mode": "HTML",
        }).encode()
        req = urllib.request.Request(f"{TG}/sendMessage", data=data)
        urllib.request.urlopen(req, timeout=10)
    except Exception as e:
        try:
            log.warning("notify_sync: не удалось отправить сообщение в Telegram: %s", e)
        except Exception:
            pass


def _handle_signal(sig):
    log.info("Signal %s — завершение", sig)
    save_state()
    notify_sync(f"🛑 <b>Бот останавливается</b> (сигнал {getattr(sig, 'name', sig)}) — настройки сохранены.")
    if _shutdown_event:
        _shutdown_event.set()


def handle_async_exception(loop, context):
    exc = context.get("exception")
    if exc:
        log.exception("Unhandled async exception", exc_info=exc)
    else:
        log.error("Async error: %s", context["message"])


# ================================================================
#  MAIN
# ================================================================

async def health_server():
    """
    v25.5: лёгкий healthcheck HTTP-эндпоинт GET /health — отдельно от
    Prometheus (:PROM_PORT/metrics), на своём порту (:HEALTH_PORT, по
    умолчанию 8081). Нужен для внешнего мониторинга (Railway healthcheck,
    UptimeRobot и т.п.), которому не всегда удобно парсить формат Prometheus.

    Полностью изолирован от остальных фоновых тасков — как и
    liquidation_listener(): если он не смог стартовать (например, порт
    занят) или упал, это НЕ должно приводить к перезапуску всего бота,
    поэтому все исключения ловятся внутри и только логируются.
    """
    async def _health_handler(request):
        now   = time.time()
        stall = (now - last_check_time) if last_check_time else None
        healthy = (
            not monitor_paused
            and (stall is None or stall < WATCHDOG_TIMEOUT)
        )
        payload = {
            "status":                 "ok" if healthy else "degraded",
            "paused":                 monitor_paused,
            "uptime_sec":             round(now - start_time, 1),
            "checks_count":           checks_count,
            "signals_count":          signals_count,
            "reversal_count":         reversal_count,
            "last_check_age_sec":     round(stall, 1) if stall is not None else None,
            "symbols_tracked":        len(price_history),
            "bybit_blocked":          _bybit_block_active(),
            "bybit_block_remaining_sec": round(_bybit_block_remaining(), 1),
        }
        return web.json_response(payload, status=200 if healthy else 503)

    app = web.Application()
    app.router.add_get("/health", _health_handler)
    app.router.add_get("/", _health_handler)
    runner = web.AppRunner(app)
    try:
        await runner.setup()
        site = web.TCPSite(runner, "0.0.0.0", HEALTH_PORT)
        await site.start()
        log.info("Health-эндпоинт: http://0.0.0.0:%d/health", HEALTH_PORT)
    except Exception as e:
        log.warning("Health-сервер не запущен (не критично, бот продолжает работать): %s", e)
        return
    try:
        # Держим таск живым до отмены снаружи (main() отменяет его при остановке).
        await asyncio.Event().wait()
    except asyncio.CancelledError:
        pass
    finally:
        try:
            await runner.cleanup()
        except Exception:
            pass


async def main():
    global monitor_task, _session, _shutdown_event, _liq_task, _health_task

    _shutdown_event = asyncio.Event()
    loop = asyncio.get_running_loop()
    loop.set_exception_handler(handle_async_exception)

    for sig in (signal.SIGTERM, signal.SIGINT):
        loop.add_signal_handler(sig, _handle_signal, sig)

    if PROM_AVAILABLE:
        prom_port = getattr(config, "PROM_PORT", 8000)
        try:
            start_http_server(prom_port)
            log.info("Prometheus on :%d", prom_port)
        except Exception as e:
            log.warning("Prometheus failed: %s", e)

    connector = aiohttp.TCPConnector(limit=50, ttl_dns_cache=300)
    async with aiohttp.ClientSession(connector=connector) as session:
        _session     = session
        monitor_task = asyncio.create_task(monitor())
        # v25.1: ликвидации — полностью изолированная фоновая задача. Специально
        # НЕ добавляется в список tasks ниже (который слушается через
        # FIRST_COMPLETED): она сама ловит все свои ошибки и никогда не должна
        # приводить к перезапуску всего бота, если WS-поток ликвидаций недоступен.
        _liq_task = asyncio.create_task(liquidation_listener())
        # v25.5: healthcheck-сервер — тоже полностью изолирован от FIRST_COMPLETED,
        # по тем же причинам, что и _liq_task (см. комментарий выше).
        _health_task = asyncio.create_task(health_server())

        # Удаляем вебхук если активен (иначе getUpdates не работает)
        try:
            async with session.get(f"{TG}/deleteWebhook", params={"drop_pending_updates": "false"}) as resp:
                result = await resp.json()
                if result.get("ok"):
                    log.info("Вебхук удалён успешно")
                else:
                    log.warning("deleteWebhook: %s", result)
        except Exception as e:
            log.warning("deleteWebhook error: %s", e)

        # Отправляем клавиатуру владельцу при каждом запуске бота
        try:
            await send_message(
                "🤖 <b>Бот запущен</b> — клавиатура активирована",
                int(config.CHAT_ID),
                reply_markup=reply_keyboard(),
            )
        except Exception as e:
            log.warning("Не удалось отправить стартовое сообщение: %s", e)

        shutdown_task = asyncio.create_task(_shutdown_event.wait())
        tasks = [
            monitor_task,
            asyncio.create_task(telegram_loop()),
            asyncio.create_task(heartbeat()),
            asyncio.create_task(save_state_loop()),
            asyncio.create_task(db_cleanup_loop()),
            asyncio.create_task(watchdog()),
            shutdown_task,
        ]

        done, pending = await asyncio.wait(tasks, return_when=asyncio.FIRST_COMPLETED)

        # graceful=True  → завершение запросил SIGTERM/SIGINT (штатная остановка/
        #                   редеплой на Railway) — внешний while True не должен
        #                   перезапускать процесс, иначе SIGTERM никогда не даст
        #                   контейнеру закрыться чисто и Railway придётся его
        #                   убивать по SIGKILL после таймаута.
        # graceful=False → завершился один из фоновых тасков сам по себе — это
        #                   НЕНОРМАЛЬНО (раньше падало молча: asyncio.wait не
        #                   поднимает исключение из done-таска, если его явно не
        #                   проверить, поэтому monitor()/telegram_loop() и т.п.
        #                   могли упасть, всё тихо сворачивалось и стартовало
        #                   заново без единой строки в логах и без паузы —
        #                   именно так бот мог выглядеть как "не работает
        #                   постоянно" / "засыпает" при частых сбоях).
        graceful = shutdown_task in done
        if not graceful:
            for t in done:
                if t is shutdown_task:
                    continue
                if t.cancelled():
                    continue
                exc = t.exception()
                if exc:
                    log.error("Фоновый таск упал с исключением — это и есть причина внезапного рестарта:", exc_info=exc)
                else:
                    log.error("Фоновый таск завершился сам по себе (return вместо бесконечного цикла) — тоже приводит к рестарту всего бота")
            try:
                await broadcast("⚠️ Один из фоновых процессов упал — бот перезапускается автоматически.")
            except Exception:
                pass

        log.info("Завершение...")
        for t in pending:
            if not t.done():
                t.cancel()
        await asyncio.gather(*pending, return_exceptions=True)

        # v25.1: _liq_task не входит в tasks/pending (специально изолирован от
        # FIRST_COMPLETED выше) — отменяем его отдельно, чтобы не оставить
        # висящую WS-задачу при штатном завершении процесса.
        if _liq_task and not _liq_task.done():
            _liq_task.cancel()
            try:
                await _liq_task
            except (asyncio.CancelledError, Exception):
                pass

        # v25.5: аналогично для health-сервера.
        if _health_task and not _health_task.done():
            _health_task.cancel()
            try:
                await _health_task
            except (asyncio.CancelledError, Exception):
                pass

    log.info("Бот остановлен")
    return graceful


# ── Захватываем single-instance lock ОДИН раз на весь процесс, ДО входа
#    в retry-цикл. Если лок брать внутри main(), при каждом внутреннем
#    перезапуске (после необработанного исключения) возникало бы окно
#    release→acquire, в которое мог проскочить второй конкурентный процесс.
#
# v25.5: всё это обёрнуто в `if __name__ == "__main__":` — раньше выполнялось
# безусловно при любом импорте модуля (включая `import main_4` для юнит-тестов
# форматирующих функций), из-за чего сам факт импорта пытался захватить
# файловый лок и запускал бесконечный цикл поллинга Telegram. Поведение при
# обычном запуске (`python main_4.py`, именно так стартует Railway) не
# меняется — там __name__ всегда равен "__main__".
if __name__ == "__main__":
    acquire_single_instance_lock()
    try:
        while True:
            try:
                graceful = asyncio.run(main())
                if graceful:
                    # Штатная остановка по SIGTERM/SIGINT (например, редеплой или
                    # ручная остановка на Railway) — процесс должен реально
                    # завершиться, а не запускаться заново сам на себя.
                    log.info("Штатная остановка — процесс завершается")
                    notify_sync("🛑 <b>Бот остановлен</b> (штатное завершение процесса).")
                    break
                # Один из фоновых тасков упал/вышел неожиданно (см. лог выше) —
                # ведём себя так же, как при необработанном исключении: пауза,
                # чтобы не уйти в горячий цикл рестартов, если сбой повторяется
                # на каждой итерации (например, сразу после старта).
                log.warning("Внутренний перезапуск после сбоя фонового таска — пауза 10с")
                notify_sync("🔴 <b>Бот сейчас не работает</b> — внутренний перезапуск после сбоя фонового процесса. Настройки сохранены, перезапуск через 10с...")
                time.sleep(10)
            except KeyboardInterrupt:
                notify_sync("🛑 <b>Бот остановлен</b> вручную (Ctrl+C).")
                break
            except Exception as e:
                log.exception("Критическая ошибка: %s", e)
                notify_sync(f"🔴 <b>Бот сейчас не работает</b> — критическая ошибка:\n<code>{_esc(str(e))[:500]}</code>\nАвтоматический перезапуск через 10с...")
                time.sleep(10)
    finally:
        db_close()
        release_single_instance_lock()
